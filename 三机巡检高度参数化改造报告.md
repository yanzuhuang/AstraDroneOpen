# 三机巡检高度参数化改造报告

日期：2026-09-18。分支：`三机巡检最小可运行整理`。基准 HEAD：`fa186ea8bbbf6fd6aa3d2ae0bff1e0cf65f6aa75`。

本轮基于《三机巡检最小可运行代码审计.md》完成高度配置入口和配置合同检查。没有删除项目文件，没有修改 EGO 核心、任务状态机、协调算法、world、外部 PX4，没有暂存或提交。开始时已有的两个未跟踪用户文档保持原样。

## 1. 修改文件

以下 bringup 路径均位于 `AstraDrone_ros1_ws/src/Swarm/astra_swarm_bringup/`。

| 文件 | 修改 |
|---|---|
| `launch/triple_tower_height_profile.launch`（新增） | 三个明确 profile；复用现有同层/异层 launch；联动高度、任务/EGO/bridge 包络、恢复和备用返航高度、30 m 起飞超时 |
| `launch/triple_tower_multi_height_inspection.launch` | 暴露每机 top/takeoff/staging/ascent 参数；默认仍为 26/20/14 m；按首层筛选 10/18 m 中间点 |
| `scripts/check_height_profile.py`（新增） | 仅加载 roslaunch 配置，校验最终任务/桥/地图/manager 高度合同；不运行 launch 节点 |
| `test/test_height_profiles.py`（新增） | 三 profile、逐参数非高度不变性、top 单改、错误 staging/ascent/layer、manager 错配、超时、未知 profile 回归 |
| `CMakeLists.txt` | 安装高度合同检查脚本；新增测试由已有 test 目录发现机制执行 |
| `scripts/run_sh/three_uav/three_uav_inspection.sh` | `--profile`、`--check-height-profile`、启动前校验、记录 profile 与合同；移除环境变量选高度路径 |
| `scripts/run_sh/three_uav/three_uav_multi_height_inspection.sh` | 通过显式参数调用共享入口，保留原单层/两层操作 |
| `scripts/run_sh/README.md` | 三 profile、检查和人工启动命令 |
| 本报告 | 参数、验证证据及后续人工飞行顺序 |

原 `triple_tower_inspection.launch`、`dual_tower_inspection.launch`、`uav_tower_stack.launch`、任务/bridge/EGO/manager 源码与 YAML 均未修改。新 profile 在 include 后明确覆盖高度参数，因此不依赖 YAML 残留高度。旧底层 launch 仍保留给现有调用者；正式高度选择应使用新入口，而非手工只改底层 top。

## 2. 最终生效参数

表中三元值顺序固定为 UAV1 / UAV2 / UAV3；数值单位 m，时间单位 s。参数以最终展开 YAML 为准。

| 参数 | same_3m | same_30m | multi_height_low_3m |
|---|---|---|---|
| top / 首层 / entry 目标高度 | 3 / 3 / 3 | 30 / 30 / 30 | 15 / 9 / 3 |
| takeoff / mission transit / bridge return | 3 / 3 / 3 | 30 / 30 / 30 | 4 / 4 / 2.5 |
| staging | 2.5 / 2.5 / 2.5 | 29.5 / 29.5 / 29.5 | 4 / 4 / 2.5 |
| ascent intermediate | [] / [] / [] | [] / [] / [] | [10] / [] / [] |
| layer offsets（每机） | [0] | [0] | [0] |
| candidate height offsets（每机） | [0] | [0] | [0] |
| mission minimum | 1 / 2 / 2 | 1 / 2 / 2 | 1 / 2 / 2 |
| mission maximum / mission virtual ceiling | 4.5 / 4.5 / 4.5 | 31.5 / 31.5 / 31.5 | 16.5 / 10.5 / 4.5 |
| EGO ground | 1 / 2 / 2 | 1 / 2 / 2 | -0.5 / -0.5 / -0.5 |
| EGO ceiling | 4.5 / 4.5 / 4.5 | 31.5 / 31.5 / 31.5 | 16.5 / 10.5 / 4.5 |
| bridge min relative height（每机） | 0.3 | 0.3 | 0.3 |
| bridge max relative height | 4.5 / 4.5 / 4.5 | 31.5 / 31.5 / 31.5 | 16.5 / 10.5 / 4.5 |
| recovery height / recovery height max / return_egress transit | 3 / 3 / 3 | 30 / 30 / 30 | 15 / 9 / 3 |
| bridge takeoff timeout（每机） | 60 | 120 | 60 |
| manager mission heights | [3,3,3] | [30,30,30] | [15,9,3] |
| manager takeoff heights | [3,3,3] | [30,30,30] | [4,4,2.5] |
| multi_layer_enabled | false | false | false |
| low_altitude/enabled（现有同层路径分支名称） | true | true | false |

共同保持：每层 1 圈、planned_cycles=1、下降中间候选 `[18,10]`（运行时按当前层和 bridge return 高度过滤，低机不会反向升到 18 m）；EGO map XYZ=90×90×46 m、resolution=0.25 m。同层模式跳过高空 STAGING/SEGMENTED_CLIMB，因此 staging 是初始化合同值，不是起飞后下降 0.5 m 的指令。

bridge 真正读取的高度界限是 `min_relative_height/max_relative_height`。原 bridge YAML 中的 `virtual_ceil_height=45` 未在 bridge C++ 中读取，不作为生效安全边界；本轮不以修改这个无效字段代替修改真实 bounds。当前 spawn/map/home z 契约保持不变，relative bounds 的对应关系不外推为任意真机 home 高度。

## 3. 3 m → 30 m 的联动

同一套同层任务代码，选择 `same_30m` 后：top、takeoff、mission transit、bridge return、entry、manager/safety/可视化的 mission heights 同步到 30；staging=top−0.5；任务与 EGO ceiling、bridge 上界均为 top+1.5；恢复高度、恢复上限及备用返航高度采用本机 top。layers/candidate offsets 保持 `[0]`，不带入下降层或下降候选。

起飞 position slew-rate 保持原默认 0.5 m/s；30 m 的理想爬升时间约 60 s，所以仅该 profile 把起飞超时设为 120 s，未提高速度或放宽跟踪/碰撞门。其他任务时间预算保持原值；120 s 是否足以覆盖实际飞控响应仍需飞行验证。

新 `same_3m` 同样收敛高度合同：旧 mission 上界 45、bridge 上界 40、recovery 5/max17、备用返航 6 不再与 EGO 4.5 m ceiling 混用。因此它保留原流程，但也必须作为本次改造后的基线先做人工回归，不能直接继承旧飞行 PASS。

## 4. 异层最低首层 3 m

选择 15/9/3 m 是把原 26/20/14 m 整体降低 11 m，保留每相邻机 6 m 名义高度差。角色仍是 UAV3 leader、UAV2 middle、UAV1 trailing，未按高度重新分配角色。

每机 takeoff=staging=min(4, top−0.5)。上升中间点只保留满足 `staging < h < top` 的原 10/18 m 候选，任务代码仍负责追加 top：

- UAV1：4 → 10 → 15 m。
- UAV2：4 → 9 m。
- UAV3：2.5 → 3 m。

这满足原有 `minimum ≤ staging < top` 和严格递增上升合同，没有绕过任何初始化检查。三个新 profile 均固定单层，CLI 拒绝 `--multi-layer`，防止 3 m 首层再套 -4 m offset。原异层两层 26→22 / 20→16 / 14→10 保留为 `multi_height_legacy`，旧异层脚本仍可使用。

每段仍由原地图、净空和通道检查决定能否飞行；配置有效不证明低机当前 staging XY、进场或返航通道可达。尤其异层 profile 仍继承既有 sector 配置和高空候选策略，本轮未改成同层低空策略；低高度下的几何/地图表现必须单独实飞核验。

## 5. 一键命令

从 `/home/yanzu/AstraDroneOpen` 执行。仅检查，不启动节点：

```bash
./scripts/run_sh/three_uav/three_uav_inspection.sh --profile same_3m --check-height-profile
./scripts/run_sh/three_uav/three_uav_inspection.sh --profile same_30m --check-height-profile
./scripts/run_sh/three_uav/three_uav_inspection.sh --profile multi_height_low_3m --check-height-profile
```

下列为下一步人工飞行命令，本轮未执行：

```bash
./scripts/run_sh/three_uav/three_uav_inspection.sh --profile same_3m --control --gui --rviz --record light
./scripts/run_sh/three_uav/three_uav_inspection.sh --profile same_30m --control --gui --rviz --record light
./scripts/run_sh/three_uav/three_uav_inspection.sh --profile multi_height_low_3m --control --gui --rviz --record light
```

旧异层两层兼容入口：

```bash
./scripts/run_sh/three_uav/three_uav_multi_height_inspection.sh --multi-layer --control --gui --rviz --record light
```

`ASTRA_THREE_UAV_LAUNCH` / `ASTRA_MULTI_LAYER_ENABLED` 不再参与选择。`ASTRA_PX4_ROOT` 仍仅用于指定已有 PX4 安装位置，不参与高度合同。默认仍无控制、无 GUI/RViz、record none；不带 `--check-height-profile` 的无控制入口仍会启动仿真，不能当静态检查命令。

light/full 在原输出目录额外记录 profile 和 `height_contract.txt`，原 bag topics 与录制/收尾逻辑保持不变。需要点云定位通道失败时人工改用 `--record full`。待落地解除武装后 Ctrl+C 收尾；Ctrl+C 不是返航指令。

## 6. 静态与测试证据

工件根目录：`runtime_artifacts/height_parameterization_20260918/`，不提交 Git。

- shell `bash -n`、launch XML/roslaunch 解析、`git diff --check` 通过。
- 三个 profile 执行完整 `roslaunch --dump-params`（包含仿真节点定义，但不运行节点），输出 `*_full_params.yaml`；`param command` 仅执行已检查的模型/传感器 xacro 展开，不启动 Gazebo/PX4。
- 三个 wrapper `--check-height-profile` 通过；测试刻意设置错误残留 `ASTRA_THREE_UAV_LAUNCH` 和 `ASTRA_MULTI_LAYER_ENABLED=true`，最终仍是指定 profile 单层参数，见 `*_check.log`。
- 新增合同测试 6/6，既有 launch parity 测试 4/4，见 `bringup_tests_delivery.log`。新旧最终参数逐键比较，所有差异仅在明确列出的高度/高度超时字段；速度、安全距离、角色、任务算法配置未变化。首次测试的包搜索路径缓存问题与缺少 manager 高度字段白名单均已修正；原日志保留。
- CLI 错误参数拒绝 5/5：未知/缺失 profile、same_30m 或 multi_height_low_3m 搭配多层、异层搭配 Learning Speed，均在启动前 exit 2；见 `cli_rejection.log`。
- manager 既有测试 80/80，见 `manager_tests.log`。
- 任务包重新构建并执行 101 项 C++ 单测（tower_route 8、ego_task_utils 10、inspection_candidate_planner 79、mission_completion 4），全部通过。两项既有无控制 rostest 均通过，见 `mission_tests.log` 与 `mission_rostest_final.log`。首次 rostest 因沙箱网络权限失败，随后仅对已授权本地无控制 ROS 测试使用审批后的权限；未改测试断言或业务算法。`catkin_test_results` 汇总为 206/0/0，包含 gtest/rosunit/rostest 嵌套统计，不将其写成 206 个独立场景。
- 额外无控制构造初始化测试：从三个最终 YAML 提取九组任务参数，仅启动九个 `sector_inspection_mission_node`，全部 `enable_control=false`，没有 bridge/EGO/FAST-LIO/Gazebo/PX4/MAVROS 节点；9/9 均收到 `WAIT_INPUTS`。见 `height_initialization.test`、`check_initialization.py`、`initialization_delivery.log`。该测试使用独立测试 namespace、wall time 和 `/dev/null` 报告目标，证明当前任务构造器接受配置，不证明原命名空间通信或飞行闭环。临时测试包注册前两次启动失败日志保留，最终有效运行未绕过节点初始化。

结论：**配置合同、源码回归及无控制任务初始化 PASS；Gazebo/SITL 实际飞行待验证。** 未宣称三种高度均已飞通。

## 7. 下一步人工飞行验证顺序

1. **same_3m 回归**：检查三机起飞高度、UAV3→UAV2→UAV1 放行、各独立 360°/8 扇区、EXIT、返航、落地解除武装；确认新高度包络未破坏已有流程。
2. **same_30m**：在上一项通过后单独新 run；重点检查 30 m 起飞耗时、实际高度/跟踪、地图与 ceiling、三机 ENTRY/相位、完整圈与返航降落。起飞超时或地图通道失败必须保留真实结果。
3. **multi_height_low_3m**：在前述回归完成后单独新 run；重点检查最低 UAV3 2.5→3 m、UAV1 4→10→15、UAV2 4→9；manager 按各机高度就绪，三机单圈后结束，无下一层负高度；检查高空策略在 3 m 的实际通道、安全距离和返航。

每次先检查该 profile 合同；使用唯一结果目录和显式 light/full。全过程仍要求 3D 最小距离门限 3.0 m、EGO swarm_clearance=1.5 m，角色顺序 `[3,2,1]`，速度 0.60 m/s、acceleration 0.50 m/s²、layer transition speed 0.30 m/s，以及原塔中心/world。任何初始化之外的真实规划、安全或任务失败都不能包装成成功；定位原因后另行决定是否修复和重跑。

本轮到此停止，不自动执行上述飞行命令，不提交 commit，等待人工审阅。
