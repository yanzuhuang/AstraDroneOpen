# 净空规则审计报告

日期：2026-08-02  
审计范围：原稳定单机阶段三、三机接入前的 `ego-swarm`、当前 `ego-swarm` 及未提交动态 ENTRY 工作区  
审计方式：只读源码、配置、launch、测试和 Git 历史审计；未修改代码或参数，未运行仿真

## 1. Git 基线与版本边界

- 当前分支：`ego-swarm`
- 当前 HEAD：`1275d3645805f5c5bca59204264b99dad571dd63`
- 分支状态：相对 `origin/ego-swarm` 领先 8 个提交
- 原稳定单机阶段三：`ego-project@b882b98`，低空核心来自父提交 `812006a`
- 三机接入前的 `ego-swarm`：`488ddfc`
- 三机安全集成提交：`9ce4670`
- 当前提交：`1275d36`
- 当前动态 ENTRY：HEAD 之上的未提交工作区修改，关键代码 `git blame` 为 `Not Committed Yet`

`b882b98` 与 `488ddfc` 是从共同祖先 `a9252f1` 分出的平行实现，规则基本一致，但不是直接父子版本。

审计时工作区已有多处修改、删除和未跟踪测试证据，均视为用户资产。本报告不恢复、覆盖或清理这些内容。

## 2. 审计结论

1. 原稳定单机的普通巡塔扇区具有完整的“同扇区候选生成、硬端点评估、软走廊风险评分、EGO 不可达后换候选”机制。
2. 原低空方案允许在同一扇区内改变角度和半径，但不改变 3 m 巡塔高度；原高空方案还允许临时下降 1～3 m。
3. 原方案虽然已有 `minimum_clearance: 1.0`，但对已经由 EGO 膨胀的 occupied map 实际使用 `map_additional_clearance: 0.5 m`。
4. 当前第五阶段动态 ENTRY 把 `candidate/minimum_clearance=1.0 m` 直接用于已膨胀地图的三个端点和完整走廊。这是本轮未提交改造新增的规则语义，不是原单机 live map 规则。
5. 当前 UAV1 第一巡塔点只在 `12.5 m` 半径、`3.0 m` 高度上搜索 `285°～300°` 七个角度。所有点在评分前因 occupied-map 距离小于 `1.0 m` 被拒绝，所以原来的半径重定位和 EGO 可达性验证没有机会执行。
6. 当前普通后续巡塔扇区的自适应代码仍然存在；问题集中在动态 ENTRY 和第一巡塔点使用了另一条固定半径、硬预筛代码路径。

## 3. 原稳定单机扇区内自适应航点规则

### 3.1 候选搜索范围

原稳定低空配置位于：

`b882b98:AstraDrone_ros1_ws/src/MissionControl/astra_tower_mission/config/low_altitude_inspection.yaml:34-38,135-156`

| 参数 | 数值 | 单位/含义 |
|---|---:|---|
| `mission/radius` | 12.5 | m，名义巡塔半径 |
| `mission/sector_angle_half_width_deg` | 12.0 | deg，扇区角半宽 |
| `mission/sector_radius_half_width` | 4.0 | m，扇区半径半宽 |
| `candidate/angle_offsets_deg` | `[0,-5,+5,-10,+10,-12,+12]` | deg |
| `candidate/radius_offsets_m` | `[0,+2,+4]` | m |
| `candidate/height_offsets_m` | `[0]` | m，低空固定高度 |
| 巡塔高度 | 3.0 | m |

因此每个低空普通扇区生成 `7 × 3 × 1 = 21` 个候选，允许：

- 在同一扇区内改变角度；
- 将半径从 `12.5 m` 外移到 `14.5 m` 或 `16.5 m`；
- 不允许改变 3 m 巡塔高度。

高空配置 `b882b98:.../config/sector_inspection.yaml:126-145` 使用相同角度、半径偏移，并增加高度偏移 `[0,-1,-2,-3] m`，即允许临时下降最多 3 m。

候选笛卡尔积生成位置：

`b882b98:AstraDrone_ros1_ws/src/MissionControl/astra_tower_mission/src/sector_inspection_mission_node.cpp:533-543`

### 3.2 候选筛选与评分

核心代码：

- `AstraDrone_ros1_ws/src/MissionControl/astra_tower_mission/src/inspection_candidate_planner.cpp:166-399`
- 原稳定版本：`b882b98:.../inspection_candidate_planner.cpp:166-389`

处理顺序为：

1. 检查地图时效、数值和扇区角度/半径/高度边界；
2. 检查塔体 keep-out；
3. 检查已知粗几何端点；
4. 检查 live occupied-map 端点；
5. 计算直线走廊风险；
6. 通过硬条件的候选进入评分；
7. 精确名义点只要通过硬条件就始终优先；只有名义点被拒绝时才选择最高分替代点。

原评分公式为：

```text
score = 0.2 × clearance
      - 6.0 × radius_error
      - 4.0 × nominal_radius × angle_error_rad
      - 14.0 × height_error
      - 0.1 × current_distance
      - 0.2 × continuity_error
      - 1.0 × unknown_ratio
      - 6.0（直线走廊被阻挡时）
```

同分时依次偏好：观测偏差更小、高度偏差更小、航程更短、candidate ID 更小。已经锁定的候选在新最佳得分只高出不超过 `mission/target_replacement_margin=2.0` 时保持不变。

原测试 `b882b98:.../test/inspection_candidate_planner_test.cpp:55-110` 验证了名义点占据后在同扇区换角/换半径且低空高度保持不变；`1390-1448` 验证了高空模式临时下降后在下一清晰扇区恢复名义高度。

### 3.3 EGO 无法到达、HOLD 和放弃条件

原状态机位置：

- 候选重新评估：`b882b98:.../sector_inspection_mission_node.cpp:1986-2095`
- 规划失败检测：`3489-3504`
- HOLD 后重试/重定位：`3685-3724`

| 参数 | 原低空值 | 单位/行为 |
|---|---:|---|
| `map_timeout` | 0.7 | s |
| `planning_timeout` | 10.0 | s |
| `goal_timeout` | 120.0 | s |
| `consecutive_plan_failure_limit` | 3 | 次 |
| `planner_unreachable_attempt_limit` | 2 | 次，同候选有限重试 |
| `failure_hold_duration` | 2.0 | s |
| `max_recovery_attempts` | 0 | 低空不执行高空 R1/R2 recovery |

具体规则：

- EGO 规划失败、持续无进展或目标超时后先进入 HOLD；
- 同一候选最多尝试 2 次；
- 达到上限后将该 candidate ID 标记为 `PLANNER_UNREACHABLE`，清除锁定并重新评估同扇区剩余候选；
- `GOAL_IN_OCCUPANCY` 在安全 HOLD 后直接进入重定位；
- 同扇区所有候选被拒绝或地图持续陈旧时进入 HOLD；
- 低空 `max_recovery_attempts=0`，候选耗尽后不会把扇区当作已完成，而是转返航/降落。

需要特别说明：原版本的“第一巡塔锚点”是例外。`b882b98:.../sector_inspection_mission_node.cpp:1770-1917` 只检查各标准扇区的精确名义点；第一锚点不可达时沿绕塔方向寻找下一个标准扇区，并没有评估该扇区的全部替代候选。完整的同扇区自适应规则明确用于普通巡塔扇区。

## 4. 原单机净空规则

### 4.1 净空对照表

| 检查层次 | 原稳定低空规则 | 硬/软约束 |
|---|---|---|
| 已膨胀 occupied-map 航点端点 | `map_additional_clearance=0.5 m` | 硬拒绝 |
| 原始/未膨胀点云端点 | `minimum_clearance=1.0 m`，并计入 `cloud_inflation=0.4 m` | 硬拒绝 |
| 已知粗几何端点 | 障碍先膨胀 `0.4 m`，再要求 `1.0 m`，约等价于距原粗几何 `1.4 m` | 低空硬拒绝 |
| 已膨胀地图直线走廊 | `0.5 m`，每 `0.4 m` 采样 | 原版本为软风险/扣分 |
| 已知粗几何直线走廊 | `1.0+0.4=1.4 m`，每 `0.4 m` 采样 | 原版本为软风险/扣分 |
| 塔体径向约束 | `6.41+2.0=8.41 m` 最小塔心半径 | 硬拒绝 |
| EGO occupied map 膨胀 | `obstacles_inflation=0.4 m`，地图分辨率 `0.2 m` | 硬占据体 |
| EGO `optimization/dist0` | `0.8 m` | 优化代价目标，不是硬保证 |

原 live-map 分流代码位于：

`b882b98:.../inspection_candidate_planner.cpp:233-256`

```cpp
const double map_required_clearance =
    config.map_points_are_inflated
        ? config.map_additional_clearance
        : config.minimum_clearance;
```

当 `map_points_are_inflated=true` 时，原代码明确使用 `0.5 m`，不会再对已膨胀地图套用 `minimum_clearance=1.0 m`。

### 4.2 EGO 自身障碍参数

关键位置：

- `AstraDrone_ros1_ws/src/Planner/ego-planner/planner/plan_env/src/grid_map.cpp:606-643,781-832`
- `AstraDrone_ros1_ws/src/Planner/ego-planner/planner/bspline_opt/src/bspline_optimizer.cpp:439,864-900,1495-1503`

EGO 的膨胀格数为：

```text
ceil(obstacles_inflation / resolution)
```

原单机低空为 `ceil(0.4/0.2)=2` 格，即水平约 `0.4 m` 膨胀。EGO 最终硬碰撞判断是轨迹不得进入膨胀 occupied voxel；`dist0=0.8 m` 进入碰撞代价函数，是软优化目标。

因此不能把 `0.4+0.8=1.2 m` 描述为硬净空。

### 4.3 无人机与障碍物最终实际最小距离

仅靠参数不能得到一个精确的最终物理最小距离：

- 任务层航点/直线诊断的近似目标为 `0.4+0.5≈0.9 m`，但受体素中心、离散分辨率和点云几何影响；
- EGO 轨迹层真正的硬条件只是“不进入膨胀 occupied voxel”，原低空水平包络约为 `0.4 m`；
- `dist0=0.8 m` 是软代价，优化结果可以小于该值；
- 实际飞行还包含轨迹离散检查和控制跟踪误差。

最终实际净空必须使用执行轨迹、原始障碍几何和跟踪误差证据测量，不能只从 YAML 宣称。

## 5. 当前第五阶段 `occupied-map clearance >= 1.0 m` 的来源

当前配置仍同时包含：

```yaml
candidate:
  minimum_clearance: 1.0
  map_points_are_inflated: true
  map_additional_clearance: 0.5
```

数值 `minimum_clearance: 1.0` 在三机接入前的 `488ddfc` 已存在，单机平行版本也存在；但它原本主要用于未膨胀点云或已知粗几何。原 live map 因 `map_points_are_inflated=true` 使用 `0.5 m`。

当前未提交动态 ENTRY 在：

`AstraDrone_ros1_ws/src/MissionControl/astra_tower_mission/src/sector_inspection_mission_node.cpp:1677-1742`

直接执行：

```cpp
const double hard_clearance = filter_config_.minimum_clearance;
mappedEndpointClear(..., hard_clearance);
```

这个 `1.0 m` 被用于：

- PRE_ENTRY 端点；
- ENTRY_GATE 端点；
- 第一巡塔点 `ORBIT_STAGING`；
- 悬停点到 PRE、ENTRY、第一巡塔点的整条折线；
- 任务侧固定高度 A* 的 `additional_clearance`。

管理器又在：

- `AstraDrone_ros1_ws/src/Swarm/astra_swarm_manager/src/astra_swarm_manager/policy.py:149-251`
- `AstraDrone_ros1_ws/src/Swarm/astra_swarm_manager/scripts/swarm_manager_node.py:456-459`

以 `hard_clearance=1.0` 对联合候选再次硬拒绝。

这些动态 ENTRY 代码的 `git blame` 为 `Not Committed Yet`。因此结论是：

> `1.0 m` 数值是旧参数，但“距已膨胀 occupied map 再留 1.0 m”的硬门限，是当前动态 ENTRY 改造新增加的规则语义。

## 6. 原规则与当前规则对比

| 项目 | 原稳定单机普通扇区 | 当前动态 ENTRY 第一巡塔点 |
|---|---|---|
| 角度搜索 | `0,±5,±10,±12°` | 名义角 `±7.5°`，步长 `2.5°` |
| 半径搜索 | `12.5/14.5/16.5 m` | 固定 `12.5 m` |
| 高度搜索 | 低空固定；高空可下降 1～3 m | 固定 `3.0 m` |
| 已膨胀地图端点门限 | `0.5 m` | `1.0 m` |
| live-map 直线走廊 | `0.5 m`，软风险，EGO 可绕行 | 全路径 `1.0 m` 硬拒绝 |
| 选择逻辑 | 名义点失败后对同扇区候选加权评分 | 先通过全部硬门限，只有幸存者才评分 |
| EGO 不可达 | 同点重试 2 次，再换同扇区候选 | UAV1 候选发布前归零，EGO 没有尝试机会 |
| 无候选 | HOLD，低空最终返航/降落 | `WAIT_ENTRY_PERMISSION` HOLD，禁止固定入口回退 |

当前普通巡塔扇区仍保留自适应逻辑。当前 launch 还在：

`AstraDrone_ros1_ws/src/Swarm/astra_swarm_bringup/launch/uav_tower_stack.launch:202-207`

将普通候选半径扩展为：

```yaml
[0.0, -2.0, -4.0, 2.0, 4.0]
```

问题不是全局删除了扇区重定位，而是动态 ENTRY/第一巡塔点没有走这条普通候选路径。

## 7. UAV1 为何在 285°～300°直接判定无解

当前动态生成器的搜索组合为：

| 项目 | 当前数值 |
|---|---:|
| 角度 | `292.5°±7.5°` |
| 角度步长 | `2.5°` |
| PRE_ENTRY 半径 | `18.0～19.5 m`，步长 `0.25 m` |
| ENTRY_GATE 半径 | `15.0～16.0 m`，步长 `0.25 m` |
| 第一巡塔点半径 | 固定 `12.5 m` |
| 高度 | 固定 `3.0 m` |
| occupied-map 硬净空 | `1.0 m` |

代码位置：

- 候选生成：`sector_inspection_mission_node.cpp:1790-1928`
- 第一巡塔点固定为 `route_.radius`：`1853-1854`
- 安全预筛：`1677-1742`
- 无候选进入 HOLD：`4359-4365`

虽然总组合数为 `7 × 7 × 5 = 245`，但同一角度的 35 个 PRE/ENTRY 组合共享同一个固定 `12.5 m` 第一巡塔点。当前工作区报告记录七个第一巡塔点距最近已膨胀占据点分别为：

| 角度 | 净空 |
|---:|---:|
| 285.0° | 0.945260 m |
| 287.5° | 0.431 m |
| 290.0° | 0.133 m |
| 292.5° | 0.113 m |
| 295.0° | 0.106 m |
| 297.5° | 0.095 m |
| 300.0° | 0.270 m |

证据位置：`ego_swarm_three_uav_integration_report.md:79-94`。

七个角度全部在评分前被 `1.0 m` 硬门限拒绝，所以：

- 改变 PRE_ENTRY/ENTRY_GATE 半径无法改变第一巡塔点结果；
- 当前评分公式无法选择 `0.945260 m` 的最佳点，因为评分只对硬门限幸存者执行；
- 普通扇区已有的 `±2/±4 m` 半径重定位没有被调用；
- EGO 没有收到目标，无法证明该点或邻近半径是否可绕行到达。

因此，对“是否把候选点评分/局部重定位错误改成固定硬门限全部拒绝”的审计判断为：

> 对当前第一巡塔点路径基本属实。它并非完全固定角度，但已经从原来的角度、半径多维候选，缩减为固定半径、固定高度的七个角度点，并在评分和 EGO 验证前统一执行新的 1.0 m occupied-map 硬拒绝。

## 8. 建议

建议恢复或复用原普通扇区的同扇区自适应候选逻辑到第一巡塔点，同时保留第五阶段新增的三机角色顺序、角间距、路径不交叉和机间净空检查。

建议原则：

1. 第一巡塔点至少恢复同扇区半径候选，不应只改变 PRE_ENTRY 和 ENTRY_GATE 半径；
2. 低空高度继续固定 `3.0 m`，除非另行批准改变高度策略；
3. 对已膨胀地图恢复 `map_additional_clearance=0.5 m` 的原语义；若确需 `1.0 m`，应定义为独立 Stage 5 参数并重新完成净空和可达性验收；
4. 候选通过正确端点规则后再进行评分并交给 EGO 验证；
5. 不建议仅通过降低一个字面门限解决问题，应同时恢复第一巡塔点的半径重定位能力并保留三机联合安全约束。

最终建议：**恢复原扇区内自适应航点思想，并将其与三机动态 ENTRY 的联合安全约束合并；不建议继续使用当前固定 `12.5 m / 3.0 m` 第一巡塔点加 occupied-map `1.0 m` 全路径硬拒绝的单一路径。**
