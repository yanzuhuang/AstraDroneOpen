# AstraDroneOpen 项目整理文档

本文件是今后整个 AstraDroneOpen **项目整理的唯一主文档**。后续代码、launch、配置、文档及其他项目整理，继续在本文追加为模块二、模块三……；同一模块的小幅补充更新该模块，不再为每个小整理生成新的独立审计文档。后续模块统一记录目标、依据、实际变更、依赖与保护边界、验证结果及待办。

本文汇总整理决策，不替代当前源码、`rule.md` 的设计语义和 `AGENTS.md` 的协作约束；操作说明已统一纳入模块一。上述历史整理报告已合并后删除；来源文件名与吸收内容保留在模块一第 11 节。旧命令、旧数量和阶段性 KEEP／删除候选仅用于解释演进，不作为当前操作指令或自动删除许可。

本次整理日期：2026-09-16。核对分支：`scene01-3uav-circuit-mission`；HEAD：`2c30cf3360f3d5c026f41da4c29257139ff9abb3`。工作区已有修改、删除及未跟踪文件，本文以当前工作区源码和最新报告为准，不能仅凭 HEAD 还原当前目录状态。首次汇总仅新增本文；本次收尾按指定集合物理删除 13 份已吸收的历史整理报告，更新文档引用；随后按追加授权，将三份运行／可视化说明合并后删除，累计收敛 16 份文档。不移动、不归档，不修改代码，不启动仿真或训练。

## 模块一：启动脚本与 run_sh 整理

### 1. 整理目标

将分散的启动入口清理历史压缩成一条可追溯的演进记录，明确哪些 shell 已退役、哪些能力仍保留、当前入口如何使用，以及下一轮整理不能跨越的边界。整理范围是用户入口与相关文档，不因删除 shell 而推导其底层 launch、节点、配置、测试或历史工件可以删除。

**当前最终状态：`scripts/run_sh/` 仅含 README 和三个子目录，共 11 个 shell，分布为 three_uav 4、reinforcement_learning 5、tools 2。旧平铺入口不存在；三机默认录制为 none，正式巡塔命令显式选择 light。**

### 2. 整理过程摘要

以下按报告日期及前后依赖排序。同一天的工作按阶段顺序描述，不臆造执行时刻。

| 时间／阶段 | 主要判断或实际动作 | 当阶段结束状态及与当前的关系 |
|---|---|---|
| 2026-09-08，启动入口收尾审计 | 盘点 27 个 run_sh shell，追踪 shell、launch、节点和外部依赖；确认普通／多高度、双机／三机存在直接复用 | 当时无可直接删除对象；统一 dispatcher、公共库等只是建议，没有实现 |
| 2026-09-11，用户导航与替代验证 | 建立入口分类、运行手册，核对帮助、默认值、控制、录制、输出及停止合同 | `SAFE_TO_DELETE=0`、`ARCHIVE_ONLY=6`、`KEEP=5`；这是“是否被 OFFICIAL 完整替代”的当时结论 |
| 2026-09-15，替代复核 | 相机查看器已加入，28 个 shell；复核未发现旧能力已被完整替代 | 相机查看器不替代 MAVROS 四路监视、旧录制 profile 或 Offboard/QGC 组合 |
| 2026-09-15，第四阶段历史实验退役 | 六个编排外壳退出活动推荐；固化协议、源码原文、Git 定位及指纹，检查集合外执行依赖 | 六个达到 READY_TO_DELETE，但当轮未删除；采用“需求退役并保留协议”判据，不声称 OFFICIAL 新增了旧实验能力 |
| 2026-09-15，第五阶段物理清理 | 按指定集合删除六个历史编排 shell，保留执行器、分析器与运行工件 | 28 → 22 个 shell；旧实验的顺序、ID、重试、终态等仍可追溯 |
| 2026-09-15，第六阶段剩余旧入口收敛 | echo 为 MIGRATE_THEN_DELETE；record、pc_example 为 KEEP_AS_IS | 22 个不变；当时只设计四路监视迁移，不能将该阶段 KEEP 当作永久结论 |
| 2026-09-16，第七阶段 echo 迁移 | 新增 mavros_monitor，迁移四路消息回显、namespace 和独立 tmux 生命周期；更新活动导航 | 22 → 23；echo 成为 READY_TO_DELETE，仍未删除；真实 ROS 显示未由隔离检查证明 |
| 2026-09-16，第八阶段 echo 删除 | 复核指纹后只删除 echo | 23 → 22；监视能力由新入口承接，未扩大删除集合 |
| 2026-09-16，单机 shell 审计与正式退役 | 在明确取消七个单机独立入口需求后审计、删除；同步撤下活动导航并移除 pc_example alias | 22 → 15；三机仍用的 sector mission、occupancy adapter、bridge 等底层组件保留 |
| 2026-09-16，三目录迁移 | 15 个 shell 按用途物理迁入三个目录，修正 repo_root、shell 调用及两处 Python 路径消费者 | 数量仍为 15，分布 6／7／2；无旧路径 wrapper、软链接或兼容 alias。这是中间布局 |
| 2026-09-16，四入口精简与默认录制检查 | 再退役两个三机专项 bag shell、fixed_speed_baseline 和 Worksite SAC shell；三机默认录制改为 none | 15 → **11**，最终分布 **4／5／2**；显式 light/full 的原有录制清单与流程保留 |
| 2026-09-16，light/full 点云审计 | 确认 light 100 个唯一 topic、full 122 个；full 独立于已删专项 recorder | light 无点云；full 增加 9 路 PointCloud2，但不是完整地图或全部专项诊断的替代 |

数量演进为 `27 → 28 → 22 → 23 → 22 → 15 → 15 → 11`。分类名称也有历史口径差异：第六阶段按用途把 SAC 视为研究接口，当前 README 的 OFFICIAL 则指维护中的统一用户接口。本文采用当前 README 分类，分类不等于飞行验收或训练放行。

### 3. 删除／退役内容

以下是已经发生的历史动作，本轮没有重新执行删除。累计退役 18 个 shell；15 个文件迁入子目录是路径迁移，不应再计作 15 次能力删除。

| 批次 | 已退役文件（保留历史名称） | 保留下来的边界 |
|---|---|---|
| 第五阶段，6 个旧编排外壳 | `fixed_speed_baseline_batch.sh`、`high_speed_exploration_batch.sh`、`high_speed_boundary_refinement.sh`、`learning_speed_manual_batch.sh`、`learning_speed_postfix_batch.sh`、`learning_speed_progressive_qualification.sh` | 当时将协议、原文与固定 Git 定位固化到归档文档；该文档现已合并后删除，必要结论与 Git 定位保留于本模块；底层执行器当阶段保留。fixed_speed_baseline 执行器后来另行退役，不能沿用当时保留清单 |
| 第八阶段，1 个旧监视入口 | `echo.sh` | 四路 MAVROS 监视迁入现有 `tools/mavros_monitor.sh`；退出生命周期经过明确调整，不是旧 tmux 行为逐字复制 |
| 单机正式退役，7 个 shell | `ego_planner_stack.sh`、`ego_waypoint_inspection.sh`、`sector_inspection.sh`、`fixed_orbit_inspection.sh`、`sector_inspection_record_bag.sh`、`sector_inspection_failure_injection.sh`、`pc_example.sh` | 取消独立 shell 需求；sector 主 shell 与专属 recorder 同批退役。保留底层节点、launch、配置、安全测试和 Offboard/QGC 程序 |
| 三目录迁移后的精简，4 个 shell | `three_uav/three_uav_self_filter_record_bag.sh`、`three_uav/uav2_occupancy_record_bag.sh`、`reinforcement_learning/fixed_speed_baseline.sh`、`reinforcement_learning/learning_speed_sac_training.sh` | 三机内置 full、Forest SAC、overlay、manual/calibration/high_speed、record/monitor 保留；Worksite 底层 launch、训练实现、配置及历史工件保留 |

单机正式退役时只移除了 `~/.bashrc` 中的 `pc_example` alias，保留 `astra`、`qgc`；本轮只读复核仍如此。其他已打开终端可能保留内存 alias，不能由配置文件不存在旧定义推断全部终端都已更新。

六份历史编排的固定源码版本为 `2c30cf3360f3d5c026f41da4c29257139ff9abb3`，六个历史路径均为 `scripts/run_sh/` 加本节表中的对应编排文件名；可用 `git show <固定版本>:<历史路径>` 只读查看源码。原协议文档已合并后删除，本文保留主要协议差异，逐字源码以该 Git 版本为准。复现须匹配当时执行器、配置和运行 manifest，不恢复到当前脏工作区直接运行。本模块保留以下历史限制：旧 manual batch 未固定 ceiling/max_acc/generation，后续执行器默认值已漂移；fixed baseline batch 并非正式失败即停；boundary refinement 末尾不要求 v150 必须 PASS；progressive 聚合失败可能先于 batch_state 写出。不得把这些历史实际行为改写成更理想的协议。

### 4. 当前 scripts/run_sh 目录结构

```text
scripts/run_sh/
├── README.md
├── three_uav/                              # 4 个
│   ├── three_uav_inspection.sh
│   ├── three_uav_multi_height_inspection.sh
│   ├── three_uav_outdoor_village.sh
│   └── three_uav_camera_view.sh
├── reinforcement_learning/                 # 5 个
│   ├── learning_speed_forest_sac.sh
│   ├── learning_speed_manual_run.sh
│   ├── learning_speed_calibration.sh
│   ├── high_speed_exploration.sh
│   └── prepare_hector_training_overlay.sh
└── tools/                                  # 2 个
    ├── record.sh
    └── mavros_monitor.sh
```

根目录没有 `.sh`，11 个 shell 均为普通文件，无软链接或旧平铺兼容包装器。早期提出的 `lib/`、统一 dispatcher、`record_evidence.sh`、`diagnostics.sh`、`ppe_yolo.sh` 等没有成为当前结构，不能作为现有入口使用。

### 5. 当前保留 shell 及用途

| 当前路径（相对 scripts/run_sh） | 当前分类 | 用途及限制 |
|---|---|---|
| [three_uav/three_uav_multi_height_inspection.sh](scripts/run_sh/three_uav/three_uav_multi_height_inspection.sh) | OFFICIAL | 多高度 profile；默认单层 26／20／14 m，显式 multi-layer 为两层；复用普通三机执行器；拒绝 Learning Speed |
| [three_uav/three_uav_inspection.sh](scripts/run_sh/three_uav/three_uav_inspection.sh) | OFFICIAL | 普通三机 3／3／3 m 单层 worksite；同时是多高度共享执行器；默认控制、GUI、RViz、Learning Speed 关闭，D435 开启，record=none |
| [three_uav/three_uav_outdoor_village.sh](scripts/run_sh/three_uav/three_uav_outdoor_village.sh) | DIAGNOSTIC | outdoor_village 三机初始化；启动仿真及节点，拒绝 `--control`；不是三机巡塔验收入口 |
| [three_uav/three_uav_camera_view.sh](scripts/run_sh/three_uav/three_uav_camera_view.sh) | DIAGNOSTIC | 附加三路 D435 彩色第一视角；ROS 或 Gazebo 图像窗口；不启动仿真、任务或控制 |
| [reinforcement_learning/learning_speed_forest_sac.sh](scripts/run_sh/reinforcement_learning/learning_speed_forest_sac.sh) | OFFICIAL | Forest `preflight/training/smoke/evaluation`；没有 demo 或 `--control` 模式；使用前仍须满足对应研究资格和运行授权 |
| [reinforcement_learning/learning_speed_manual_run.sh](scripts/run_sh/reinforcement_learning/learning_speed_manual_run.sh) | RESEARCH | A/B 单次标定及资格实验共享执行器；旧 batch 删除不取消其独立合同和源码预检依赖 |
| [reinforcement_learning/learning_speed_calibration.sh](scripts/run_sh/reinforcement_learning/learning_speed_calibration.sh) | DIAGNOSTIC | 向已运行系统附加 Observation C／标定采集，保留其数据合同 |
| [reinforcement_learning/high_speed_exploration.sh](scripts/run_sh/reinforcement_learning/high_speed_exploration.sh) | LEGACY | 旧高速档位、run-ID 配对及证据分析链；不作为日常三机任务或训练入口 |
| [reinforcement_learning/prepare_hector_training_overlay.sh](scripts/run_sh/reinforcement_learning/prepare_hector_training_overlay.sh) | INTERNAL | Forest SAC 所依赖的 Hector overlay 检查／准备工具；准备模式可能复制、移动及构建 |
| [tools/record.sh](scripts/run_sh/tools/record.sh) | LEGACY | 根 namespace 的旧单机录包 profile；预期输出 `~/bag`，没有被三机 light/full 等价替代 |
| [tools/mavros_monitor.sh](scripts/run_sh/tools/mavros_monitor.sh) | DIAGNOSTIC | 四路 MAVROS 消息回显；默认根 namespace，可选 `/uav1`、`/uav2`、`/uav3`；不发布、不启动系统、不录包 |

当前分类合计：OFFICIAL 3、RESEARCH 1、DIAGNOSTIC 4、LEGACY 2、INTERNAL 1，共 11。`OFFICIAL` 只表示维护的用户接口，不意味着所有运行模式均获准。Forest 不替代已退役 Worksite shell 的环境合同；本模块不改变既有 SAC NO-GO、qualification、Reward、Observation 或 reset 边界。

monitor 在已 source ROS、有 ROS master 的独立终端运行；它使用独立 socket 和唯一 tmux session。`Ctrl+b d` 会退出并清理本次四路监视，pane 内 `Ctrl+C` 只中断该路；这与旧 record 的 detach 后继续录制不同。monitor 无 `--stop`，不接管其他 tmux 会话。

<a id="runbook"></a>

### 6. 当前正式三机命令

以下从仓库根目录执行，是操作说明，本轮未运行。带 `--control` 会启动自动飞行；使用现有已构建环境，先结束上一轮仿真。仓库根目录为 `/home/yanzu/AstraDroneOpen`，必要时先执行 `cd /home/yanzu/AstraDroneOpen`。

三机多高度两层巡塔：

```bash
./scripts/run_sh/three_uav/three_uav_multi_height_inspection.sh --multi-layer --control --gui --rviz --record light
```

当前 profile 为 UAV1 26→22 m、UAV2 20→16 m、UAV3 14→10 m。改为 `--single-layer` 时分别只巡 26／20／14 m；不传层模式也为单层。若两个模式开关同时出现，以最后一个为准。多高度始终禁止 Learning Speed，不沿用更早的高度快照。

普通三机 3.0 m 同高度、单层巡塔：

```bash
./scripts/run_sh/three_uav/three_uav_inspection.sh --control --gui --rviz --record light
```

需要 full 所含的点云与模型状态时，将该命令的 `light` 改为 `full`。不写 `--record` 则为 none，不会自动录 bag。没有 `--control` 仍会启动仿真和节点，不能作为只读检查。

light/full 默认输出到 `runtime_artifacts/three_uav_inspection_control_<时间戳>/`；无控制为 `three_uav_inspection_dry_run_<时间戳>/`。目录包含相应 bag、summary、三机 CSV、swarm.csv、candidates、metadata、ROS／launch／rosbag 日志；有效 swarm 轨迹存在时尝试生成 `trajectory_xy.png` 和 `trajectory_3d.png`。自定义 `--results-dir` 须匹配本仓库 `runtime_artifacts/three_uav_inspection_*` 绝对路径，当前不具备训练目录那样的独占拒绝复用机制，应选新目录。

正常飞行先等待任务结束、落地、解除武装，再在启动终端 `Ctrl+C` 收尾。脚本停止 bag、launch 并等待，随后条件绘图；`Ctrl+C` 和 `--duration SEC` 均不是返航／降落或任务成功指令。三机入口没有 `--stop`；duration 是墙钟时限。

<a id="forest-sac"></a>

#### 6.1 Forest SAC 运行说明


用途：Forest 地图池上的 SAC 训练，以及独立切图检查、短程 smoke 和 evaluation。

**本节提供入口，不解除既有训练资格限制。尚未获准的正式训练／evaluation 不应因文档列出命令而启动；本轮没有产生新的训练放行证据。** Forest SAC 没有 `--control` 开关，非帮助命令可能准备／构建 Hector overlay 并启动仿真。

获准执行相应阶段后，选用以下模式之一：

```bash
./scripts/run_sh/reinforcement_learning/learning_speed_forest_sac.sh --mode preflight --gui
```

`preflight` 启动 Gazebo 并运行三地图 reset 生命周期，不执行 Episode、Replay 或 learner；它不是只读环境检查。

```bash
./scripts/run_sh/reinforcement_learning/learning_speed_forest_sac.sh --mode smoke --gui
```

`smoke` 实际执行 31 个训练 Episodes，每 10 个 completed Episodes 换图，共三次切换。

```bash
./scripts/run_sh/reinforcement_learning/learning_speed_forest_sac.sh --mode training --gui
```

`training` 目标为 10000 个 completed Episodes。默认自动生成 RUN_ID；可用 `--run-id UNIQUE_RUN_ID` 指定新的简单名称，已有输出目录会被拒绝。

独立 evaluation 使用明确的 checkpoint；下方占位符必须替换为实际文件和对应 Episode 编号：

```bash
./scripts/run_sh/reinforcement_learning/learning_speed_forest_sac.sh --mode evaluation --gui \
  --seed 8 --checkpoint /absolute/path/to/checkpoint --checkpoint-episode N
```

evaluation 固定 100 Episodes，seed 可选 8 或 9。当前入口只接受上述四种 mode，没有 `demo` 模式。

停止：启动终端 `Ctrl+C`，等待收尾；人工中断不计作正常完成。

| 模式 | 结果目录 | 主要结果文件 |
|---|---|---|
| training | `runtime_artifacts/rl_training/<RUN_ID>/` | `sac_runtime_summary.json`、checkpoint、`logs/console.log`、`logs/ros/` |
| evaluation | `runtime_artifacts/rl_evaluation/<RUN_ID>/` | `sac_runtime_summary.json`、`logs/console.log`、`logs/ros/` |
| preflight | `runtime_artifacts/learning_speed/forest_randomization_training_integration_v2/<RUN_ID>/` | `qualification_summary.json`、`logs/console.log` |
| smoke | 同上，独立 RUN_ID | `sac_runtime_summary.json`、`logs/console.log` |

Worksite SAC shell 已退役；底层 launch、Python/C++ 节点、配置与历史训练工件保留，既有训练资格限制不变。

各入口完整选项使用原脚本 `--help` 查看；分类见本模块第 5 节。历史实验入口不作为日常操作方式。

### 7. record none／light／full 的当前语义

三机录制由 `three_uav_inspection.sh` 内置 topic 数组与 `rosbag record --lz4` 实现；多高度直接复用。它不调用 `tools/record.sh`，也不调用已删除的专项 bag shell。

| 模式 | 当前行为 | 证据范围 |
|---|---|---|
| none（默认） | 不启动 bag；任务 report 指向 `/dev/null`，关闭脚本管理的结构化证据；临时 ROS 日志在 `/tmp/astra_three_uav_none.*`，收尾清理；不能同时显式指定 `--results-dir` | 不保留脚本管理的任务证据，不能用于要求这些工件的验收；不意味着 PX4、FAST-LIO 等所有下游组件绝对零文件写入 |
| light（须显式选择） | lz4 bag，100 个唯一 topic；保存状态、定位、规划／轨迹、MAVROS、协调／安全和有关诊断，以及任务证据 | **没有点云**，没有 Gazebo model/link states；低维 Observation 和 Odometry 都不是点云 |
| full（须显式选择） | 同一 bag 文件名 `three_uav_inspection.bag`；122 个唯一 topic，是 light 的严格超集，多 22 个 | 多 9 路 PointCloud2，但不等于全部传感器数据、完整地图或全部专项过滤诊断 |

full 的全部新增项为 `/gazebo/model_states`，加每机以下 7 项（`N=1,2,3`）：

| 每机新增 topic | 数据含义 |
|---|---|
| `/uavN/mavros/local_position/pose` | 原 MAVROS 位姿 |
| `/uavN/fast_lio/Odometry_raw` | frame adapter 前的 FAST-LIO 里程计 |
| `/uavN/fast_lio/cloud_registered_raw` | FAST-LIO 已注册的当前扫描；raw 指适配前，不是原始雷达数据 |
| `/uavN/cloud_registered` | 上一路经 frame adapter 改为 `uavN/camera_init`；点坐标不变 |
| `/uavN/stage3/occupancy_inflate` | EGO 局部膨胀占据体素经时间戳适配后的点云，可能含虚拟限高 |
| `/uavN/tower_mission/candidate_targets` | 正式任务候选 |
| `/uavN/tower_mission/entry_corridor_candidates` | 进场候选 |

每机中间三路为 PointCloud2，共 9 路。两路 registered cloud 是同一流水线的前后数据，不能重复相加当作额外覆盖；occupancy 是规划几何，不是环境表面地图。

light/full 均未包含 Mid360 原始 lidar/IMU、D435 图像／深度、`/tf`、`/tf_static`、peer/self/ground 过滤云、stage5 过滤前后旁路或 RViz 的 `grid_map/inflated_cloud`。因此 full 不能直接恢复当前 RViz 全部显示，不能独立重跑原始 LIO，也不自动输出全局 PCD/PLY。累计注册扫描须依据当次运行恢复坐标变换并核查覆盖、丢帧和漂移；实际录包质量待运行证据验证。录制范围与坐标限制已吸收在本节；精确 topic 清单以当前 `three_uav_inspection.sh` 数组为准，原点云审计报告已合并后删除。

**旧 `tools/record.sh` 继续单独保留。** 它含 17 个 topic 参数、16 个唯一 topic（raw setpoint 重复一次），使用根 namespace，覆盖旧相机压缩图像、body 注册云、livox、MAVROS 电池／IMU 等。三机 light/full 的完整 topic 字符串与这 16 项无交集，不能去掉 namespace 假装等价。

record 通过 tmux 向 pane 发送 `cd ~/bag` 和 rosbag 命令；不创建或强制校验该目录，cd 失败可能落到 pane 原目录。使用 rosbag 默认时间戳命名，没有 bag 压缩参数；图像 topic 名中的 compressed 不代表 bag 压缩。它无 `--help`／`--stop`／统一 trap；不要执行 `--help` 来探测。detach 不停止录包，再次启动会结束同名 record 会话，也不能当作优雅停止保证。保留该旧合同不代表推荐它承担正式三机验收。

<a id="camera-view"></a>

### 8. camera_view 的用途

camera_view 是运行中三架机的独立第一视角查看器。先按第 6 节启动三机仿真，保持 D435 启用，不使用 `--disable-d435`，在另一个桌面终端选择一种方式：

```bash
# 三个 Gazebo 原生图像窗口
./scripts/run_sh/three_uav/three_uav_camera_view.sh --gazebo

# 或三个 ROS image_view 彩色窗口
./scripts/run_sh/three_uav/three_uav_camera_view.sh
```

默认直接调用 `/opt/ros/noetic/lib/image_view/image_view`，订阅 `/uav1/d435/color/image_raw` 至 `/uav3/d435/color/image_raw`。`--gazebo` 从实际话题列表发现各机 `UAVN_Camera_color/image` 后缀，再使用 `gz topic -v`；要求每机恰有一条匹配，不写死 world/model 前缀。缺失时报告具体 UAV 并退出，不会启动或重启 Gazebo。

正式三机 shell 不自动调用它。`--gui` 控制 Gazebo GUI，`--rviz` 控制 RViz，camera_view 则是额外图像窗口；它不提供四路 MAVROS 文本诊断，不控制任务，不默认录制视频。`Ctrl+C` 只关闭自己创建的查看器。日志和 ROS image_view 手动保存图片写入独立 `runtime_artifacts/three_uav_camera_view_<时间戳>_<随机后缀>/`。

#### 8.1 在 Gazebo 菜单中打开


点击顶部 **Window → Topic Visualization**，或在 Gazebo 主窗口按 **Ctrl+T**。
在 `gazebo.msgs.ImageStamped` 类型下选择下面的话题，再点 `Okay`；每架机重复一次。
这是 Gazebo 自带的 Image View 窗口，可拖动位置，顶部 Topic 下拉框可以切换相机。

| 无人机 | 历史现场的 Gazebo 原生彩色话题示例 |
|---|---|
| UAV1 | `/gazebo/default/iris_mid360_00/d435_link/UAV1_Camera_color/image` |
| UAV2 | `/gazebo/default/iris_mid360_11/d435_link/UAV2_Camera_color/image` |
| UAV3 | `/gazebo/default/iris_mid360_22/d435_link/UAV3_Camera_color/image` |

相机画面选择 `color/image`，不是 IMU、depth 或以 `cmd` 结尾的话题。
操作依据：[Gazebo 官方相机教程](https://classic.gazebosim.org/tutorials?cat=sensors&tut=sensor_noise)。

相机整理当时撤销了临时增加的 RViz 第一视角面板，恢复到该任务开始时的配置；这不撤销另行完成的 EGO 膨胀显示修复，也不是本轮配置操作。

#### 8.2 Gazebo 列表为空时

在仿真运行期间检查：

```bash
gz topic -l | rg 'UAV[123]_Camera_color/image$'
source /opt/ros/noetic/setup.bash
rostopic list | rg '^/uav[123]/d435/color/image_raw$'
```

Gazebo 的相机图像话题与 ROS `/uavN/d435/...` 话题属于不同传输系统。
Gazebo 11 原生 CameraSensor 按父模型、link 和 sensor 名组成 `/image` 路径，
因此脚本从实际列表发现完整名称，不写死 world/model 前缀。
依据：[Gazebo 11 CameraSensor 源码](https://github.com/gazebosim/gazebo/blob/gazebo11/gazebo/sensors/CameraSensor.cc)。

若缺少原生话题，检查当前进程是否为三机入口、是否禁用 D435，以及终端的
`GAZEBO_MASTER_URI` 是否匹配仿真。若原生图像可用而 ROS 图像不可用，检查
RealSense ROS 插件和 `ROS_MASTER_URI`。若话题存在但画面不刷新，检查仿真暂停、
相机停用服务以及渲染错误；仅有话题或插件加载日志不足以证明有有效图像。

#### 8.3 历史现场记录与验证边界（2026-09-11）

- 基线 HEAD：`2c30cf3360f3d5c026f41da4c29257139ff9abb3`。
- 三机 launch 默认启用 D435，RGB sensor 已有 `visualize=true`。
- 最近启动 `runtime_artifacts/three_uav_inspection_control_20260911_172007/`
  的 metadata 为 `d435_enabled=true`，roslaunch 日志包含三个模型的 RealSense 插件加载记录。
- 当时仅保留独立图像查看器及 Gazebo 菜单说明，未修改相机模型、插件或飞行链。
- 脚本语法、帮助及 Gazebo `-v` 命令帮助检查通过。
- 后续用户反馈时，现有仿真 `three_uav_inspection_control_20260911_172708` 正在运行。
  现场查询确认三路原生彩色话题均存在，已打开三个 Gazebo Image View 窗口并检查出图。
  UAV1 查看器首次 `load error` 保留于 `three_uav_camera_view_20260911_173136_iRRAk9/uav1.log`；
  单独重新打开该查看器后正常出图，没有重启仿真或传感器。
- 三路画面截图：`runtime_artifacts/gazebo_camera_ui_check_20260911/view_0.png`（UAV1）、
  `image_a.png`（UAV2）、`image_b.png`（UAV3）。这证明现场可出图，不代表长期帧率验收。

以上是原相机说明保留的现场记录，本轮未复验图像或工件，不将一次出图扩大为长期帧率或任务联合验收。


### 9. 已确认的依赖关系

```text
three_uav/three_uav_multi_height_inspection.sh
  -> 同目录 three_uav_inspection.sh
     -> triple_tower_multi_height_inspection.launch
        -> triple_tower_inspection.launch
           -> triple_px4_mavros.launch
           -> dual_tower_inspection.launch（start_sim=false，复用 UAV1/2）
           -> UAV3 栈
           -> 每机 FAST-LIO、frame adapter、过滤、uav_tower_stack
              -> sector_inspection.yaml / sector_inspection_mission_node
              -> occupancy_stamp_adapter_node
              -> ego_gazebo_bridge.launch / EGO / traj_server / bridge
           -> swarm manager / safety / evidence recorder / RViz
     -> 内置 rosbag 录制；收尾调用 plot_three_uav_trajectory.py

reinforcement_learning/learning_speed_forest_sac.sh
  -> prepare_hector_training_overlay.sh
  -> hector_forest_sac_training.launch
     -> map manager / SAC runner / reset / Observation C / Hector backend
```

普通三机直接选择 `triple_tower_inspection.launch`；outdoor 初始化使用专用初始化 launch，继续复用三机底座并关闭控制与任务。不得因文件名含 dual、sector 或单机历史来源而删除底层实现。

| 依赖／消费者 | 当前关系及保护含义 |
|---|---|
| 多高度 → 普通三机 shell | sibling `exec`，共享执行器仍有独立用户用途，不能作为重复 wrapper 删除 |
| 三机 → dual launch | 复用 UAV1/2 与公共组件，关闭重复仿真；不是冗余双机文件 |
| `high_speed_parameter_preflight.py` | 读取 `scripts/run_sh/reinforcement_learning/learning_speed_manual_run.sh`；三目录迁移已修正路径 |
| `test_hector_overlay_preflight.py` | 通过路径组件定位 reinforcement_learning 下 overlay preparer；迁移已修正路径，测试依赖保留 |
| manual/high_speed/calibration | 各自直接调用现有包内 launch、recorder 和 summarize；不因旧 batch 或 fixed baseline shell 退役而删除 Python 分析实现 |
| 正式三机录制 | 内置 rosbag 与 launch 内的结构化证据 recorder；不依赖已退役专项 recorder，但也不覆盖其全部 topic |
| 模型与环境 | Xacro/URDF/SDF、YAML、RViz、插件、外部 PX4、Hector 源码／overlay 均可能处于实际启动链 |
| `runtime_artifacts/sac_python_packages/` | 现有训练依赖路径，不能按旧实验目录清理 |

当前可见保留 shell 未发现已退役入口的悬空调用；README 的 shell 导航与实际 11 个文件集合一致。本模块来源表中的旧路径仅用于标识已删除的来源文件；任意外部私有脚本、二进制内部调用和动态拼接路径不在本轮穷尽保证内。

### 10. 后续项目整理规则与保护边界

1. **先读当前状态再决定。** 每轮检查 branch、HEAD、Git status/diff；区分已有修改与本轮增量。当前源码决定行为，当前工件决定验证程度，本模块的历史演进与可核查 Git／工件记录说明当时发生了什么。旧 KEEP、READY_TO_DELETE、SAFE_TO_DELETE=0 或空候选集合均只在对应阶段和范围内成立。
2. **精确限定删除与迁移集合。** 保留需求须先证明替代的参数、默认控制、namespace、输出、停止、错误和录制合同；需求已明确取消时，可以按退役标准处理，但须留协议、查依赖并同步活动导航。不能为了产生删除结果强行合并功能，也不能把一个 shell 的退役扩成底层代码删除。
3. **保护用户资产。** 不恢复、覆盖、暂存、提交或清理既有用户修改；尤其保留 `FAST_LIO/Log/mat_pre.txt`。用必要的文件摘要、权限、链接目标对比约束实际变更。`runtime_artifacts/` 不随入口清理删除、迁移或回写。已吸收的 13 份旧整理报告及追加授权的 3 份运行／可视化说明按指定集合删除，不扩展到其他文档或 Git 历史。
4. **保护当前主链与设计语义。** 保留三机共享 shell、multi/triple/dual launch、sector mission/config、occupancy adapter、bridge、FAST-LIO、PX4/MAVROS、EGO、协调、安全及其测试。按 `rule.md` 维护八扇区、候选、净空、ENTRY/EXIT、返航合同；不借整理改变安全阈值、规划、控制或多机职责。
5. **路径迁移要查完整调用链。** 同时核对 source/exec、launch/XML、Python、CMake/test、安装规则、收尾绘图／分析、活动文档和已知 alias；正确处理 BASH_SOURCE 推导层数、空格路径及非仓库 cwd。不能以名字相近或文本零命中作为可删除证明，不为清空字符串改写历史。
6. **只读检查不得隐式启动运行。** 先审读帮助分支；record 等无帮助合同的脚本不可试跑。Forest preflight 会启动 Gazebo/reset，smoke 会训练；launch 参数展开可能执行 `<param command>`。未经对应授权不启动 Gazebo、控制、训练或 evaluation，不把 dry-run 理解为零副作用。
7. **证据等级分开。** 文档／语法／隔离参数／离线检查通过，不等于真实 ROS、录包落盘、停止 flush、RViz、飞行、qualification 或训练通过。保留真实失败、中断与基础设施错误，不能靠重试、调参、跳 seed 或降低安全门制造 PASS。
8. **研究和依赖边界不变。** 入口清理不放行 SAC；training-only Hector/truth 与 FAST-LIO＋PX4/MAVROS full-stack 继续互斥。不改 SAC、Reward、Observation C、数据身份和 reset 合同，不恢复旧 pilot。未经批准不升级或清理外部 PX4、Hector、ROS/Gazebo、EGO、FAST-LIO 及第三方依赖，不手改 build/devel。
9. **保持文档单一主线。** 后续新模块追加本文；本模块后续小修改更新当前结论并保留简短演进。README、运行手册与协作入口按实际需要同步导航，已吸收的历史报告名称和关键结论留在本文，不再为小整理新增独立审计报告。本模块不产生新的自动删除集合。

### 11. 汇总依据与本轮核对结果

首批 13 份历史整理报告已合并后删除；追加的 3 份运行／可视化说明也已合并后删除，共 16 份，未移动或另行归档。下表保留来源文件名及吸收内容说明，不提供指向已删除文件的链接；重复 Git status、长哈希表、topic 全量展开或测试日志不机械拼接。

| 来源 | 本文采用的内容 |
|---|---|
| `项目启动入口收尾审计报告.md` | 原始盘点、底层调用链、保护边界与尚未实现的建议 |
| `docs/启动入口替代验证与删除候选报告.md` | 第三阶段及 9 月 15 日复核，替代合同与历史 SAFE_TO_DELETE=0 |
| `docs/历史实验入口退役验证报告.md` | 六个编排的需求退役判据、协议固化及删除准备 |
| `docs/第五阶段首批物理清理报告.md` | 六个历史编排 shell 实际删除 |
| `docs/第六阶段剩余旧入口收敛报告.md` | echo/record/pc 的独立需求、旧 record 合同、分类口径 |
| `docs/第七阶段echo诊断入口迁移报告.md` | 四路监视迁移和 tmux 所有权／退出差异 |
| `docs/第八阶段echo物理删除报告.md` | echo 单文件退役和保留边界 |
| `docs/单机shell入口退役审计报告.md` | 七入口需求取消后的依赖审计，sector 底层复用 |
| `docs/单机shell入口正式退役报告.md` | 七入口实际删除、alias 和活动导航收尾 |
| `docs/run_sh最终三目录整理报告.md` | 15 文件迁移、repo_root 和路径消费者修正；作为中间状态读取 |
| `docs/run_sh四入口精简与默认录制检查.md` | 最终 11 文件、4／5／2 布局、none 默认和 camera 保留 |
| `docs/三机record_light_full点云审计.md` | 录制清单、注册扫描／占据云区别、full 与专项诊断的边界 |
| `docs/archive/LEGACY_EXPERIMENT_PROTOCOLS.md` | 六份协议的主要差异、固定 Git 定位及历史默认值漂移限制；原文通过固定版本查询 |
| `docs/FINAL_RUNBOOK.md` | 第 6、7 节：三机运行与收尾、Forest 四种 mode、输出与资格边界 |
| `docs/THREE_UAV_CAMERA_VIEW.md` | 第 8 节：三路查看、Gazebo 菜单、排障与历史现场证据 |
| `docs/EGO膨胀障碍物RViz可视化.md` | 第 12 节：问题机制、显示输出规则、规划保护和历史构建／单测边界 |

汇总时对照 [run_sh README](scripts/run_sh/README.md) 和上表运行／可视化说明，核对现存 shell、关键 launch、两处 Python 路径消费者及本机 `.bashrc`；操作与技术内容现已直接纳入本文。历史教程与项目说明用于检索引用位置，不作为覆盖最新源码的依据。

汇总及收尾检查范围与结论：

- **目录与导航一致：** 根目录三个子目录加 README；现存 shell 11 个、4／5／2 分布，README shell 链接集合完整匹配；无旧平铺 shell 或软链接。
- **录制清单一致：** 只截取并执行源码中的纯 topic 数组组装段，light 100／full 122 个唯一 topic、无重复；light 无点云，full 9 路点云，与最新审计一致。未执行环境准备、roslaunch 或 rosbag。
- **正式入口与依赖一致：** 当前命令、none 默认、多高度 sibling exec、关键 launch 复用及迁移后的路径消费者与最新报告一致；旧报告 15 个入口和默认 light 均已明确为历史。
- **文档收尾：** 13 份历史整理报告与追加的 3 份运行／可视化说明已物理删除，来源表保留普通文件名，活动链接统一改指本文对应小节。原 FINAL_RUNBOOK 的单机退役报告失效链接已随内容合并消除，不再有待确认项；空的 `docs/archive/` 已移除。不新增报告，不修改源码、launch、配置、运行工件或 Git 历史，保留其他既有用户修改。
- **运行结果待独立验证：** 本轮没有 ROS/Gazebo、飞行、录包、图像窗口、qualification、training 或 evaluation 验收，不新增任何运行 PASS。历史报告中的测试计数和现场记录只归属于各自当轮。

<a id="rviz-inflation"></a>

### 12. 三机 RViz 局部障碍膨胀显示

本节吸收原 EGO 可视化说明。以下修复与测试为历史记录，本轮只合并文档，未修改 EGO 或 RViz 配置，未重跑构建／测试或启动界面。

#### 12.1 问题机制与显示规则

同高度任务中，原 RViz 直接订阅 EGO 的 `grid_map/occupancy_inflate`。这个话题供规划／任务使用，其中同时包含真实感知障碍的膨胀体素和人为设置的虚拟限高平面。`clearAndInflateLocalMap()` 会把局部窗口 XY 范围内整层限高格子设为 occupied，原显示把它们全部画成红色方块。

同高度 profile 的 `local_update_range_x/y=12.5 m`，三架机的大窗口叠加，使虚拟平面看起来像“全图膨胀”。多高度 profile 默认局部半范围 X/Y 为5.5 m，虚拟限高通常在当前窗口之外，因此两种任务的视觉表现不同。此次没有取得用户当时画面的点云或运行参数快照；源码确认存在上述显示缺陷，不能据此断言昨天与今天除任务选择外没有其他运行差异。

修复新增一个只供可视化的输出，RViz 的三机显示改为：

| 无人机 | 新显示话题 |
|---|---|
| UAV1 | `/uav1/drone_0_ego_planner_node/grid_map/inflated_cloud` |
| UAV2 | `/uav2/drone_1_ego_planner_node/grid_map/inflated_cloud` |
| UAV3 | `/uav3/drone_2_ego_planner_node/grid_map/inflated_cloud` |

输出规则：

- 只遍历当前 `local_bound_min_`／`local_bound_max_`，不加 `local_map_margin`，不另设感知半径。
- 只显示已膨胀且有真实 occupied 体素贡献的格子：`occupancy_buffer_inflate_ != 0` 且 `occupancy_buffer_inflate_count_ > 0`。
- 仅由虚拟限高产生、没有实际障碍贡献的平面不进入此话题；限高平面上的真实障碍及其膨胀仍保留显示。
- 保留原 frame、点云源时间戳和可视化截断高度；沿用现有可视化 timer，有订阅者时才构造点云。
- RViz 保持 `Decay Time: 0`、0.25 m红色半透明方块；未初始化或 reset 后输出空帧，避免沿用旧障碍画面。

`grid_map/occupancy_inflate`、`stage3/occupancy_inflate`、虚拟限高、实际 occupancy/inflation、碰撞检查、任务候选和控制链都保持原样。新输出是“真实障碍膨胀显示”，不代表全部规划硬约束；不能用它推断虚拟限高已被关闭。同高度实际感知窗口仍较大，起飞前若传感器已看到附近实体障碍，显示它们是正常行为。

#### 12.2 使用条件

编译后的新输出由现有 EGO 节点自动发布，无需新增启动参数。同高度和多高度分别使用本模块第 6 节的正式命令，均带 `--rviz`；当前项目配置默认开启三机局部膨胀显示。

必须重新启动 EGO 进程并重新加载项目 `triple_tower.rviz`；已经运行的旧节点不会因源码或库更新自动获得新话题。若 RViz 载入个人另存配置，需重新加载项目配置。

#### 12.3 历史验证及未验证范围

原修复使用独立地图单测覆盖：仅虚拟平面时显示为空但规划仍判占据；虚拟平面上的真实障碍仍显示；局部窗口外的格子被排除、移动后按新窗口显示；reset后清空。每次构造显示点云前后比较 raw、inflated、inflation-count 缓冲及局部边界，确认没有写入规划状态。

原报告记录已完成 `plan_env`、`ego_planner` 构建；地图测试16/16、扫掠碰撞测试6/6通过，共22个独立用例，0失败。构建日志为 `/tmp/astra_rviz_inflated_build.log`，测试日志为 `/tmp/astra_rviz_inflated_tests.log`，XML位于 `AstraDrone_ros1_ws/build/test_results/plan_env/`。

源码对比确认：从 `grid_map.cpp` 去掉本次新增的 debug publisher、回调调用及显示构造函数后，与原修复开始时的 HEAD 逐字一致。RViz YAML 的有效差异只有三机话题切换，样式和 `Decay Time: 0` 保持。未启动 Gazebo、飞行或 RViz，真实界面效果待运行确认。
