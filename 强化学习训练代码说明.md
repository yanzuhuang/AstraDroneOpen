# 强化学习训练代码说明

本说明对应 `强化学习训练整理` 分支。整理基线是 `三机巡检最小可运行整理` 的 `9b5b2049899e421b2c728e2c7547615ddc0501f7`，起始工作区干净。此次只裁剪依赖闭包之外的受版本控制内容、清理安装/测试引用并隔离构建路径；SAC、Reward、Observation、规划器、控制器及正式训练参数均未调整。原分支和历史提交保留，不推送远端。

## 当前系统与轻量化环境

实际主入口是 `scripts/run_sh/reinforcement_learning/learning_speed_forest_sac.sh`，进入 `hector_ego_training_backend/launch/hector_forest_sac_training.launch`。当前使用 Forest 环境，不是历史 worksite 绕塔任务。launch 依次包含 `hector_training_episode_reset.launch`、`hector_training_observation_c.launch`、`hector_ego_training_backend.launch`，后者装载 Hector、控制器、truth odometry、EGO 的 `advanced_param.xml` 和 `traj_server`；同时启动 Forest map manager 与 SAC runner。

```text
Forest Gazebo Classic + Hector + Mid-360
  ├─ ground_truth/state → gazebo_truth_odometry_adapter → /uav1/Odometry
  ├─ /uav1/livox/lidar → truth_cloud_registration_adapter → EGO GridMap
  └─ 原始点云 + truth pose history → 五帧球面 surrogate → Observation C
Observation C → SAC Actor → stamped speed request → SpeedAdapter / SpeedSafetyFilter
  → stamped action → EGO 动态 v_max → B 样条 → traj_server PositionCommand
  → position_command_to_hector → Pose/Twist controller → Gazebo UAV
Episode terminal → transition/Replay closure → reset coordinator → 下一 Episode
```

训练用 Hector 替代 PX4/MAVROS 执行链，用 Gazebo truth pose 替代 FAST-LIO 状态估计，但仍使用真实仿真传感器点云，不是用真值地图直接替代感知。`gazebo_ros_p3d` 输出 world/base_link 状态；Mid-360 为 15 Hz、每扫描 20000 点，PointCloud2 模式 2、`mid360_link`，传感器安装平移为 `[0,0,0.13] m`。注册点云供 EGO 使用，原始点云和因果 truth pose 供 Observation 使用。Hector 接收 Pose/Twist，PositionCommand 的加速度并不等价于控制器实际消费的加速度命令。

`simulation/forest` 保留 base world、seed0–9 world、`forest_pool_v1.json`、生成器和地图 QA。world 使用内嵌几何；正式训练由 map manager 读取池并执行地图切换。场地 X=[0,60]、Y=[-10,10]，名义起点 `(5,0,3)`，目标 `(57.5,0,3)`。训练 seed0–7 每 100 completed Episodes 换图，每轮 800 Episodes；evaluation 单独选择 seed8 或 seed9。Gazebo 本体、ROS Noetic、系统库和外部 Hector 源码仍是机器环境依赖。

## Observation 与学习输入

Observation C 的策略输入冻结为 3267 维：3200 个 LiDAR surrogate 值、20×3 个未来轨迹位置、3 维实际速度、3 维 tracking error，以及上一 applied v_max。未来位置来自 EGO 正式 B 样条，统一到当前机体坐标与时间，不用自行生成的理想轨迹代替。

3200 维感知由 80×40 球面方向 bins 表示，合并五帧点云并用 truth pose 对齐，区分 obstacle/free/unknown；unknown 的估计是项目 surrogate，不声称精确复现论文未公开的方向探针实现。`observation_v2_lidar_surrogate.yaml` 定义编码和几何，`observation_c_trajectory_fusion.yaml` 定义融合。frame、stamp、source provenance、generation 或轨迹缺失均 fail closed。mission/planner 状态、点云 masks 和复杂度诊断不直接扩展 policy input。

旧 Observation v1 公共 builder/types/voxelizer 暂时保留：当前 `speed_adapter_node.py` 仍从 `learning_speed_rl.observation` 导入它们，不能凭版本名删除。v2 则是 C 的实际输入依赖，绝非废弃版本。此次没有改写这些接口以追求更小文件数。

## SAC 网络和训练流程

`learning_speed_rl/src/learning_speed_rl/training/sac.py` 实现 Gaussian Actor、双 Q、target Q、自动 entropy 系数与异步 LearnerWorker。Actor 为 3267→256→256→mean/log_std，经重参数化 Gaussian 和 tanh 输出一维动作；Critic 将 Observation 与 action 拼接，经两层 256 隐层输出 Q。使用 ReLU、双 Q 的保守目标、soft target update，没有 PER 或额外 CNN。

`training/sac_replay.py` 保存策略经验；`training/astra_drone_env.py` 的正式 Episode 执行拥有唯一 open transition，名义决策周期 0.1 s。只有 request/action/applied 身份、正式轨迹来源和因果 post-hold Observation 闭合后，才能顺序写入 Replay 并生成下一 request。来不及准备的 Observation 记录 tick miss，不能补造重叠 step。`transition_persistence.py` 提供有序不可变记录，`actor_action_ownership.py` 管理 Actor action 的归属。

前 1000 条有效经验期间使用 deterministic Actor mean；到 learning_starts 后开始 learner，前 100 次更新仅更新 Critic，随后按配置更新 Actor 和 alpha。Checkpoint 按 completed Episode 保存，不以预设 transition 总数结束训练。旧 pilot 不提供 resume 支持。

## Learning Speed Reward

唯一公式 owner 是 `training/reward.py::LearningSpeedReward.evaluate()`；当前配置 `config/stage1_reward.yaml` 选择 `stage_1`，版本为 `astradrone_paper_guided_reward_v3.1`。不能依据旧报告把当前公式写成 v3.0 或旧 UNKNOWN 固定权重。

令 N 为最近已知障碍距离，D 为已知障碍 bin 比例，clip 表示截断至 [0,1]：

```text
qN = clip((6 - N) / 3.5)，N 缺失时 qN=0
qD = clip((D - 0.040) / 0.040)
φ2 = 1 - (1-qN)^0.46 * (1-qD)^0.54
φ1 = 1.75 - φ2
w_safe=(1-φ2)^2，w_middle=2φ2(1-φ2)，w_danger=φ2^2
r_speed = w_safe*0.80*(v-φ1) + w_middle*0.25*v + w_danger*1.00*(φ1-v)
r_smoothing = -0.10*(applied_v_max - previous_applied_v_max)^2
r_error = -2.00*min(||tracking_error_body||,0.40)^2
r_danger = -2.00*v^2（仅 dangerous terminal；否则为 0）
r_total = r_speed + r_smoothing + r_error + r_danger
```

v 是 state_t 的实际机体速度范数，不是请求的速度上限。v3.1 不将 UNKNOWN 本身当作 Reward 目标；N 缺失且 D=0 时 φ2=0、φ1=1.75。Bernstein 权重连续混合，Low/Medium/High label 只是诊断。progress、完成奖励、存活时间、距离目标、planner shaping 不进入此公式；tracking error 同时是 Observation 输入、直接惩罚和独立安全门的依据。invalid、跨 Episode 或 truncated 候选不得包装成 training-ready reward。

这些是项目 paper-guided 实现与标定值，不声称论文精确复现。Stage 2 的速度项候选仍在同一个 owner 中，未启用；不为删除候选分支改动奖励逻辑。`calibration_data_recorder.py` 仅为当前 Reward 的在线/离线一致性回归保留，不是正式 train 入口。

## 动态速度链、Episode 与 evaluation

归一化动作 a∈[-1,1] 映射为 `v_max=1.025+0.725*a`，即 [0.30,1.75] m/s。Runner 发布 SpeedRequestStamped；request-driven MockSpeedPolicy 将请求交给相同 SpeedSafetyFilter，生成 SpeedActionStamped；EGO 消费 stamped action 并返回 SpeedAppliedStamped。episode_id、step_index、request_id 必须一致，scalar topic 只作状态镜像。安全过滤器做 finite 检查和 capability bounds clamp，不增加 slew/低通动作整形。训练 launch 的 filter/EGO 静态能力上限为 4.00 m/s，和 SAC 策略动作上限 1.75 m/s 是两种不同约束。

相邻 action delta 超过 [-0.3,+0.5] m/s 时，Learning Speed 额外强制一次重规划；其余时候遵守 EGO 原生规则。RL 只修改速度约束，不选择航点、不直接发 Hector 控制，也不替代 EGO 碰撞检查。规划器中的共享轨迹结构和安全代码保留，删除的是独立 swarm inspection 任务包与入口。

Reset 由 `training_episode_reset_coordinator.py` 独占：terminal latch 阻止新请求，唯一 transition exactly-once closure、Replay 顺序写入完成并收到 ACK 后，才停止控制、暂停/teleport zero twist、恢复控制，经过 generation barrier、五帧点云 warm-up 和 fresh trajectory 再进入新 Episode。训练首 Episode 用 nominal Hover，其后以 seed1001 在起点 x/y ±1 m 矩形中采样，z=3、yaw=0，最多 32 次验证；evaluation 固定 nominal Hover。不能将 `AstraDroneEnv.reset()` 当成可绕过 coordinator 的独立 Gazebo reset。

正式训练完成条件是 10000 completed Episodes，每 500 Episode 保存一次，共 20 个 checkpoint，10000 同时为 final，不能开始 Episode10001。独立 evaluation 从指定 checkpoint 运行 100 Episodes，deterministic Actor，不更新网络、不写训练 Replay、不随机 reset。训练不会自动启动 evaluation。

## 当前实际参数与核心目录

| 项目 | 当前生效值或所有者 |
|---|---|
| SAC / Reward 配置 | `learning_speed_rl/config/sac_training_v1.yaml` / `stage1_reward.yaml` |
| hidden / gamma / tau | [256,256] / 0.99 / 0.005 |
| Actor / Critic / alpha LR | 1e-5 / 1e-3 / 1e-3 |
| batch / Replay capacity | 64 / 100000，初始分配4096 |
| log_std / target entropy | [-3,-1] / -1；alpha 初值0.2 |
| learner / policy 更新 | wall-time 5次/s；100次 Critic warm-up 后每2次更新 Actor；每次更新 target |
| policy period / max steps | 0.1 s ROS时间 / 500 |
| Episode 时间上限 | SAC YAML 80 s；Forest coordinator launch 55 s，不能把两个 owner 混写 |
| tracking safety | 1.0 m 持续1.0 s |
| SAC seed / map seed / reset seed | 1 / 20260824 / 1001 |
| EGO static max_vel / max_acc | 4.0 m/s / 3.0 m/s² |
| EGO planning horizon | 5.0 m（以展开后的 launch 参数为准） |
| training / evaluation | 10000 completed Episodes / 独立100 Episodes |

主工作空间保留 `learning_speed_rl`、`MissionControl/hector_ego_training_backend`、EGO 的 `plan_manage/plan_env/path_searching/bspline_opt/traj_utils`、`quadrotor_msgs`、`astra_custom_msgs`、`cv_bridge` 和 `livox_ros_driver2`。仿真保留 `livox_laser_simulation` 包，尤其保留 meshes、scan_mode 和插件源码：插件通过编译时 `__FILE__` 相对路径加载扫描 CSV，不能只搬走 .so。`simulation/forest` 为当前场景资源，ground_plane/sun 为保留的辅助验证 world 依赖。

`third_party/Livox-SDK2` 保留用于 driver2 的 SDK 构建依赖；`third_party/nlopt` 暂按 EGO vendor 构建支持资源保留，当前实际链接是否必须依赖此本地副本未完全确证。完整保留包名单、基线和裁剪计数见 `docs/rl_dependency_audit.json`。

## 构建、启动、训练和评估

以下命令从仓库根目录执行。机器需要 Ubuntu20.04 / ROS Noetic / Gazebo Classic、PCL/OpenCV/Eigen、Livox SDK2 以及 Python PyTorch 等既有依赖。外部 Hector 源默认 `/home/yanzu/rl_reference/controllers/hector-quadrotor-noetic`，可用 `ASTRA_HECTOR_SOURCE` 指定；不自动下载或升级。`runtime_artifacts/sac_python_packages` 是已安装 Python 依赖目录，必须保留；新 checkout 需要另行准备它，仓库不是自带所有二进制依赖的发行镜像。

先构建隔离的最小 ROS overlay，避免原三机分支 devel 残留掩盖缺包：

```bash
./scripts/run_sh/reinforcement_learning/build_rl_minimal.sh
```

默认输出 `/tmp/astra_rl_minimal_overlay` 和 `/tmp/astra_hector_training_overlay`，可分别用 `ASTRA_RL_OVERLAY`、`ASTRA_HECTOR_OVERLAY` 更改，构建和启动时应使用同一设置。脚本只从审计名单链接12个包，保留源码的仓库路径关系。临时目录被清理后必须重建。

```bash
# 最小 Gazebo/reset 生命周期检查：不执行 Episode，不写 Replay，不做梯度更新。
./scripts/run_sh/reinforcement_learning/learning_speed_forest_sac.sh --mode preflight

# 已有 smoke 固定31 Episodes，涉及地图切换与学习，不是一个瞬时启动检查。
./scripts/run_sh/reinforcement_learning/learning_speed_forest_sac.sh --mode smoke

# 正式训练：需要另行满足完整运行资格要求；本次整理验证不授予长期训练 PASS。
./scripts/run_sh/reinforcement_learning/learning_speed_forest_sac.sh --mode training

# 独立评估：替换为实际 checkpoint 及其 completed Episode 编号。
./scripts/run_sh/reinforcement_learning/learning_speed_forest_sac.sh --mode evaluation \
  --seed 8 --checkpoint /absolute/path/to/checkpoint.pt --checkpoint-episode 500
```

可加 `--gui` 打开 Gazebo；`--run-id UNIQUE_ID` 可指定唯一运行 ID，否则自动生成。训练产物在 `runtime_artifacts/rl_training/<RUN_ID>`，评估在 `runtime_artifacts/rl_evaluation/<RUN_ID>`，preflight/smoke 在 `runtime_artifacts/learning_speed/forest_randomization_training_integration_v2/<RUN_ID>`。已有目录拒绝复用。Ctrl+C 在启动终端停止；中断不等于正常完成。`preflight` 会真正启动 Gazebo，不是静态命令。

## 整理范围、限制与验证

删除三机同层/异层巡检、swarm manager/safety/bringup/perception/TF 独立包、塔任务与 MAVROS bridge、FAST-LIO、PX4 仿真部署资产、D435/YOLO、无关车辆/场景/模型、旧固定速度/高速标定入口、非训练工具、无关文档和第三方视觉库。EGO 规划核心保持不变。旧 worksite SAC launch 移除，其通用参数传递测试改为检查 Forest launch；其他现行算法与合同测试保留，以防裁剪破坏训练。

保留的不确定或兼容依赖包括 Observation v1 导入组件、统一 Reward 的 Stage2 分支、calibration recorder 回归依赖、worksite reset 配置与几何合同回归、driver2/SDK2、nlopt vendor 目录、共享消息中的非训练消息。它们不表示对应旧方案仍是默认训练链。此次没有用删公共接口或改算法的方式消除这些边界。

旧 `runtime_artifacts`、build/devel、第三方已生成文件、嵌套 Livox-SDK checkout 属于同目录切分支时共享的本地文件，采用排除方式保留，不纳入本分支提交，也不进入最小构建包清单。这样不会不可逆地销毁原分支的实验结果；物理磁盘目录并非只有本分支受控文件。切回原分支仍应使用原分支对应构建入口。

本次验证结果（2026-09-18）：

- 原有 Hector overlay 缺失，使用保留的官方准备脚本从 canonical checkout 重建通过；12个保留包在新的 `/tmp/astra_rl_minimal_overlay` 中从零配置并编译通过，没有借用原主工作空间 devel。
- 在新 overlay 中，Learning Speed 167项、Hector backend 39项单测全部通过。114个 Python、13个 YAML、34个 package/launch/Xacro XML 解析通过；CMake 安装/测试文件引用检查无缺项，保留运行文件对已删除 ROS 包的静态引用为零。三个 shell 入口 `bash -n` 及主入口 `--help` 通过。
- training 与 evaluation launch 文件解析通过；完整 training 参数展开通过，和隔离构建前的展开结果相比，仅包路径别名与 Xacro 自动生成的注释路径不同。解析 realpath 和 robot XML 后语义一致。保留源码/配置的 SHA256 对照中，仅修改了上述旧 worksite 测试引用，没有改动算法、数值配置或模型。
- SAC 最小启动合同得到 `STARTUP ACTION/CAPABILITY CONTRACT PASS`，Episode、environment step、gradient update、map request 都为0。工件：`runtime_artifacts/learning_speed/rl_minimal_startup_20260918/sac_runtime_summary.json`。
- 实际 Gazebo Forest preflight 得到 `FOREST MAP-SWITCH PREFLIGHT PASS`：3次换图/reset、3次新 generation 的有效 v2/C 和 fresh trajectory，acceptance failures为空，collision/controller failure计数为0。没有执行训练 Episode，未运行31-Episode smoke、正式训练或 evaluation。工件：`runtime_artifacts/learning_speed/forest_randomization_training_integration_v2/rl_minimal_cleanup_preflight_20260918_r02/qualification_summary.json`。
- 首次 r01 在 Gazebo启动前因沙箱 `netifaces.interfaces()` PermissionError 失败，记录保留；只对基础设施限制换新ID重试。r02日志还保留扫描CSV的 `cannot convert str` 提示（随后成功加载800000条扫描数据）和完成后的 Hector controller unload错误。preflight合同通过不能消除这些日志现象；此次没有顺带修改插件或控制器生命周期。

可提交的结果摘要与工件哈希在 `docs/rl_validation.json`；详细构建和单测日志已复制到 `runtime_artifacts/learning_speed/rl_minimal_cleanup_validation_20260918/`。启动与静态通过不能证明长期 SAC 收敛、全动作范围稳定、真机可用或完整高保真验证通过。以往的真实失败不因代码整理被改写。
