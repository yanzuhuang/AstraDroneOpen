# EGO 静态障碍 / 绕塔卡死问题：给 GPT 的上下文

## 目的

请基于以下事实，分析并提出一个**安全、可测试、尽量不修改 EGO vendor 核心**的阶段三改进方案。当前任务是 ROS1 Noetic + Gazebo Classic + PX4 SITL + MAVROS + FAST-LIO + EGO-Planner 的单机仿真；不允许修改外部 PX4 或升级依赖。正常飞行只有 bridge 能发布 MAVROS 控制指令。

本文件是一次故障分析摘要，不应把问题描述成“阶段三静态避障已经完成”。

## 本次现象

无人机完成第一个 30 m 绕塔点后，飞向第二个绕塔点时，EGO 报告障碍并停住。视觉上看起来像尝试向下绕开后卡住。

录制 bag：`/home/yanzu/bag/astra_flight_2026-07-22-15-57-06.bag`。

bag 时长 257 s，只含以下 topic：

- `/mavros/local_position/odom`
- `/mavros/local_position/pose`
- `/mavros/state`
- `/mavros/extended_state`
- `/rosout`

没有录制 `/Odometry`、`/cloud_registered`、`/stage2/cloud_registered_filtered`、`/grid_map/occupancy_inflate`、`/planning/bspline`、`/planning/pos_cmd`、bridge 状态或 TF。因此能确认状态与实际飞行轨迹，但不能从 bag 直接定位哪一个点云体素导致占据。

## bag 证据

时间为仿真时间（秒）：

| 时间 | 事件 |
| --- | --- |
| 10636.767 | 首个进场目标：`(-4.70, 6.78, 4.00)` |
| 10674.517 | 同一水平位置的爬升目标：`(-4.70, 6.78, 30.00)` |
| 10789.668 | 第二个绕塔目标：`(2.88, 14.35, 30.00)` |
| 10827.869 | EGO 首次报：`terminal point of the current trajectory is in obstacle` |
| 10840.118 | EGO 报：`Suddenly discovered obstacles. emergency stop!` |
| 10840.169 起 | 持续报：`ERROR! the drone is in obstacle. This should not happen.` |

无人机最终约停在 `(3.0, 13.7, 29.8)`。从第二个目标发布后，实际高度范围约为 `29.13–30.04 m`：没有证据表明形成了长距离、明确的向下绕障轨迹；更符合“重规划失败 + emergency stop / 持续重试后悬停”的行为。

## 当前任务参数与几何

当前未提交配置 `astra_tower_mission/config/stage2_ego.yaml`：

- 选择的目标塔：`radio_tower`
- 塔中心：`(-10.0551, 19.7104)`（map ENU）
- 塔碰撞半径：`6.41 m`
- 任务绕行半径：`14.0 m`
- 最小安全距离：`2.0 m`
- 进场高度：`4.0 m`
- 绕塔高度：`30.0 m`
- 最大任务高度：`32.0 m`
- 目标高度直接传给 EGO：`use_goal_height=true`

因此第二个绕塔点到 `radio_tower` 中心的水平距离为 14 m，不在该任务层定义的无线电塔包络内。任务层余量为 `14 - 6.41 = 7.59 m`，大于要求的 2 m。

world 的实际状态中还有：

- `tower_crane` 约在 `(6.36, 21.73, 0)`；它**没有**被阶段2任务层纳入障碍/净空检查。
- `person_walking` 约在 `(2.97, 12.13, 0.05)`；它水平上靠近第二航点，但物理高度很低。

因此，如果“吊塔”指 `radio_tower`，目前航点并不在其配置碰撞包络内；如果指 `tower_crane`，任务层根本还不知道它。无论哪种情况，都仍需用注册点云和膨胀占据图验证，因为 Gazebo 碰撞几何、点云回波和任务层的圆形包络不是同一个东西。

## 当前三层状态机

### 阶段2任务管理器（`stage2_ego_mission_node`）

正常路径：

```text
WAIT_INPUTS -> PUBLISH_GOAL -> WAIT_PLAN -> TRACK_GOAL
                                      ^         |
                                      |         v
                              下一航点 <- 到达并保持
TRACK_GOAL（所有点完成） -> RETURN_HOME -> DONE
```

失败路径：

```text
检测到任务层失败 -> FAILURE_HOLD -> FAILURE_LANDING -> ERROR
```

任务层仅在如下情况失败：10 s 没有新 trajectory id、指令/里程计过期、跟踪误差超过 1 m、bridge 进入 `HOLD/ERROR/LANDING/DONE`、或单航点超过 240 s。

它**没有**“目标占据 / EGO 重规划失败 / 跳过该航点”的状态。

### EGO 规划 FSM

```text
INIT -> WAIT_TARGET -> GEN_NEW_TRAJ -> EXEC_TRAJ
                                 ^         |
                                 |         v
                             REPLAN_TRAJ <-+
                                 |
                          EMERGENCY_STOP
                                 |
                    速度低后再尝试 GEN_NEW_TRAJ
```

EGO 在发现障碍时会重规划；若终点或当前控制点占据，可能进入 emergency stop，并继续尝试生成轨迹。它只输出轨迹/日志，不向任务层发布明确的“该目标不可达”结果。

### bridge / PX4 控制 FSM

```text
WAIT_FCU -> WAIT_INPUTS -> PRESTREAM -> ARM_OFFBOARD -> TAKEOFF
  -> HOVER_READY -> TRACK_EGO -> LANDING -> DONE
                         |
                         v
                       HOLD -> LANDING
```

bridge 只要仍收到新鲜且合法的 `/planning/pos_cmd`，就会停留在 `TRACK_EGO`；它不理解 EGO 内部是否已经判定当前目标不可达。

## 当前故障的跨层缺口

根据本次日志，最可能的状态组合是：

```text
任务层：TRACK_GOAL（第二个绕塔点）
bridge：TRACK_EGO
EGO：EMERGENCY_STOP <-> GEN_NEW_TRAJ 的失败循环
```

因此任务层并不会立即进入 `FAILURE_HOLD`，而是可能继续等待 240 秒航点超时。EGO 若仍发布停止/重试形成的合法命令，bridge 也不会触发 HOLD。这是本次“卡住”的核心系统问题。

## 已知技术风险 / 待验证假设

以下均尚未由当前 bag 直接证实，需按优先级验证：

1. `/cloud_registered` 或 `/stage2/cloud_registered_filtered` 与 `/Odometry` 的 `camera_init` 坐标不一致，导致低处物体被错投影到约 30 m。
2. 任务层只检查 `radio_tower`，没有检查 tower crane 等其它静态障碍或由其引起的路径净空。
3. EGO 运行高度 30 m，而 `virtual_ceil_height=32 m`；上方逃逸空间只有约 2 m，局部优化出现冲突时更可能尝试下降。
4. 点云、局部更新范围、膨胀或占据图存在陈旧/错误体素；EGO 的 cloud callback 直接假定点云与 odom 在同一坐标系。
5. 当前 30 m / 14 m 半径方案是未提交修改，历史 8 m 阶段2通过证据不适用于本次飞行。

## 希望的改进方向

不要只做“失败就直接跳下一个航点”。更安全的层级策略应为：

1. **目标发布前检查**：等待地图新鲜；检查候选目标是否位于 EGO 膨胀占据图，且检查当前位置到候选点的短走廊/可达性。
2. **拒绝而非盲跳**：被占据目标标记为 `REJECTED`；下一点也必须重新检查。绕塔时跳点不能导致穿过塔或未知区域。
3. **明确规划失败接口**：EGO 的目标占据、连续重规划失败、emergency-stop 超时应作为可机器读取的失败事件传给任务层，而不是解析日志。
4. **安全恢复**：若当前飞机也被判在障碍中，不能继续跳点；先 HOLD，寻找安全恢复点。恢复不可行则返航或降落，且不能继续跟踪旧危险轨迹。
5. **障碍模型分层**：任务层可为已知静态模型提供保守禁区/净空检查；FAST-LIO+EGO 点云仍是实际避障权威。不能仅依赖 world 中的模型名。
6. **阶段三验收**：从一个大且易观测的静态障碍开始，在不解锁时先验证原始点云、注册点云、占据图、膨胀图与 Gazebo 几何对齐；然后验证绕行、最小净空、不可达目标、地图过期和规划失败安全终态。

## 下次必须录制的 topic

至少录制：

```text
/livox/lidar
/cloud_registered
/stage2/cloud_registered_filtered
/Odometry
/grid_map/occupancy
/grid_map/occupancy_inflate
/planning/goal
/planning/bspline
/planning/pos_cmd
/ego_planner_node/goal_point
/ego_mavros_bridge/state
/ego_mavros_bridge/tracking_error
/tower_mission/state
/tower_mission/current_target
/tf
/tf_static
/rosout
```

## 约束

- 当前只处理单机 Gazebo/PX4 SITL。
- 不修改外部 PX4、FAST-LIO 或 EGO vendor 核心；若确实需要 EGO 修改，应先形成独立、可测试的 patch 并获得批准。
- 仅 bridge 可发布 MAVROS 控制 setpoint；任何时刻只能有一个 MAVROS 控制出口。
- 不应在没有 preflight、控制权检查和明确批准时启动 `--control` 飞行。
- 目标是先完成阶段三静态障碍与不可达目标的安全闭环，不进入动态障碍或多机。
