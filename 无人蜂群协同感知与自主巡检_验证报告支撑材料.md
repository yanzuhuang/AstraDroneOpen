# 《模型数据互补融合的无人蜂群协同感知与自主巡检》验证报告支撑材料

整理日期：2026-09-18。本文面向比赛验收，依据当前源码、launch、参数、已有运行记录和轨迹图整理；本次未启动仿真、飞行或训练，仅新增本文档。

**工程结论：三机协同感知信息组织、EGO-Swarm 协同规划、同层与异层绕塔及返航降落已形成可运行系统。低空同层与高空异层均找到完整任务工件，支持“经多轮仿真验证，能够完成完整巡检流程”的定性表述。SAC 已完成环境观测、速度约束决策、EGO 执行与训练数据闭环的工程集成和实际交互验证，尚不能称为已收敛策略或三机部署成果。AirGym 属后续迁移计划。**

证据使用说明：

- “已实现”指当前代码和参数存在；“已验证”须有实际运行工件，或明确注明项目负责人在本次需求中确认的多轮运行经验；“计划开展”不计入当前成果。
- 对已开展多轮但没有逐次统计的同层、异层和自主避障实验，使用定性工程结论，不要求重做成功率统计。已有成功、失败和中断工件均保留，不将目录数量当作实验次数。
- 本文单次运行数字只描述所指记录，不代表总体均值或成功率。截图、视频等尚未归档的材料写“待人工补充”；仅对新增量化指标使用“可选补测”。
- 当前分支为 `三机巡检最小可运行整理`，HEAD 为 `fa186ea8bbbf6fd6aa3d2ae0bff1e0cf65f6aa75`。开始整理时已有未跟踪文件 `三机巡检最小可运行代码审计.md`，未改动。历史运行使用的版本见各目录 `run_metadata.txt`，不把历史运行称为本次 HEAD 新增回归。

## 1. 测试环境与系统组成

| 组成 | 当前实际配置与作用 | 依据 |
|---|---|---|
| 操作系统与中间件 | Ubuntu 20.04.6 LTS、ROS1 Noetic、Gazebo Classic 11.15.1、MAVROS 1.20.1 | 本机 `/etc/os-release`、已安装软件包版本；三机仿真入口见 S1、S2 |
| 飞控与无人机 | 三套 PX4 SITL，`iris_mid360_d435` 四旋翼，每机独立 MAVROS 和命名空间 | S2、S3；外部 `/home/yanzu/PX4-Autopilot` 为 `v1.15.4-dirty`、HEAD `99c40407ffd7ac184e2d7b4b293f36f10fe561ef`，本次只读核对 |
| 主实验场景 | `worksite.world`，塔体、吊机、树木、电杆等障碍；塔中心 world XY 为 `(-10.0551,19.7104) m` | S1、`simulation/astra_gazebo_worlds/worksite.world` |
| 出生位置 | UAV1/2/3 的 world XY 分别为 `(0,0)/(4,0)/(8,0) m` | S1 默认参数；对应每机局部坐标原点平移 |
| 激光与惯性传感器 | 每机 Mid-360 仿真雷达及 IMU；full-stack 模型配置雷达 10 Hz、IMU 100 Hz，雷达 downsample=1 | S3；这是设定频率，不是本次测得吞吐率 |
| 相机 | 每机一套 D435 RGB-D，默认启用；RGB 640×480、水平视场 1.211 rad，彩色/深度等相机更新配置 30 Hz | S3；YOLO 是独立只读感知扩展，未进入导航控制链 |
| 定位与点云 | 基于 FAST-LIO2 的项目适配激光惯性里程计，`fastlio_mapping`；雷达与 IMU 融合产生位姿、注册点云和 TF | S4，`feature_extract_enable=0`、`point_filter_num=3`、`max_iteration=3`、表面/地图滤波尺寸均 0.5 m |
| 局部地图与规划 | 各机独立 EGO-Swarm 规划器与 GridMap，占据更新、障碍膨胀、B 样条优化、在线重规划及队友轨迹避碰 | S5；节点名仍为 `ego_planner_node`，不能据节点名写成未改动的单机 EGO-Planner |
| 执行与控制 | `traj_server → PositionCommand → ego_mavros_bridge → MAVROS raw-local → PX4 OFFBOARD` | S6；控制桥检查状态、输入时效、跟踪误差和控制出口独占 |
| 多机协调与安全 | manager 管理角色、进场/切层/返航许可；safety 检查状态时效与当前/预测机间距离 | S7、S8 |
| 可视化与记录 | RViz 统一 world 下查看三机点云、地图、目标、轨迹与状态；脚本保留 CSV、summary、bag 和轨迹图 | S9、E1、E2；当前脚本默认 `record none`，历史正式记录显式选择 `light` |

主系统的数据流如下；`N=1,2,3`，三条链并行运行：

```text
Mid-360 lidar + IMU
  → FAST-LIO：/uavN/fast_lio/Odometry_raw、cloud_registered_raw
  → frame_adapter：/uavN/Odometry、/uavN/cloud_registered
  → teammate_cloud_filter → self_cloud_filter → 地面过滤
  → /uavN/stage3/cloud_registered_filtered
  → 本机 EGO GridMap + 任务目标 → /uavN/planning/bspline
  → traj_server → /uavN/planning/pos_cmd
  → ego_mavros_bridge → /uavN/mavros/setpoint_raw/local
  → MAVROS → PX4 → Gazebo 机体运动

本机状态 → /uavN/swarm/state、predicted_trajectory → manager / safety / 队友过滤
本机 B 样条 → world 坐标的 /swarm/trajectories → 其他 EGO 的时空避碰
D435 → 图像/深度；可选 YOLO → 检测结果与标注图，不发布飞行控制
```

`world` 是三机共享坐标；`uavN/map` 为本机任务/MAVROS 局部参考；`uavN/camera_init` 为 FAST-LIO/EGO 参考；`uavN/body`、`base_link`、`mid360_link` 和相机 optical frames 表达机体与传感器关系。当前仿真采用本机 map 与 camera_init 的单位对齐约定，world 通过出生点平移连接。frame adapter 主要修正命名空间和 frame 标签，并非自动求解多机外参或全局地图配准。静态仿真 TF 不等于真机标定。（S1、S4、S10）

## 2.1 基于多源信息融合的无人蜂群协同感知与定位建图

**实验设置。** 在 worksite 场景布置三架带 Mid-360、IMU 和 D435 的无人机，每机独立运行激光惯性定位、点云过滤及局部占据建图，在统一 world 坐标下汇集状态、规划轨迹与可视化信息。FAST-LIO 用 IMU 运动信息与激光几何观测估计位姿，并形成注册点云；EGO GridMap 负责服务规划的占据表达。当前 EGO 点云路径已包含跨帧占据更新、射线 hit/miss 与膨胀处理，不能将它描述为纯单帧点云避障。（S4、S5、S10）

“模型数据互补”在本系统中可落实为：激光/惯性数据提供实时环境与运动观测，已知塔体几何及任务包络提供安全约束，队友状态和未来轨迹提供其他无人机的运动信息，规划器据此生成可执行轨迹。塔体 keep-out、已知粗几何、传感器点云与膨胀地图有不同职责，不能把仿真真值、先验模型和在线估计混写为同一种测量。

**测试方法。** 对照 Gazebo 中塔体、障碍物和三架机体位置，在 RViz 中检查各机点云、机体姿态、局部地图和规划轨迹是否位于相应空间位置；结合时间戳、frame、定位健康和轨迹链记录，检查三机信息能否同时进入协同决策。通过完整巡检期间的状态/轨迹更新和最终任务闭环验证系统可用性。此方法验证工程链路与空间一致性，不直接给出定位绝对精度、建图误差或通信带宽。

三机相对单机实际新增的信息和能力如下。

| 信息 | 产生与共享方式 | 实际使用方式及边界 |
|---|---|---|
| 位姿、速度、加速度、任务阶段、目标、心跳和定位状态 | `swarm_state_publisher.py` 从本机里程计及任务/飞控状态产生 `/uavN/swarm/state`，变换到 world | manager 判断角色与许可，safety 判断风险，点云过滤器依据队友位置排除机体回波；没有实现跨机位姿联合优化 |
| 正式未来轨迹 | 本机 EGO 输出带起始时间、控制点、节点向量与机号的 B 样条，转换到 world 后经 `/swarm/trajectories` 共享 | 其他 EGO 按共同绝对时间预测队友位置，参与 B 样条优化和冲突检查；共享的是未来运动计划，不是只共享当前点 |
| 安全预测轨迹 | 状态发布器基于位置、速度、加速度生成有界外推，当前预测时域 4.0 s、采样周期 0.25 s | 供安全与协调使用；与正式 EGO B 样条是两种消息，不能混称为同一规划轨迹 |
| 注册点云与占据地图 | 各机由自身激光/位姿生成并独立维护；RViz 可同时显示 | 主链没有把其他机点云或占据地图合并后供本机规划；多视角展示不等于联合建图或远端盲区补全 |
| 图像/检测信息 | 各机 D435 图像；可选 `/uavN/yolo/detections` | 扩展为多个观测视角；没有证据证明已实现跨视角目标关联、三维语义融合或检测驱动的巡检业务闭环 |

补充边界：仓库存在 `world_cloud_fusion_node.cpp`，但它是双路点云按 home 平移后的可视化拼接；其 launch 开关 `enable_cloud_fusion` 默认 false，普通三机入口没有启用三机联合融合。验收可采用“**多源定位感知、分布式局部建图与状态/轨迹共享协同**”表述，不能写成“已完成三机全局稠密地图实时融合”。（S10、S11）

**验证过程。** 每机从雷达/IMU 启动、FAST-LIO 输出有效位姿和点云开始，经过本机命名空间适配、队友/自身点云排除与地面过滤，把环境障碍送入 EGO；管理器收齐健康状态后组织进场和巡检，轨迹共享在飞行中持续更新。已有 E1/E2 的 `swarm.csv` 保留各机 raw FAST-LIO、planner odom、MAVROS 位姿计数和时效字段，以及三机轨迹链 ID；`summary.json` 的 `full_three_trajectory_messages` 分别为 828、1690，说明对应记录中确实观察到含三机的共享轨迹消息。这是消息次数，不能当作通信频率或任务次数。

**实验结果。** 结合项目已开展的多轮仿真验证和 E1/E2 完整记录，三机能够在共同场景下同时提供位姿、局部环境和未来运动信息，协同感知接口持续支撑同层/异层巡检、局部避障与返航降落。各机均形成独立导航链，并可在统一坐标下展示。三机相较单机增加了队友的状态、运动预测、正式轨迹和角色信息，使规划器与任务层能够处理机间相互影响。

**结果分析。** 当前成果重点是多源感知到多机任务执行的工程闭环，而非地图融合精度提升的定量证明。三机任务完成可说明输入和协调信息足以支撑所测场景；不能据此推导定位误差下降比例、地图完整率、目标识别准确率或全时域无碰撞保证。激光—IMU 融合已实现；跨机信息在状态、轨迹、任务与安全决策层协同，地图仍是各机局部维护。

**插图/视频位置。** 本节实验设置之后插入“三机 Gazebo 场景及传感器配置”截图；信息共享表之后插入“RViz 三机点云、机体、局部地图和共享轨迹”截图；结果段配合从三机起飞到进入巡检的连续视频片段。Gazebo/RViz 截图及视频文件由项目方从已完成实验中整理，**待人工补充**。目前已有轨迹图见下一节。局部 `grid_map/inflated_cloud` 为显示用数据，不能将虚拟限高显示或 RViz 叠加误认成新的协同建图结果。（S9、`项目整理文档.md` 的 RViz 膨胀显示说明）

## 2.2 基于 EGO-Swarm 的多机协同轨迹规划与分层巡检

**实验设置。** 三机在同一 worksite 塔体周围执行逆时针八扇区巡检，任务层选择正式目标，EGO-Swarm 在本机地图及队友未来轨迹约束下规划到达路径。当前普通入口默认为 3 m 同层单圈；独立异层入口默认每机一个高度，`--multi-layer` 启用每机两层。高空同层由通用高度参数配置，当前没有独立固定 30 m 的正式 shell 入口。（S1、S12）

| 参数 | ① 低空同层 | ② 高空同层 | ③ 高空异层/多层 |
|---|---|---|---|
| UAV1/2/3 巡检高度 | `3/3/3 m` | 通用 launch 支持三机设置相同 `top_height`；30 m 属可配置工况，不是当前默认 | 单层 `26/20/14 m`；两层分别 `26→22 / 20→16 / 14→10 m` |
| 层偏移与圈数 | `[0]`，`inspection_laps=1` | 单层 `[0]`，默认一圈；具体历史高空运行参数须与素材关联 | 默认 `[0]`；启用多层为 `[0,-4]`，`planned_cycles=1`、每层一圈，即每机两层各一圈 |
| 起飞/初始悬停 | 起飞目标 3 m；普通 staging 参数为 top−0.5，即 2.5 m；低空流程由 bridge 完成起飞后进场 | 应采用高空任务配置并明确起飞、爬升及 low_altitude 开关，不能只把低空高度改成 30 | 起飞和 staging 均 4 m；UAV1/2 经 10、18 m 中间高度，UAV3 经 10 m，再到首层 |
| 名义进场/巡检速度 | 0.60 / 0.60 m/s | 复用三机框架默认 0.60 / 0.60 m/s；不冒充历史运行实测值 | 0.60 / 0.60 m/s |
| 切层名义速度 | 单层不发生切层 | 单层不发生切层 | 0.30 m/s；manager 使用 `0.30/0.60=0.5` 速度缩放，执行桥按许可/缩放限制命令 |
| EGO 最大速度/加速度 | 0.60 m/s、0.50 m/s² | 通用默认同左，具体工况可参数化 | 0.60 m/s、0.50 m/s² |
| bridge 最大速度/加速度 | 0.60 m/s、0.50 m/s² | 通用默认同左 | 0.60 m/s、0.50 m/s² |
| 候选高度偏移 | `[0]` | 当前三机参数默认 `[0]` | `[0]`；层间 −4 m 与候选高度偏移不同 |
| 角色 | UAV3 leader → UAV2 middle → UAV1 trailing | 通用角色顺序同左 | 同一角色顺序；不同高度不强制执行同层软相位跟随 |
| Learning Speed | 默认关闭；可选开关启动 mock adapter | 不等于已加载 SAC 策略 | 异层 profile 明确禁止 Learning Speed |

上述速度是名义值或上限，不是全程匀速实测值。等待、转弯、避障、轨迹加减速、相位缩放和切层都会改变实际速度；也不能用配置上限代替飞行轨迹的三维速度统计。

共同参数：巡检名义半径 12.5 m，外圈候选 14.5/16.5 m；固定八扇区中心为 `0/45/90/135/180/225/270/315°`；PRE_ENTRY 半径 18 m，ENTRY_GATE 名义半径 15 m。三机入口参考角 UAV1/2/3 为 `292.5/315/337.5°`，实际入口由联合候选选择产生，不能当作每次固定轨迹。EGO map 为 90×90×46 m、分辨率 0.25 m、规划时域空间长度 7.5 m、障碍膨胀 0.40 m；低空局部地图更新半范围 XY 12.5 m、Z 4.5 m。任务读取已膨胀地图后仅附加 0.5 m 净空；原始点云/粗几何净空低空为 1.0 m，高空配置为 2.0 m。高空 2.0 m 不应被通用低空描述覆盖。（S1、S5、S13、`rule.md` 第 15 节）

同层目标相位差 67.5°，正常带 57.5°～77.5°，跟随机放行窗口 65°～70°；预警/紧急相位参数 45°，leader 等待阈值参数 95°。到位检查包含高度误差不超过 0.35 m、速度不超过 0.20 m/s；前机放行运动检查使用最小前向速度 0.03 m/s。三机最小三维距离为 3.0 m，EGO `swarm_clearance=1.5 m`。`takeoff_interval_sec=0` 不等于无条件同时解锁，仍须获得安全和起飞许可。entry 目标超时 360 s、任务总超时 3600 s；普通任务到达保持参数为 1.0 s。（S1、S7、S13）

**测试方法。** 将验证过程组织为“起飞悬停—进场放行—绕塔闭环—切层（适用时）—离场返航—落地解锁”。用 Gazebo/RViz 视频展示同时运行与动态避障，用 XY/3D 轨迹及高度时序展示任务空间覆盖；用每机 mission CSV、三机同步 `swarm.csv` 和 `summary.json` 核对任务终态、扇区覆盖、距离和误差。三机最终均为任务/bridge/flight `DONE`、`armed=false`、`extended_landed_state=1`，才能把对应记录表述为完整完成；单纯安全降落或录制中断不等于巡检成功。

**验证过程与完整任务流程。**

1. **起飞与悬停。** 检查 FCU 连接、里程计/点云时效、控制权和 swarm 安全许可；bridge 建立 OFFBOARD 控制并起飞至指定高度。高空任务在 home 悬停位置采用分段爬升，固定 XY 走廊受地图检查；阻塞时进入有界 HOLD/退出处理，不任意改变起飞走廊。
2. **进场与排队。** manager 联合评估三机 PRE_ENTRY→ENTRY_GATE→ORBIT_STAGING 候选，兼顾静态障碍净空、角色顺序、路径关系和预测机间距离。ENTRY_GATE 是正式进场/放行锚点，ORBIT_STAGING 是第一个普通扇区候选，两者职责不同。UAV3 先放行，同层 UAV2/UAV1 在前机满足运动与相位窗口后依次放行，等待期间保持安全位置。
3. **同层绕塔与自主避障。** 各机独立完成八个扇区及回到本层起点的闭环。名义点可行时优先使用；受障碍影响时在同一扇区按半径分层搜索候选，优先保留仍安全的锁定目标。EGO 负责实际绕行与在线重规划，局部直线受阻不自动等于目标不可达；真实规划失败执行有限重试、换候选和 HOLD，必要时安全退出。机体 yaw 面向塔体，便于巡检观察。
4. **异层与层间切换。** 单层异高模式在 26/20/14 m 各执行一圈。多层模式每机完成上层后申请切层，任意时刻最多一个有效切层执行者，按 UAV3→UAV2→UAV1 的角色链交接；不要求所有飞机先完成整圈再统一切层。每段垂向变化不超过 2.0 m，XY 相近时保持相同 XY，否则分段插值连接，由 EGO 检查和规划。到达新层、速度稳定且进入相应任务状态才确认完成，再开始下一层巡检。仅实际处于相同高度队列的飞机执行同层相位/软速度协调；不同高度仍受全局当前/预测三维距离及椭球净空检查。不得把 6 m 名义层差直接当作切层全过程实测最小距离。（S7、S13、`rule.md` 第 15 节）
5. **离场、返航与降落。** 最终层完成后申请退出许可，检查当前层 EXIT_GATE。低空模式沿本机实际进场采样路线逆序提供返航参考，并按最新地图重新规划，不是机械回放旧轨迹。高空模式经当前层 EXIT_GATE 到 home 正上方 `HOME_OVERHEAD_TRANSIT`，再 `SEGMENTED_HOME_DESCENT` 分段固定 XY 下降，使用适用的 18/10 m 中间高度及 home 悬停目标；不执行高空直线斜穿塔区的无检查返航。最终由许可和控制桥完成受控降落、解除武装与 DONE。多层末层退出还有 follower 放行交接检查，避免前机过早离开。（S13、S6、S7）

EGO-Swarm 的协同规划机制是：各机将本地三次 B 样条变换至 world 共享，接收方检查 frame、时间与机号，再转回本地规划坐标；按照本机候选轨迹的绝对时刻，求其他机 B 样条在同一时刻的位置，构造机间距离代价及梯度，与平滑性、障碍物距离、动力学可行性和目标项共同优化。冲突检查可触发重规划。因此，三机路径在 XY 图上重叠不必然碰撞，关键是**同一时刻的空间距离**。（S5）

当前 `calcSwarmCost()` 使用 `d_e=sqrt(Δx²+Δy²+Δz²/4)`，优化参考距离为 `2×swarm_clearance=3 m`；同 XY 时对应 6 m 垂向参考距离。这是优化器各向异性距离，与 safety 的 3 m 欧氏距离门不同，不能相互替代，也不是优化软代价对所有工况的硬保证。历史 2/3/4 m 分层方案不应写入当前已验收工况；当前 26/20/14 m 分配及分段切层已另有真实完整运行记录。

**实验结果。**

- **① 低空同层：已实现、已完成实际运行验证。** E1（2026-09-11 18:33）三机任务/bridge/flight 全部 DONE，均落地解锁、扇区 mask 全部 `0xFF`。全记录机间最小三维距离为 3.840 m；并发绕塔窗口中最小一对距离为 10.079 m。另有 E3（2026-09-06 13:27）的完整 DONE 记录，与项目多轮运行经验共同支持“经多轮仿真验证，三机能够协同完成进场、绕塔、自主避障、返航和降落”的表述。
- **② 高空同层：已有参数化实现；定性运行经验与具体高度材料分开表述。** 根据项目负责人本次需求确认，同层绕塔已经开展多轮实际验证，可保留高空同层工程展示内容，不要求补做成功率。当前通用 launch 可表达三机统一高空高度，但默认值仍为 3 m；找到的 `three_uav_inspection_30m_KLqov4eS/summary.json` 仅记录 WAIT_ENTRY_PERMISSION/HOLDING、三机 `0x00`、累计绕塔角 0°，且没有可核实的完整 30 m 参数快照，不能用目录名证明“该次 30 m 完整成功”。具体高空同层高度、对应成功视频/截图/轨迹和运行日期**待人工补充关联**；不将这份未完成记录包装为成功证据，也不否认其他已完成但未归档的同层实验。
- **③ 高空异层：已实现、已完成两层完整实际运行验证。** E2（2026-09-06 11:37）的 roslaunch 参数明确为 26→22、20→16、14→10 m，三机最终均 DONE、落地解锁、`0xFF`。3D 轨迹直接展示每机两个环形高度层、层间连接及回到地面。全记录最小机间三维距离为 3.842 m，并发绕塔窗口最小一对为 10.643 m。结合项目已有多轮运行经验，可表述为“异层协同巡检与有序切层已完成实际运行验证”。

![图2-2 低空同层三机XY轨迹与高度时序](runtime_artifacts/three_uav_inspection_control_20260911_183305/trajectory_xy.png)

图 2-2：E1 实际状态轨迹，三色对应 UAV1/2/3；展示各机绕塔回路、进出路径和约 3 m 巡检高度。高度曲线存在实际波动，不能写成严格等高无误差飞行。

![图2-3 三机异层两层巡检3D轨迹](runtime_artifacts/three_uav_inspection_control_20260906_113711/trajectory_3d.png)

图 2-3：E2 实际状态轨迹，展示 26→22、20→16、14→10 m 六个巡检高度环及起降连接。图中曲线源于三机记录状态，不是凭空绘制的设计圆。

**结果分析。** XY 图可证明绕塔回路形状与实际路径绕行，3D 图可证明高度分层与切层连接；配合共同时间轴上的高度/状态记录可证明并行执行。仅看静态线条不能证明同时到达、逐时刻无碰撞或所有障碍净空合格，需结合距离曲线、共享轨迹和状态记录。E1/E2 最终状态与距离记录为完整工程闭环提供了更强支撑。累计角统计为 E1 三机约 359.51°/361.73°/358.49°，E2 约 731.90°/719.60°/735.01°；它由活动巡检状态下离散位置的正向角增量累计，可能漏计状态切换或累积局部运动，不是任务闭环的唯一判据，不能把单个数略低于 360°直接判为失败。应联合圈/层任务过程、`0xFF`、图形和 DONE 判断。

**插图/视频位置。** 起飞/entry gate/有序放行段配 Gazebo 连续画面；自主避障段配同一时刻的 RViz 障碍与 EGO 轨迹局部放大图；高空同层另插项目方已有高空成功素材；图 2-3 后插异层切层及依次返航降落视频。现有图可直接引用，额外截图和视频**待人工补充**，不要求为定性验证重新统计实验成功率。

## 2.3 基于 SAC 的环境自适应速度约束决策

**实验设置。** Learning Speed 以单机 Gazebo/Hector training-only 环境开展速度约束研究，已包含 worksite 与 Forest 场景接入。training-only 使用 Gazebo truth odometry、仿真 Mid-360 和 truth-pose 点云对齐，不启动 FAST-LIO、PX4/MAVROS；full-stack 则使用 FAST-LIO+PX4/MAVROS，两条执行/里程计链不能混用。Forest launch 的 EGO 静态能力上限为 4.0 m/s、3.0 m/s²，SAC 动作范围为 0.30～1.75 m/s；二者均不同于三机常规巡检的 0.60 m/s 配置。（S14、S15）

模块目标是利用环境几何复杂度、未来轨迹及执行状态选择合适的最大速度约束：开阔区域允许更高效率，密集障碍或跟踪困难时倾向保守。SAC 不选择航点、不输出飞行方向、不生成轨迹，也不接管多机协调；EGO-Swarm 继续负责路径、轨迹、重规划和机间避碰。这里的效率/风险权衡是研究目标，当前尚未以正式策略对比实验量化证明收益。

```text
五帧 Mid-360 + 因果位姿对齐 → Observation V2 3200 探针
 + 正式 EGO B 样条未来位置 + 实际速度 + 跟踪误差 + 上次 applied v_max
 → Observation C → SAC Actor → 一维 normalized action
 → ActionMapping → SpeedRequestStamped
 → SpeedAdapter → SpeedSafetyFilter → SpeedActionStamped
 → EGO 动态速度约束提交 → SpeedAppliedStamped 回执
 → EGO 轨迹 / traj_server / Hector 控制 → 因果后继 Observation
 → Reward → transition → 有序 Replay → 异步 SAC learner
```

| 项目 | 当前实际值/实现 |
|---|---|
| Observation C | `3200+20×3+3+3+1=3267` 维 |
| 激光环境输入 | 五帧点云按因果位姿对齐，80×40 球面分区形成 3200 个距离/观测状态编码探针；不是 3200 个原始点，也不是三类 one-hot 向量 |
| 其余四项 | body frame 下 20 个未来位置（三维）；实际速度（三维）；跟踪误差（三维）；上一 applied 最大速度（标量） |
| 不进入 policy 的信息 | mission/planner 状态、progress、复杂度标签、clearance、mask、诊断及 run/episode provenance；这些可用于诊断、数据检查或 reward context |
| 动作 | 一维 `a∈[-1,1]`，绝对速度约束 `v_max=1.025+0.725a` m/s，范围 `[0.30,1.75]` |
| SafetyFilter | finite 检查和审核范围 clamp；范围内不做 slew、低通或迟滞，不将动作变成加速度指令 |
| EGO 生效机制 | 独立 speed 回调队列、不可变版本化快照及 stamped ACK；相邻约束变化超出 `[-0.3,+0.5] m/s` 额外请求重规划，其余沿用原生规则 |
| SAC 网络 | Gaussian Actor、双 Q 与 target Q；两层 256 隐层；自动熵调节，初始 alpha=0.2、目标熵 −1、log-std 范围 `[−3,−1]` |
| 优化参数 | gamma=0.99，tau=0.005，batch=64；Actor/Critic/alpha 学习率 `1e−5/1e−3/1e−3`；100 次 critic-only 启动更新；之后 policy 每 2 次更新一次、target 每次更新 |
| Replay 与学习启动 | 逻辑容量 100000，初始分配 4096；`learning_starts=1000` transitions；warmup 使用 deterministic actor mean；异步 learner 配置 5 updates/s 墙钟时间 |
| 时间/停止合同 | policy 名义周期 0.1 s；每 Episode 最多 500 steps、80 s；一次最多一个 active action interval，terminal 闭合和 writer drain 后才 reset |
| 正式训练配置 | 从空 Replay 开始，10000 completed Episodes；每 500 Episodes 保存 checkpoint，共 20 个；seed=1、CPU、PyTorch 线程数 1；这是配置，不是已完成规模 |
| 独立评估配置 | 指定 checkpoint、100 Episodes、deterministic Actor、无网络更新、不写训练 Replay；与训练使用独立目录 |
| 随机场景配置 | worksite reset seed=1001、nominal hover `(0,0,3)`，训练 XY offset `[−1,+1] m`；Forest 配置训练 logical seeds 0～7、评估 8～9，每 map block 100 Episodes，每 round 800 Episodes，调度 seed=20260824；具体运行可有专项覆盖 |
| 工件目录 | training：`runtime_artifacts/rl_training/<RUN_ID>/`；evaluation：`runtime_artifacts/rl_evaluation/<EVAL_ID>/` |

表中当前值来自 S14～S17；传感器/观测链已有 15 Hz 感知与名义 10 Hz policy 的实现和阶段验证，但日志不支持写成所有训练阶段始终实测 10 Hz。3200 探针的 occupied/free/unknown 状态有相应数值编码；单独语义 mask 不作为新增 policy 通道。（S16）

**Reward 主要公式与来源。** 当前唯一实现为 `LearningSpeedReward.evaluate()`，配置选择 `stage_1`，版本 `astradrone_paper_guided_reward_v3.1`。设计吸收项目 Learning Speed 文献材料中的复杂度相关速度奖励及平滑/危险惩罚思路，并加入工程跟踪误差项；融合权重、门限和系数是 AstraDroneOpen 的工程选择/离线标定值，不能宣称精确复现论文未公开参数。完整文献题录及比赛报告参考文献编号**待人工补充**；当前可核查来源是 S17 和《项目技术演进与LearningSpeed阶段汇总》第 23 节。

令 `u=||actual_velocity_body||`，`e=||tracking_error_body||`，`v_t/v_(t−1)` 为相邻实际生效的速度约束；`N` 为最近已知障碍探针距离，`D` 为已知障碍 bins 占 3200 bins 的比例。`clip01` 限制到 `[0,1]`：

```text
q_N = clip01((6.0 − N)/3.5)，N=None 时取 0
q_D = clip01((D − 0.040)/0.040)
p = phi_2 = 1 − (1−q_N)^0.46 (1−q_D)^0.54
phi_1 = 1.75 − p
w_safe=(1−p)^2，w_middle=2p(1−p)，w_danger=p^2

r_speed = w_safe·0.80·(u−phi_1)
        + w_middle·0.25·u
        + w_danger·1.00·(phi_1−u)
r_smoothing = −0.10·(v_t−v_(t−1))^2
r_error = −2.00·min(e,0.40)^2
r_danger = −2.00·u^2·I[明确的 dangerous terminal]
r = r_speed + r_smoothing + r_error + r_danger
```

速度/危险项使用 `state_t` 的实际速度，不是动作指定的最大速度；平滑项使用 stamped applied 链。progress 不进入当前奖励，无额外完成奖励或规划器 shaping 项。v3.1 不再使用历史 UNKNOWN 固定 `phi_1=1.25、phi_2=0.5` 分支；`N=None,D=0` 时为 `phi_1=1.75、phi_2=0`，UNKNOWN 仍保留在观测输入中。Stage 2 仅实现候选公式，将速度项改为 `0.25u`，其他项共用；当前未选择，也没有 Stage 2 已完成训练/评估的证据。

**测试方法。** 检查 Observation frame/stamp 与正式未来 B 样条的因果关系；按 `(episode_id,step_index,request_id)` 对齐 request→action→applied ACK→后继观测→transition→Replay；检查 reward 有限性、Replay 有序性、网络更新和 Episode reset/terminal。环境真实 planner failure、碰撞代理、tracking failure 和超时均保留，不用数据闭合成功替代任务成功。单次测试可证明速度约束被 EGO 接收以及数据链可学习，性能提升仍须固定速度基线与独立策略评估支持。

**验证过程。** 项目先完成 full-stack 固定速度与 Observation 标定，再接入 Hector training-only 后端、truth-pose 点云对齐、自动 Episode reset、SAC/Replay/checkpoint；随后在 Forest 等场景开展短程资格和早期学习观测。已有日志保留真实 request/action/ACK、transition、Replay、网络更新和失败终态。E4/E5 均为实际 Gazebo 交互记录，不是仅离线训练或静态单元测试。（S14～S17、E4、E5）

**实验结果。** E4 完成 100 个 Episode 闭合，记录 29543 条环境 step/Replay、24007 次 learner 更新，但结果为 `NO-GO`，包含 invalid-observation 等真实问题；E5 完成 87 个 Episode，另有 Episode 88 的未终止数据，总 Replay 26826 条、finite reward 26826 条，随后外部中断，仍为 `NO-GO`。E5 requested 速度约束实际覆盖约 0.361～1.747 m/s；Replay 审计 `passed=true` 只说明该项数据审计通过，不能覆盖飞行/训练总体结论。正式 10000-Episode 训练完成、独立 100-Episode 策略评估、收敛及三机部署均无完成证据。

**结果分析。** 创新点已有明确工程接口：把感知、未来轨迹和执行状态压缩成观测，用连续 SAC 决策约束 EGO 速度，并保持任务与安全模块的独立职责。现阶段宜写“已实现并验证环境自适应速度约束决策的训练交互与执行链路”，不宜写“已证明 SAC 提高三机巡检效率”或“已经解决复杂环境安全巡检”。普通三机脚本的 `--learning-speed` 默认启动 mock adapter，也不代表三机运行了已训练 Actor；异层入口明确禁用该模块。

**后续 AirGym 计划。** 本次在项目源文件和文档检索中未发现已接入 AirGym 的训练入口或验证工件。按项目后续方向，可将速度约束决策迁往 AirGym 继续训练，首先建立与当前 3267 维观测、一维速度约束动作及 EGO 下游执行等价的接口，并明确仿真后端、传感器、动力学及 reset 差异，再开展训练与独立评估。若替换/简化 EGO 或感知模型，应作为新的实验设置报告，不能直接继承当前 Gazebo 证据。迁移、训练加速、收敛和回迁三机部署均列“计划开展”。

**插图/视频位置。** 链路图后插 Observation 组成示意；公式后插已有离线奖励曲线（如使用须标注离线分析）；结果段插训练场景视频及时间对齐的实际速度/requested/applied 速度曲线。SAC 曲线不得使用三机常规 0.60 m/s 轨迹冒充；AirGym 图仅可放入“后续计划”。具体比赛展示素材**待人工补充**。

## 3. 可直接使用的工程数据与展示材料

以下 E1～E5 是本次直接读取的代表性记录，选择用于说明不同能力和边界，不构成成功率抽样集。

| 编号 | 实际工件目录 | 可直接引用的材料与证据 |
|---|---|---|
| E1 | `runtime_artifacts/three_uav_inspection_control_20260911_183305/` | 低空同层完整任务；`summary.json`、`swarm.csv`、`uav1.csv/uav2.csv/uav3.csv`、`run_metadata.txt`、`roslaunch.log`、`candidates.jsonl`、`trajectory_xy.png`、`trajectory_3d.png`；bag 文件 64900482 字节 |
| E2 | `runtime_artifacts/three_uav_inspection_control_20260906_113711/` | 异层两层完整任务；同类工件齐备；`roslaunch.log` 保存 26/20/14 m、`[0,-4]`、0.60 m/s 和 0.50 m/s² 参数；bag 文件 102665686 字节 |
| E3 | `runtime_artifacts/three_uav_inspection_control_20260906_132714/` | 另一组低空同层三机 DONE、disarmed、`0xFF`；全记录最小三维距离 3.891 m；可作为多轮定性运行的辅助证据 |
| E4 | `runtime_artifacts/rl_training/ego_speed_async_100ep_endurance_20260901_114200_r01/` | `sac_runtime_summary.json`、`qualification_summary.json`；100 Episode 数据闭合/早期学习记录，总体 NO-GO |
| E5 | `runtime_artifacts/rl_training/post_lifecycle_rollback_100ep_baseline_20260901_202221_r03/` | 相同两个 summary；87 completed Episode，Replay 26826，总体中断/NO-GO；勿将第 88 个计为完成 |

E1/E2 的 bag 文件本次确认存在及大小，未播放或重新解析全量 bag；本文数字来自 JSON/CSV 和日志。`light` 录制不包含点云，也不包含 Gazebo model states，不能承诺仅靠这些 bag 重建完整点云 RViz 场景。`full` 虽增加点云等话题，仍需检查 TF 与所需话题后再判断可否完整回放。当前 `record none` 本就不保留任务证据，因而没有逐次工件不代表实验没有进行。（S9、`项目整理文档.md`）

| 量化项目 | E1：低空同层 | E2：异层两层 | 来源/口径 |
|---|---|---|---|
| 最终任务/bridge/flight | 三机均 DONE | 三机均 DONE | `summary.json` 三组 final 字段 |
| 最终解锁/落地 | 三机 armed=false、landed_state=1 | 同左 | final_armed、final_extended_landed_state；1 对应 ON_GROUND |
| 扇区覆盖 | 三机均 `0xFF` | 三机均 `0xFF` | visited_sector_mask；多层需同时核对层过程，单个 mask 不独立证明两层 |
| 全记录最小三维机间距 | 3.840 m | 3.842 m | `pairwise_minimum.*.three_d` 的最小值；状态采样距离，不是连续时间真值保证 |
| 并发绕塔窗口最小三维机间距 | 10.079 m | 10.643 m | `concurrent_orbit_pairwise_minimum.*.three_d` 的最小值，与全记录窗口不同 |
| 最大跟踪误差 UAV1/2/3 | 0.503 / 0.518 / 0.228 m | 0.386 / 0.405 / 0.420 m | `maximum_tracking_error`；bridge 中 MAVROS 位置与规划目标位置的距离，不是定位 ATE，也不是 RMSE |
| 起飞到首次 DONE 时长 UAV1/2/3 | 421.400 / 413.300 / 387.299 s | 789.300 / 729.499 / 712.300 s | 本次只读计算：`swarm.csv` 首个 uN_phase=DONE 的 sim_time 减 summary.takeoff_times[N]；仿真时间 |
| 记录时间跨度/CSV 行数 | 549.900 s / 5500 行 | 812.900 s / 8130 行 | CSV 最后一行减第一行 sim_time；含任务前后，不等于飞行时长 |
| ERROR/FAILURE_LANDING 进入次数 | 0 / 0 / 0 | 0 / 0 / 0 | `emergency_count`，recorder 按进入任务错误/失败降落状态计数；不能直接命名为 Gazebo 碰撞次数 |
| 规划轨迹 ID 变化次数 UAV1/2/3 | 259 / 280 / 295 | 585 / 555 / 555 | `planner_trajectory_changes`；不是规划失败次数，也不等于静态障碍避让次数 |

任务成功次数不从以上代表性记录外推；比赛报告可展示这些完整实例并使用定性多轮结论。轨迹变化、候选拒绝计数会多次观察同一事件，不能机械相加作为“避障次数”或“规划失败次数”。三机物理接触碰撞总数、全场景定位真值误差、规划耗时均值、带宽等没有在本文建立统一统计，若报告需要这些数字则**待人工补充**。

SAC 已有真实统计可单独列示，但应放在创新模块验证边界中，不混入三机巡检成果：

| 项目 | E4 | E5 | 来源/解释 |
|---|---|---|---|
| completed Episodes | 100 | 87 | 两份 summary 的 episode_count；闭合不是飞行成功 |
| success / failure / truncated | 1 / 91 / 8 | 1 / 76 / 10 | qualification_summary；互斥终态分类 |
| planner_failure_count / collision_count | 59 / 27 | 55 / 20 | qualification_summary 的原始字段；collision 为训练终止/代理口径，不能改称 Gazebo 接触事件，也不要与 failure 重复相加 |
| global_environment_step / Replay | 29543 / 29543 | 26826 / 26826 | sac_runtime_summary 与 replay_audit；E5 含未闭合 Episode 88 的非终止数据 |
| learner/gradient 更新 | 24007 | 22490 | E4 learner_update_count；E5 gradient_update_step |
| 实际速度 median / p95 / max | 0.905 / 1.435 / 2.662 m/s | 0.858 / 1.408 / 2.961 m/s | qualification_summary.actual_speed；分别 48159 / 44289 个监测样本，不是 Replay 条数或成功 Episode 专用窗口 |
| 跟踪误差 median / p95 / max | 0.036 / 0.211 / 2.779 m | 0.038 / 0.229 / 9.754 m | qualification_summary.tracking_error；保留监测窗口异常峰值，不据此宣称训练全程稳定 |
| 最终结论 | NO-GO，final audit invalid_observation | NO-GO，中断于活动 Episode | sac_runtime_summary.status/failure/verdict |

这些数据证明 SAC 确实执行过在线交互与更新，同时也清楚表明当前不能宣称学得了稳定有效的最终策略。避免仅引用 Replay 数量而省略总体结论。实际速度监测峰值与动态最大速度约束是不同量，不能将约束范围解释为实际速度全程严格上界。监测统计的采集实现见 `AstraDrone_ros1_ws/src/MissionControl/hector_ego_training_backend/scripts/training_episode_reset_coordinator.py`。

**最值得补充的 5 项简单展示指标。** 优先从已有成功 CSV/bag 离线整理；仅在素材缺失且比赛确实需要数字时选择性补录，不要求重新开展成功率试验：

1. **完整任务阶段时间线**：每机起飞、entry、绕塔、切层、返航、DONE 的仿真时刻，配视频时间标记；已有 E1/E2 可提取。
2. **机间距离曲线**：三对欧氏距离叠加 3 m 参考线，区分全任务与并发绕塔窗口；已有 `swarm.csv` 可提取。
3. **三机高度—时间曲线**：显示 3 m 同层或 26→22/20→16/14→10 m 切层，标记实际放行时刻；已有图和 CSV 可用。
4. **跟踪误差曲线与窗口内峰值**：沿用 bridge 明确定义，不包装成定位精度；已有 CSV 可提取，RMSE 如需使用应另行明确窗口后计算。
5. **速度约束与实际速度对照**：SAC 场景同时画 requested/filtered/applied 与实际速度，并标注开阔/障碍区域。现有训练数据可展示链路生效；若要宣称提升巡检效率，固定速度与最终策略的同场景独立对比属后续计划。

## 4. 关键文件索引

下列路径均相对仓库根目录 `/home/yanzu/AstraDroneOpen`，便于按编号核查。本文使用的是当前源文件及明确注明的历史运行工件，根目录旧报告若已合并，以当前汇总文档后部最新结论为准。

| 编号 | 准确路径及职责 |
|---|---|
| S1 | `AstraDrone_ros1_ws/src/Swarm/astra_swarm_bringup/launch/triple_tower_inspection.launch`：三机总入口、高度/速度/地图和安全参数传递；`AstraDrone_ros1_ws/src/Swarm/astra_swarm_bringup/launch/uav_tower_stack.launch`：单机任务、规划和 bridge 接线 |
| S2 | `AstraDrone_ros1_ws/src/Swarm/astra_swarm_bringup/launch/triple_px4_mavros.launch`；同目录 `dual_px4_mavros.launch`、`single_vehicle_spawn_verified.launch`：模型与 PX4/MAVROS 启动 |
| S3 | `AstraDrone_ros1_ws/src/Swarm/astra_swarm_bringup/models/iris_mid360_d435.sdf.xacro`；`AstraDrone_ros1_ws/src/Swarm/astra_swarm_bringup/launch/uav_sensor_frames.launch`：传感器模型、频率和 TF |
| S4 | `AstraDrone_ros1_ws/src/SLAM/FAST_LIO/launch/mapping_mid360.launch`；`AstraDrone_ros1_ws/src/SLAM/FAST_LIO/config/mid360.yaml`；`AstraDrone_ros1_ws/src/SLAM/FAST_LIO/src/laserMapping.cpp`：激光惯性定位建图 |
| S5 | `AstraDrone_ros1_ws/src/Planner/ego-planner/planner/plan_manage/src/ego_replan_fsm.cpp`；同级 `planner_manager.cpp`、`traj_server.cpp`；`AstraDrone_ros1_ws/src/Planner/ego-planner/planner/bspline_opt/src/bspline_optimizer.cpp`；`AstraDrone_ros1_ws/src/Planner/ego-planner/planner/plan_env/src/grid_map.cpp`；`AstraDrone_ros1_ws/src/Swarm/astra_swarm_bringup/config/swarm_low_altitude_ego.yaml`：共享轨迹、地图、优化和输出 |
| S6 | `AstraDrone_ros1_ws/src/MissionControl/ego_gazebo_bridge/src/ego_mavros_bridge.cpp`；`AstraDrone_ros1_ws/src/Swarm/astra_swarm_bringup/config/uav1_bridge.yaml`、同目录 `uav2_bridge.yaml`、`uav3_bridge.yaml`：控制状态机与执行限制；最终值还须服从 S1/S12 的 launch 覆盖 |
| S7 | `AstraDrone_ros1_ws/src/Swarm/astra_swarm_manager/scripts/swarm_manager_node.py`；同目录 `swarm_state_publisher.py`；`AstraDrone_ros1_ws/src/Swarm/astra_swarm_manager/src/astra_swarm_manager/policy.py`；`AstraDrone_ros1_ws/src/Swarm/astra_swarm_manager/config/three_uav_inspection_formation.yaml`：状态、角色、许可和切层交接 |
| S8 | `AstraDrone_ros1_ws/src/Swarm/astra_swarm_safety/scripts/swarm_safety_node.py`；`AstraDrone_ros1_ws/src/Swarm/astra_swarm_safety/src/astra_swarm_safety/prediction.py`：机间安全检查；`AstraDrone_ros1_ws/src/Swarm/astra_swarm_msgs/msg/SwarmState.msg`、同目录 `PredictedTrajectory.msg`：共享数据合同 |
| S9 | `scripts/run_sh/three_uav/three_uav_inspection.sh`；`scripts/run_sh/three_uav/three_uav_multi_height_inspection.sh`；`scripts/tool/plot_three_uav_trajectory.py`；`scripts/tool/analyze_three_uav_orbit.py`；`AstraDrone_ros1_ws/src/Swarm/astra_swarm_bringup/scripts/swarm_evidence_recorder.py`；`AstraDrone_ros1_ws/src/Swarm/astra_swarm_bringup/config/triple_tower.rviz`：运行记录、绘图、RViz |
| S10 | `AstraDrone_ros1_ws/src/Swarm/astra_swarm_tf/scripts/frame_adapter_node.py`；`AstraDrone_ros1_ws/src/Swarm/astra_swarm_perception/src/teammate_cloud_filter_node.cpp`、同目录 `self_cloud_filter_node.cpp`：frame 适配、队友/自身点云过滤 |
| S11 | `AstraDrone_ros1_ws/src/Swarm/astra_swarm_perception/src/world_cloud_fusion_node.cpp`；`AstraDrone_ros1_ws/src/Swarm/astra_swarm_bringup/launch/dual_tower_inspection.launch`：默认关闭的双机可视化点云拼接；`AstraDrone_ros1_ws/src/Detection/yolo_detect/launch/ppe_yolo_three_uav.launch`：独立 YOLO 扩展 |
| S12 | `AstraDrone_ros1_ws/src/Swarm/astra_swarm_bringup/launch/triple_tower_multi_height_inspection.launch`：26/20/14 m profile、两层开关、禁止 Learning Speed |
| S13 | `AstraDrone_ros1_ws/src/MissionControl/astra_tower_mission/src/sector_inspection_mission_node.cpp`；`AstraDrone_ros1_ws/src/MissionControl/astra_tower_mission/config/low_altitude_inspection.yaml`；同目录 `sector_inspection.yaml`；`rule.md`：候选、进出场、绕塔、切层和返航语义 |
| S14 | `AstraDrone_ros1_ws/src/MissionControl/hector_ego_training_backend/launch/hector_forest_sac_training.launch`；同目录 `hector_worksite_sac_training.launch`、`hector_training_observation_c.launch`；`AstraDrone_ros1_ws/src/MissionControl/hector_ego_training_backend/config/worksite_training_reset.yaml`：Gazebo training-only 后端与 reset |
| S15 | `AstraDrone_ros1_ws/src/learning_speed_rl/config/sac_training_v1.yaml`；`AstraDrone_ros1_ws/src/learning_speed_rl/scripts/sac_training_runner.py`；`AstraDrone_ros1_ws/src/learning_speed_rl/src/learning_speed_rl/training/sac.py`、同目录 `sac_replay.py`、`astra_drone_env.py`：SAC、Replay、Episode 与训练/评估参数 |
| S16 | `AstraDrone_ros1_ws/src/learning_speed_rl/src/learning_speed_rl/observation/v2/lidar_surrogate.py`；`AstraDrone_ros1_ws/src/learning_speed_rl/src/learning_speed_rl/observation/scheme_c/builder.py`；`AstraDrone_ros1_ws/src/learning_speed_rl/scripts/speed_adapter_node.py`；`AstraDrone_ros1_ws/src/learning_speed_rl/src/learning_speed_rl/policy/safety_filter.py`：Observation、动作适配和范围保护 |
| S17 | `AstraDrone_ros1_ws/src/learning_speed_rl/src/learning_speed_rl/training/reward.py`；`AstraDrone_ros1_ws/src/learning_speed_rl/config/stage1_reward.yaml`：唯一 Reward 实现与当前 v3.1 参数 |
| S18 | `项目整理文档.md`：当前运行入口和记录边界；`AstraDroneOpen_项目技术演进与LearningSpeed阶段汇总.md` 第 23～29 节：Reward/SAC 与最近训练验证历史；其早期“没有 100EP”不能覆盖后文实际完成但 NO-GO 的记录 |

## 5. 验收材料汇总表

| 关键技术 | 实验场景 | 测试对象 | 主要参数 | 可展示结果 | 现有证据 | 待补充数据 |
|---|---|---|---|---|---|---|
| 2.1 多源协同感知与定位建图 | worksite 三机同时运行 | 激光—IMU 定位、局部地图、队友状态/轨迹共享 | 三套 Mid-360/IMU/D435；world 共享参考；各机独立 GridMap | 三机位姿、点云、地图和未来轨迹同场景展示，支撑任务闭环 | S1～S5、S7～S11；E1/E2 状态与轨迹链 | 已有 Gazebo/RViz 截图、视频待人工补充；定位/地图精度如需数字待人工补充，不宣称联合地图融合 |
| 2.2 低空同层协同巡检 | worksite，三机同层 | 进场放行、八扇区绕塔、避障、返航降落 | 3/3/3 m，0.60 m/s，0.50 m/s²，目标相位 67.5°，安全距离 3 m | 经多轮仿真验证；完整闭环及落地解锁；代表记录最小距离 3.840 m | E1/E3 JSON、CSV、轨迹图，E1 bag/日志；S1、S5～S9、S13 | 仅补选比赛视频/局部避障截图；无需补做成功率统计 |
| 2.2 高空同层协同巡检 | 三机共同高空高度 | 同层角色放行与高空完整任务 | 当前为参数化实现；30 m 不是默认，具体成功工况参数待关联 | 按项目方已完成的同层运行经验组织定性展示 | S1/S13 参数化代码；当前 30m 命名目录仅为未完成记录，不能代作成功证据 | 已有高空成功素材、准确高度及运行日期待人工补充关联；无需为此重做成功率统计 |
| 2.2 异层及分层协同巡检 | worksite，三机异层两圈 | 分段爬升、每层闭环、有序切层、高空返航 | 26→22/20→16/14→10 m；巡检 0.60、切层 0.30 m/s；一次一个切层执行者 | 已完成实际完整运行；六个高度环、切层与落地解锁；代表记录最小距离 3.842 m | E2 参数日志、JSON、CSV、3D 图、bag；S7/S12/S13 | 已有切层/返航视频待人工补充；无需补做成功率统计 |
| 2.3 SAC 环境自适应速度约束 | 单机 Gazebo/Hector worksite/Forest | Observation→SAC→速度约束→EGO→Replay/learner | 3267 维，1 维 action，0.30～1.75 m/s，Replay 100000，learning_starts 1000 | 已完成训练交互与约束执行链路验证；保留真实失败，总体尚未通过正式训练资格 | S14～S17；E4/E5 实际交互/更新/失败 summary | 速度曲线与演示视频待人工补充；正式收敛、独立评估、效率收益和三机策略部署尚未完成 |
| 2.3 后续迁移计划 | AirGym | 等价观测、速度动作与下游规划接口迁移 | 计划保持任务/安全边界，具体 AirGym 配置待设计 | 仅可展示后续技术路线，不计当前成果 | 本次未找到 AirGym 接入代码或运行工件 | 计划开展迁移、继续训练、独立评估和回迁验证 |
