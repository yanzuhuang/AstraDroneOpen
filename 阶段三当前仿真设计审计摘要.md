# 阶段三当前仿真设计审计摘要

更新日期：2026-07-22

## 审计范围

本摘要基于当前工作区源码、配置、launch、启动脚本、已有测试 XML 和阶段三 CSV 记录整理。审计时没有修改代码、配置或 launch，也没有启动新的仿真、解锁、起飞或 OFFBOARD 控制。

当前 Git 基线：

- 分支：`ego-project`
- HEAD：`48e35c28297317ace562c95b925180c853e23f43`
- 相对远端：领先 7
- HEAD 提交主题：`阶段三优化版本一（未跑通）`
- 工作区原有修改和未跟踪文件均保留，未被本审计覆盖或清理。

## 总体结论

阶段三已经形成任务管理器、EGO-Planner、MAVROS bridge 三层骨架，包含外侧观察点、8 个巡检扇区、有限候选目标、候选过滤/评分、HOLD、R1/R2/re-entry 和 PlannerStatus 外部适配器。

但是，当前实现还不符合“可以完整进行带控制飞行验收”的预期。主要原因是：

1. 现有控制记录只到达第一个 4 m 外侧观察点，没有进入 30 m 爬升和扇区巡检；任务仍停留在 `APPROACH`。
2. 任务层要求到达时 yaw 朝塔，而 stage3 bridge 没有启用阶段二的朝塔 yaw 策略；`traj_server` 使用轨迹前视 yaw。当前第一个观察点的前视方向约 142.2°，朝塔方向约 112.5°，相差约 29.7°，超过约 14.9° 的到达 yaw 容差。
3. PlannerStatus 是外部启发式适配器，不是 EGO FSM 的真实规划结果。它根据命令、目标、odom、点云和占据图推断失败，并可能以 20 Hz 持续增加失败计数。
4. 配置中的恢复高度为 35 m，但没有写入实际 `RecoveryConfig`；R1/R2 实际使用结构体默认高度 30 m。
5. HOLD 后的取消/返航接口存在状态缺口：取消会清除 bridge 的 `have_valid_goal_`，返航重新发布目标却没有恢复该标志，可能无法重新进入 `TRACK_EGO`。

## 当前实际执行链路

标准入口为 `scripts/run_sh/stage3_ego.sh`，启动顺序为：

```text
PX4 SITL + Gazebo + MAVROS
    → FAST-LIO laserMapping
    → EGO planner + grid map + traj_server + waypoint_generator
    → ego_mavros_bridge
    → planner_status_adapter + occupancy_stamp_adapter
    → stage3_ego_mission_node
```

实际目标链为：

```text
任务管理器
  /move_base_simple/goal
    → bridge 校验并发布 /planning/goal
      → waypoint_generator
        → EGO FSM
          → /planning/bspline
            → traj_server
              → /planning/pos_cmd
                → bridge
                  → /mavros/setpoint_raw/local
```

标准入口下，只有 bridge 创建 MAVROS raw-local setpoint publisher；任务管理器不发布 MAVROS 控制 Topic。dry-run 时 bridge 不创建 MAVROS setpoint publisher。

任务执行设计为：

```text
WAIT_INPUTS
  → bridge 起飞至 1 m
  → HOVER_READY
  → 外侧观察点水平进场，目标高度 4 m
  → 观察点原地爬升至 30 m
  → 逐扇区候选评估和 EGO 跟踪
  → 所有扇区完成
  → bridge 返航
  → AUTO.LAND
```

但现有控制记录显示实际只执行到第一个 `approach_1=(-2.78411, 2.15669, 4)`，最近位置误差约 0.015 m，之后仍在 `APPROACH`，没有进入爬升或扇区巡检。

证据：`/tmp/astra_stage3_evidence/control_20260722_231040.csv`。

## 当前三层状态机

### 任务管理器

当前状态为：

```text
WAIT_INPUTS
APPROACH
EVALUATING
TARGET_LOCKED
NAVIGATING
HOLDING
RELOCATING
RECOVERING
RETURN_HOME
FAILURE_LANDING
DONE
ERROR
```

任务管理器负责扇区政策、候选目标选择、到达判定、失败分类、HOLD/恢复/返航请求。它不直接控制 MAVROS。

### EGO FSM

EGO 原有 FSM 仍为：

```text
INIT
WAIT_TARGET
GEN_NEW_TRAJ
REPLAN_TRAJ
EXEC_TRAJ
EMERGENCY_STOP
```

阶段三只增加了可选的 per-goal 高度读取 `fsm/use_goal_height`。没有新增 PlannerStatus、取消轨迹服务或任务状态接口，也没有改 EGO 规划核心。

### bridge FSM

bridge 状态为：

```text
DRY_RUN
WAIT_FCU
WAIT_INPUTS
PRESTREAM
ARM_OFFBOARD
TAKEOFF
HOVER_READY
TRACK_EGO
HOLD
LANDING
DONE
ERROR
```

bridge 负责 preflight、home 捕获、起飞、EGO 命令转换、HOLD、返航目标发布以及降落。bridge 没有独立的 `RETURN_HOME` 状态，而是用 `return_in_progress_` 标志追踪返航。

## 当前绕塔和恢复设计

### 扇区

配置固定为 8 个单层扇区：名义半径 14 m、名义高度 30 m、起始角 -67.5°、逆时针、扇区角半宽 12°、半径半宽 4 m、高度半宽 1 m。

第一个扇区固定为 `sector 0`，不是按无人机当前位置选择最近扇区。

### 外侧观察点

观察点半径为：

```text
14 m + observation_radius_offset 5 m = 19 m
```

当前位置到观察点按 `approach_segment_length=6 m` 分段，再原地爬升到巡检高度。当前 worksite 几何中 home 到观察点约 3.52 m，所以实际只产生一个水平分段。

### 候选目标

每个扇区生成 45 个候选：

```text
角度偏移：0, -5, +5, -10, +10°
半径偏移：0, +2, +4 m
高度偏移：0, -1, +1 m
```

候选先执行地图新鲜度、边界、塔 keep-out、静态障碍、点云净空和直线走廊过滤，再按净空、当前距离、连续性和高度偏差评分。

当前实现与预期有三处差异：

- 名义候选不是优先目标，所有候选一起评分；空旷时外侧 18 m 候选可能胜出。
- 角度和半径名义偏差没有纳入完整评分，名义误差实际主要只体现为高度误差。
- `unknown_ratio` 被写死为 `0.0`，未知空间没有真正参与过滤。

### R1/R2

恢复点几何已经实现：R1 从当前位置沿塔心径向外扩 3 m、最大半径 24 m；R2 沿选定绕塔方向切向移动 4 m；re-entry 回到当前扇区名义角度和名义半径。

首选方向不安全时才检查相反方向，不是同时评估两个方向后择优。R1/R2 由任务层发布目标，EGO 负责每一段局部规划。

当前恢复高度存在实现缺陷：YAML 为 35 m，实际 R1/R2 仍为 30 m。

## 当前故障处理结论

| 故障 | 当前处理 | 主要缺口 |
|---|---|---|
| 目标点被占据 | 候选重定位；执行中报告占据后 HOLD 再 `RELOCATING` | 没有 EGO 真值接口 |
| 目标净空不足 | 候选过滤拒绝 | 直线走廊过滤可能过度保守 |
| 路径阻断 | 任务层先拒绝直线走廊；EGO 无命令超过阈值后启发式失败 | 不能确认 EGO 真实“不可达” |
| 连续重规划失败 | PlannerStatus 失败计数达到 3 后 HOLD | 计数按 20 Hz 增长，非规划尝试计数 |
| emergency stop | 根据低命令速度和无进展推断 | 不是 EGO FSM 实际状态 |
| 当前位置占据 | HOLD；要求新鲜地图和邻域自由后才允许恢复 | 无法确认时 2 s 后返航/降落 |
| 地图过期 | 选目标前处理；导航中可能忽略 `MAP_STALE` 内容 | 无统一 map/odom stale 状态 |
| 里程计过期 | bridge preflight 进入 HOLD | 任务层没有专门消费 bridge HOLD |
| 恢复次数耗尽 | 请求返航，失败则请求降落 | HOLD 后返航可能因 goal 状态缺失失败 |

## PlannerStatus 和控制服务

`astra_custom_msgs/PlannerStatus.msg` 已定义：

```text
planner_state
target_id
trajectory_id
last_plan_success
consecutive_plan_failures
goal_in_collision
current_position_in_collision
emergency_stop_active
emergency_stop_duration
failure_reason
status_timestamp
```

Topic 为 `/planner/status`，发布节点为 `planner_status_adapter`，任务管理器订阅它。主要输入为 `/planning/pos_cmd`、`/Odometry`、`/planning/goal`、`/stage3/cloud_registered_filtered` 和 `/stage3/occupancy_inflate`。

bridge 服务为：

```text
/ego_mavros_bridge/enable_tracking             std_srvs/SetBool
/ego_mavros_bridge/cancel_current_trajectory   std_srvs/Trigger
/ego_mavros_bridge/resume_ego                  std_srvs/Trigger
/ego_mavros_bridge/return_home                 std_srvs/Trigger
/ego_mavros_bridge/land                        std_srvs/Trigger
```

取消服务只清除 bridge 本地执行标志，没有取消 EGO FSM 或 traj_server 的旧轨迹。

## 当前关键参数

| 参数 | 实际值 | 结论 |
|---|---:|---|
| 正常巡检高度 | 30 m | 生效 |
| 恢复最大高度 | 40 m | 主要用于校验，不是实际恢复高度 |
| EGO 虚拟上限 | 45 m | EGO 地图和任务校验生效 |
| 观察点半径 | 19 m | 14+5 m |
| 扇区数量 | 8 | 代码强制为 8 |
| 候选角偏移 | 0, ±5, ±10° | 生效 |
| 候选半径偏移 | 0, +2, +4 m | 只有向外偏移 |
| 候选高度偏移 | 0, ±1 m | 生效 |
| 连续失败阈值 | 3 | 生效，但适配器按周期累计 |
| emergency 超时 | 6 s | 任务和适配器使用 |
| 最大恢复次数 | 2 | 每扇区 |
| 恢复目标超时 | 60 s | 每个 R1/R2/re-entry |
| 任务总超时 | 2400 s | 生效 |
| bridge 起飞高度 | 1 m | 与阶段二 4 m 不同 |
| bridge 返航高度 | 1 m | 与阶段二 4 m 不同 |
| bridge command timeout | 0.25 s | 比阶段二 0.60 s 更严格 |
| bridge 自动降落等待 | 8 s | HOLD 超时转 AUTO.LAND |
| bridge 朝塔 yaw | 未启用 | 当前首个观察点到达风险 |

## 阶段三预期对照

| 设计项 | 当前判断 |
|---|---|
| FAST-LIO/EGO 持续运行 | 已实现，但完整任务未飞行验证 |
| bridge 唯一控制出口 | 已实现，失败返航状态仍有缺口 |
| 外侧观察点 | 已实现，当前只产生一个水平分段 |
| 8 个巡检扇区 | 已实现，未完成实际巡检验证 |
| 候选目标重定位 | 部分实现，名义目标优先级偏离预期 |
| HOLD/R1/R2/re-entry | 部分实现，恢复高度和集成返航未闭合 |
| 三个独立 FSM | 架构已实现 |
| EGO 只负责局部规划 | 基本实现 |
| 机器可读 PlannerStatus | 外部适配已实现，但不是 EGO 真值 |
| 30/40/45 高度分层 | 部分实现，恢复高度未生效 |
| 当前占据先 HOLD | 部分实现，诊断和后续策略不完整 |
| 不接入 YOLO/动态预测/多机/真机 | 当前 launch 未接入 |

## 当前能测试与不能测试的功能

### 可以在无控制条件下测试

- 消息生成和构建；
- 候选硬过滤、走廊检查、评分和迟滞；
- R1/R2 几何；
- PlannerStatus 话题和故障注入；
- 点云、TF、占据图和时间戳适配；
- dry-run bridge preflight 和控制权唯一性检查。

已有 stage3 planner 测试 XML 显示 6/6 通过；EGO goal height 测试为 3/3 通过。没有任务 FSM、PlannerStatus adapter 或 cancel/return/resume 集成测试。

### 暂时不能进行带控制飞行的功能

- 完整 8 扇区巡检；
- 30 m 观察点爬升后的绕塔；
- 目标占据后的重定位飞行；
- EGO 失败后的 R1/R2 恢复；
- HOLD 后返航；
- 当前点占据、地图过期和 emergency stop 的自动安全闭环；
- 完整返航和降落验收。

## 最小后续修正建议

1. 先统一 yaw 契约：要么启用阶段二 bridge 的朝塔 yaw，要么移除任务层当前无法由执行链满足的 yaw 到达硬条件。
2. 将 `recovery_height_` 明确写入 `recovery_config_.recovery_height`，并增加 30/35/40/45 m 高度单测。
3. 修复 `cancel_current_trajectory → return_home → fullPreflight` 的 `have_valid_goal_` 状态流。
4. 将 EGO FSM 的规划状态和失败事件通过明确接口导出，停止以 20 Hz 周期推断连续失败。
5. 增加任务 FSM 无控制集成测试，然后按“单观察点 → 单扇区 → 目标占据 → R1/R2 → 多扇区”顺序重新做带控制验证。

## 主要证据索引

- 任务状态机：`AstraDrone_ros1_ws/src/MissionControl/astra_tower_mission/src/stage3_ego_mission_node.cpp`
- 扇区/候选/R1/R2：`AstraDrone_ros1_ws/src/MissionControl/astra_tower_mission/src/stage3_planner.cpp`
- PlannerStatus：`AstraDrone_ros1_ws/src/MissionControl/astra_tower_mission/src/planner_status_adapter_node.cpp`
- bridge FSM 和服务：`AstraDrone_ros1_ws/src/MissionControl/ego_gazebo_bridge/src/ego_mavros_bridge.cpp`
- 阶段三任务参数：`AstraDrone_ros1_ws/src/MissionControl/astra_tower_mission/config/stage3_ego.yaml`
- 阶段三 bridge 参数：`AstraDrone_ros1_ws/src/MissionControl/ego_gazebo_bridge/config/stage3_ego.yaml`
- 阶段三 launch：`AstraDrone_ros1_ws/src/MissionControl/astra_tower_mission/launch/stage3_ego.launch`
- 完整启动脚本：`scripts/run_sh/stage3_ego.sh`
- 现有控制记录：`/tmp/astra_stage3_evidence/control_20260722_231040.csv`
