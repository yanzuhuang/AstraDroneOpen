# 临时完成情况审计报告

审计日期：2026-08-02  
审计范围：当前 `ego-swarm` 分支、`/home/yanzu/AstraDroneOpen` 工作区、2026-08-02 三机启动日志。  
审计方式：只读检查源码、YAML、launch、Git 状态/历史和已有日志；本轮未修改代码、未改参数、未提交或清理文件，未运行完整长时间绕塔仿真。

## 结论摘要

1. 扇区自适应候选已经写回当前工作区：角度 `[0,-5,+5,-10,+10,-12,+12]°`、半径 `[0,+2,+4] m`，第一巡塔点通过普通扇区候选进入动态 ENTRY 联合选择；低空高度仍为 `[0]`、3.0 m。
2. 已膨胀 occupied map 的任务层附加净空已经改为 `0.5 m`。代码不再把膨胀地图统一按 `1.0 m` 硬拒绝；`1.0 m` 仍用于未膨胀原始点云/粗几何。
3. EGO 不可达后的同候选有限重试和候选切换代码存在，`planner_unreachable_attempt_limit=2`。但本次三机运行停在启动安全门，未产生当前规则恢复后的候选选择、EGO 重试或换候选飞行证据。
4. 三机角色、22.5°名义集结、65–70°依次放行和 67.5°正式相位差仍在代码/YAML中；本次没有实飞验证这些阶段。
5. `rule.md` 已创建，语义与当前 stage3 YAML、stage5 YAML 和候选代码一致；它是设计规范，不是运行时解析文件。
6. UAV2 的 `spawn_model` 确实以退出码 1 结束，但日志显示“已推入 spawn queue，等待实体出现超时”；随后 Gazebo 加载了 UAV2 的传感器插件并注册了 UAV2 话题。因此更准确的结论是：spawn 客户端等待超时竞态，不能据此认定模型生成失败；本次没有保存 `/gazebo/model_states` 消息样本，故不能仅凭日志证明整个运行期间三机模型状态持续存在。
7. 当前 `WAIT_INPUTS` 不是三架 MAVROS 都断心跳。日志中 UAV1/UAV2/UAV3 均出现 `CON: Got HEARTBEAT, connected. FCU: PX4 Autopilot`，桥接均从 `WAIT_FCU` 进入 `WAIT_INPUTS`。安全节点首先对 UAV1 报 `HEARTBEAT_OR_LOCALIZATION_TIMEOUT`，而状态发布器在收到 `/uavN/Odometry` 前不发布 `/uavN/swarm/state`。聚合错误码不能单独区分心跳和定位；现有证据更支持“UAV1 的 SwarmState/FAST-LIO 定位健康门未建立或未及时建立”，而不是 FCU 心跳连接失败。

## 1. 工作区与 Git 基线

| 项目 | 只读结果 |
|---|---|
| 分支 | `ego-swarm` |
| HEAD | `1275d3645805f5c5bca59204264b99dad571dd63`（`初步修复三机绕塔`） |
| 分支状态 | `origin/ego-swarm [领先 8]` |
| 工作区 | 有大量既有修改、未跟踪 `rule.md`、stage5 YAML、测试证据及历史报告；本轮未触碰这些文件 |
| 历史参照 | `9ce4670 complete ego-swarm three-uav safety integration` 等提交存在，但当前结论以当前工作区源码和本次日志为准 |

## 2. 本轮规则恢复的实际代码状态

| 审计项 | 当前实现与精确位置 | 状态/证据 |
|---|---|---|
| 角度候选 | `AstraDrone_ros1_ws/src/MissionControl/astra_tower_mission/config/low_altitude_inspection.yaml:143-148` | 已恢复 `[0,-5,+5,-10,+10,-12,+12]°`；普通扇区和第一点共用候选网格 |
| 半径候选 | 同上 `:147`；`rule.md:60-69` | 已恢复 `[0,+2,+4] m`，名义 12.5 m，候选 12.5/14.5/16.5 m |
| 低空高度 | YAML `:148`、`rule.md:71-78` | 当前三机 `[0]`，高度 3.0 m；没有启用 2/1/0 m |
| 第一巡塔点 | `sector_inspection_mission_node.cpp:1784-1827`、`:2577-2662` | `buildEntryCorridorCandidates()` 遍历 `first_sector.candidates`；`lockDirectionalInitialSector()` 对全部候选评估并优选，日志含角度、距离、净空和 candidate ID。不是固定 12.5 m 单点路径 |
| 膨胀 map 净空 | YAML `:90-99`、`:149-152`；C++ `sector_inspection_mission_node.cpp:1618-1644` | `map_points_are_inflated=true` 时 endpoint/corridor 使用 `map_additional_clearance=0.5`；未膨胀时使用 `minimum_clearance + cloud_inflation` |
| 粗几何/塔体 | YAML `:161-164`；C++ `:1815-1820`、`:2617-2622` | 已知障碍端点和塔体 keep-out 仍是硬检查；直线路径风险不等于自动拒绝，交给走廊/EGO |
| 候选评分 | `sector_inspection_mission_node.cpp:2624` 的 `chooseBestCandidate()` 调用；权重 YAML `:155-160` | 先过硬约束，再综合净空、半径/扇区/高度/距离/连续性；没有“找到第一个可用点立即使用”的实现证据 |
| EGO 失败重试 | C++ `:320-323`、`:5049-5063` | `planner_unreachable_attempt_limit=2`；同一 candidate 先重试，达到上限后加入 `planner_unreachable_candidates_`，清除锁并进入 `kRelocating`，再换同扇区候选 |
| 静态扇区绕行 | C++ `:2881-2929` | 静态 detour 半径偏移仅 `[0,2,4]`，没有恢复旧的向内半径偏移 |
| 三机联合选择 | `AstraDrone_ros1_ws/src/Swarm/astra_swarm_manager/config/three_uav_inspection_formation.yaml:1-35`、`scripts/swarm_manager_node.py:46-71,463`、`src/astra_swarm_manager/policy.py` | UAV3 leader→UAV2 middle→UAV1 trailing；名义角 `[292.5,315,337.5]`（按 YAML UAV1/UAV2/UAV3 顺序）；目标相位 67.5°；map margin 0.5 m；三机高度 3.0 m |
| `rule.md` | 项目根目录 `rule.md:1-252` | 已创建；明确 YAML 是运行参数来源、Markdown 不被程序解析、规则修改需同步 YAML/代码/测试/报告。候选、净空、retry、第一点和三机阶段均有对应条款 |

已有构建/测试证据（此前工作区操作留下，非本次重跑）：目标包构建通过，`catkin_test_results` 为 `292 tests, 0 errors, 0 failures, 0 skipped`；Python swarm policy 测试为 `35 passed`。这些是实现证据，不是本次完整三机飞行证据。

## 3. UAV2 spawn_model 是否真的失败

本次日志目录：`/home/yanzu/.ros/log/d645b022-8dcc-11f1-a949-bbf6f1d93fcc`。

| 机体 | spawn 客户端日志 | roslaunch 结果 | Gazebo/话题旁证 | 判定 |
|---|---|---|---|---|
| UAV1 | `uav1-vehicle_spawn...-4.log:18`：`Successfully spawned entity` | 无对应失败 | 有 UAV1 传感器插件 | 客户端成功 |
| UAV2 | `uav2-vehicle_spawn...-7.log:18`：`Entity pushed to spawn queue, but spawn service timed out waiting for entity to appear ... iris_mid360_11` | `roslaunch-yanzu...-307711.log:1185`，exit code 1 | `rosout.log:404-417` 随后出现 `/uav2/livox/`、`uav2/mid360_link`、200 Hz/100 Hz IMU 插件加载；`master.log:2376,2572,2601,2647,3867` 注册 UAV2 FAST-LIO、`/uav2/Odometry`、`/uav2/mavros/state` | 不是“可证明的模型生成失败”；是 spawn 客户端等待超时，实体已进入队列并有强旁证实际加载 |
| UAV3 | `uav3-vehicle_spawn...-10.log:18`：`Successfully spawned entity` | 无对应失败 | 有 UAV3 传感器插件 | 客户端成功 |

Gazebo 节点确实发布 `/gazebo/model_states`（`master.log:3471`），但本次未录制该消息内容，因此不能在审计中声称已从消息字段持续确认 `iris_mid360_11`、UAV1、UAV3 全程存在。建议下一次只做短时预检并记录 `model_states.name[]`，先解决 spawn 等待竞态再启动安全门。

## 4. WAIT_INPUTS 与三机健康判断

### 4.1 桥接节点实际检查

`AstraDrone_ros1_ws/src/MissionControl/ego_gazebo_bridge/src/ego_mavros_bridge.cpp:1584-1619` 的 `baseInputsFresh()` 按顺序检查：

1. `/uavN/mavros/state`：`connected` 且 `fcu_state_timeout` 内；
2. `/uavN/mavros/extended_state`：新鲜；
3. `/uavN/mavros/local_position/pose`：消息接收时间和 `header.stamp` 均新鲜；
4. `/uavN/Odometry`：规划里程计接收时间和消息时间戳新鲜；
5. `/uavN/cloud_registered`（本次 stage5 还接入 filtered cloud）：点云接收时间和消息时间戳新鲜。

通用默认值位于 `ego_gazebo_bridge/config/ego_gazebo_bridge.yaml:24-35,59-68`：
`fcu_state_timeout=1.0 s`、`extended_state_timeout=1.0 s`、`mavros_pose_timeout=0.2 s`、`planner_odom_timeout=0.2 s`、`cloud_timeout=0.5 s`、`wait_inputs_timeout=120 s`；话题为 `/mavros/state`、`/mavros/extended_state`、`/mavros/local_position/pose`、`/Odometry`、`/cloud_registered`。

本次三机 launch 的有效参数在 `/tmp/stage5_real_run4.log:76,197-224,439,793-820,1035,1389-1416,1631`：三机均使用 `fcu_state_timeout=2.0 s`、`mavros_pose_timeout=1.5 s`、`planner_odom_timeout=0.7 s`、`cloud_timeout=0.7 s`；`require_start_permission=true`。桥接日志随后反复输出 `WAIT_INPUTS ... waiting for swarm takeoff permission`（`/tmp/stage5_real_run4.log:3243-3274` 等），没有进入 PRESTREAM/起飞。

### 4.2 “三机心跳”的真实定义与本次证据

状态发布器 `AstraDrone_ros1_ws/src/Swarm/astra_swarm_manager/scripts/swarm_state_publisher.py`：

- 订阅 `/uavN/Odometry`（:48-49）和 `/uavN/mavros/state`（:56-60）；
- `/uavN/mavros/state` 使用消息字段 `mavros_msgs/State.connected`（:89-90）；
- 只有收到一次 `/uavN/Odometry` 后才发布 `swarm/state`（:92-94）；
- 定时器 0.1 s，即 10 Hz（:64）；`heartbeat_timeout=1.0 s`（:29）；
- `fresh = now-last_odom_wall <= 1.0 s`（:95-96）；最终 `heartbeat_ok=fresh && fcu_connected`、`localization_valid=fresh`（:118-119）。

安全节点 `AstraDrone_ros1_ws/src/Swarm/astra_swarm_safety/scripts/swarm_safety_node.py:137-149` 订阅 `/uav1|2|3/swarm/state`，以 `heartbeat_timeout=1.0 s`（:29）检查是否存在、是否新鲜以及两个布尔字段；循环遇到第一架不健康机体立即 `return`，所以一次聚合日志不能代表三架均被独立诊断。

| UAV | 心跳健康话题/字段 | 本次日志 | 定位健康话题/字段 | 本次最后更新时间/频率 |
|---|---|---|---|---|
| UAV1 | `/uav1/mavros/state`, `connected` | `rosout.log:471`：`Got HEARTBEAT, connected`；桥接 `:473` 进入 `WAIT_INPUTS` | `/uav1/mavros/local_position/pose`；状态发布器实际使用 `/uav1/Odometry`，其 `fresh` 决定 `localization_valid` | 未在 rosout/roslaunch 日志中记录消息计数、实际最后 header.stamp 或 `rostopic hz`；仅能确认订阅/发布器注册 |
| UAV2 | `/uav2/mavros/state`, `connected` | `rosout.log:466`；桥接 `:469` 进入 `WAIT_INPUTS` | `/uav2/mavros/local_position/pose`；`/uav2/Odometry` | 同上，未记录实际最后更新时间/频率 |
| UAV3 | `/uav3/mavros/state`, `connected` | `rosout.log:476`；桥接 `:478` 进入 `WAIT_INPUTS` | `/uav3/mavros/local_position/pose`；`/uav3/Odometry` | 同上，未记录实际最后更新时间/频率 |

FAST-LIO 链路的可见注册证据为：每机 `/uavN/fast_lio/Odometry_raw`、`/uavN/fast_lio/cloud_registered_raw` 由 `laserMapping` 发布，frame adapter 再发布 `/uavN/Odometry`、`/uavN/cloud_registered`；UAV2 的注册行见 `master.log:2376,2572,2601,2647`，三机同构。注册不等于收到有效消息，当前日志没有消息频率或最后时戳。

### 4.3 WAIT_INPUTS 的根因判定

最早安全事件为 `/home/yanzu/.ros/log/d645.../rosout.log:418`（仿真时间 11934.111）：

`code=HEARTBEAT_OR_LOCALIZATION_TIMEOUT uav=1 ... mission_state=MISSING; flight_state=MISSING ... action=SAFETY_INHIBIT_HOLD`。

之后相同事件反复只报告 `uav=1`。这是因为安全节点在 `swarm_safety_node.py:139-149` 遇到第一架失败即返回，并没有继续给 UAV2/UAV3 生成独立错误。桥接的 `WAIT_INPUTS` 日志（如 `/tmp/stage5_real_run4.log:3243`）说明其 FCU 已连接但在等 swarm takeoff permission；不是日志所证明的 MAVROS 连接断开。

能确定的最小结论：

- **已排除为“所有 UAV MAVROS 心跳未连接”**：三架都有 `Got HEARTBEAT, connected`。
- **已确定触发统一安全门的是 UAV1**：首次事件中的 `uav=1`。
- **无法仅凭当前日志把聚合码进一步拆成 heartbeat 超时或 localization 超时**：安全码故意合并两者，且没有录制 `/uavN/swarm/state` 消息字段/时间戳。
- **最有支持的技术解释是 UAV1 的 `/uav1/Odometry` 尚未被状态发布器及时接收，导致没有可用 `SwarmState` 或 `localization_valid=false`**；状态发布器在 `odom is None` 时直接不发布（:92-94）。这仍需短时 topic/rosbag 证据确认，不能写成已经证明的 FAST-LIO 算法故障。

## 5. 是否具备再次完整试飞条件

| 类别 | 当前结论 | 证据/缺口 |
|---|---|---|
| 已完成 | 候选角度/半径恢复；第一点共用候选；膨胀 map 0.5 m 语义；EGO retry/same-sector relocation；`rule.md`；三机角色/相位配置 | 见第2节文件和行号；已有 292/292 catkin、35/35 policy 测试证据 |
| 已实现但未实飞验证 | 0.945260 m 膨胀地图候选进入评分/EGO；第一点临时 14.5/16.5 m；EGO 失败后切换 candidate；ENTRY 联合选择、依次放行、完整一圈/EXIT/返航/降落 | 本次运行未越过安全门，没有 `STAGE5_ENTRY` 选点或 candidate-switch 记录 |
| 未完成证据 | 三机 `/gazebo/model_states` 持续包含三个实体；每机 heartbeat/localization/FAST-LIO 实际频率、最后时戳；规则恢复后的完整闭环结果 | 当前日志只有 ROS master 注册和一次 heartbeat 连接日志，无消息样本/频率统计 |
| 当前阻塞项 | `require_start_permission=true` 下 swarm safety 未 clear；首个失败为 UAV1 `SwarmState` 健康门；UAV2 spawn 客户端等待超时造成启动竞态 | `rosout.log:418`、`/tmp/stage5_real_run4.log:3243`、UAV2 spawn 日志 |
| 下一步最小操作（本报告不执行） | 先做短时、不开控制的预检：等待 Gazebo spawn 完成后记录 `/gazebo/model_states`；分别记录 `/uavN/mavros/state`、`/uavN/mavros/local_position/pose`、`/uavN/fast_lio/Odometry_raw`、`/uavN/fast_lio/cloud_registered_raw`、`/uavN/Odometry`、`/uavN/cloud_registered`、`/uavN/swarm/state`、`/swarm/safety/event` 的 `rostopic hz`、header.stamp 和字段；确认三机 SwarmState 连续超过 1 s 后再考虑启用任务 | 只需修复/明确启动等待顺序和诊断采集，不应先修改净空或关闭安全门 |

## 6. 历史结果隔离说明

`test_evidence/stage5_dynamic_entry_20260801_attempt4/roslaunch.log` 等 2026-08-01 旧记录属于规则恢复前版本，其中出现的 `hard_clearance=1.00` 不能作为本轮规则恢复后的结果。本报告只把 `/home/yanzu/.ros/log/d645b022-8dcc-11f1-a949-bbf6f1d93fcc`（2026-08-02）作为本轮启动证据；该次运行停在安全/等待输入阶段，**不宣称三机任务成功**，也不把旧的第四次实飞结果移植到本轮。

## 审计结论

当前代码和配置已经具备恢复后的扇区内自适应设计，但本轮尚未具备“再次完整试飞已被证明安全”的证据条件。应先解决 UAV2 spawn 等待竞态并补齐三机逐项 heartbeat/localization/FAST-LIO 的短时消息证据；在安全门连续清晰后，才可进行受控的分阶段试飞。任何仅凭 spawn 客户端 exit 1 或聚合 `HEARTBEAT_OR_LOCALIZATION_TIMEOUT` 推断的单一根因，都不足以替代上述逐话题诊断。
