# Reviewed inference models only

Do not write training checkpoints here. Training output belongs under
`runtime_artifacts/learning_speed/checkpoints/`. A model may be copied into
this directory only after evaluation, interface-version validation and a
separate flight-authorization review. No model is selected in the first mock
integration.
