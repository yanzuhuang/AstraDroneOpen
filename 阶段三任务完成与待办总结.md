# 阶段三任务完成与待办总结

更新时间：2026-07-24（Asia/Shanghai）
当前分支：`ego-project`
本轮开始 HEAD：`1f9552a06e86f88f66271186478e25ede01d6ede`
提交状态：未 commit、未 push

本文是阶段三当前状态的简明总表。启动、参数、历史故障和详细 bag 时间线见
[`ego-stage3学习.md`](./ego-stage3学习.md)。

## 0. 当前两层验收结论

阶段三默认任务现为：home XY `10→18→26 m` 直升，固定 sector 3
ENTRY_GATE（R=15 m），26 m 层 `3→4→5→6→7→0→1→2→3` 闭环，
sector 3 同 XY `26→24→22 m` 下降，22 m 层同序闭环，再经塔外高空弧到
HOME 径向门、HOME_HOVER 和 AUTO.LAND。巡检名义半径为 12.5 m；障碍候选
只能外移，未增加动态避障、未改速度或仿真倍率。

最终实飞 bag：
`AstraDrone_ros1_ws/log/stage3_two_layer/attempt8.bag`；分析：
`attempt8_analysis.json`，结果 `passed=true`。两层闭环、固定入口、实际
竖直爬升、26→24→22 m 同 XY 下降、朝塔 yaw、塔外 HOME 径向退出、唯一
raw-local 控制出口、无持续跟踪超限、AUTO.LAND、解锁和接地全部通过。
关键值：历时 1709.341 s，直升/层间下降最大 XY 偏差 0.357/0.200 m，
yaw 最大/P95 误差 0.0214/0.0117 rad，跟踪误差最大/P95
0.731/0.207 m，采样最小占据净空 0.864 m，最终落点距 HOME 约 0.309 m。
三包最终回归为 `166 tests, 0 errors, 0 failures, 0 skipped`。

## 1. 单层历史结论（已由第 0 节两层任务取代）

2026-07-24 单层新进场、HOLD 和返航方案已经完成代码、构建、故障注入与 8 扇区
Gazebo/PX4 SITL 控制验收。最终流程是：

```text
home hover → 锁定 home-local XY → 3 m 分段直升到 30 m
→ 安全高度驻留建图 → 只锁定一个 ENTRY_GATE → 水平转场
→ 30 m 名义点绝对优先/严格字典序备用点
→ waypoint1→…→waypoint8→waypoint1
→ NORMAL_RETURN 反向复用成功进场目标
→ HOME_HOVER → PX4 AUTO.LAND
```

原问题的 bag 根因是低空把远端 ENTRY_GATE 的 XY 当等待点，导致多个目标抖动、
树旁低净空和 EGO 失败累积；进入 HOLD 后，入场前 RETURN_EGRESS 不可用、land
只接受 HOME_HOVER、bridge 又抑制 supervised HOLD 的自动降落，形成无法退出
的闭环。最终任务区分目标占据、路径阻挡、地图未知/过期和 EGO 不可达，并为
ENTRY_GATE、导航、HOLD、恢复、正常返航、RETURN_EGRESS 和落地设置独立超时、
无进展检测与重试上限。HOLD 有界恢复失败后先正常返航，再 RETURN_EGRESS，
最后可在严格门禁下 AUTO.LAND，禁止无限停留。

构建和测试为 `160 tests, 0 errors, 0 failures, 0 skipped`。最新成功 bag
`/home/yanzu/bag/astra_stage3_yaw_sectorfix_2026-07-24.bag`
离线分析 `passed=true`：固定 XY 分段上升的 yaw 最大偏差 0.035 deg、XY 最大
漂移 0.071 m；ENTRY_GATE 只锁定一次；扇区目标为
`s1_c3(38)→s2_c50(31)→s3_c0…s7_c0(30)→s0_c3(38)→s1_c3(38)`，
完整闭环；planner 无连续失败，最小采样净空 2.172 m，跟踪误差 max
0.643 m、P95 0.128 m；正常返航优先并完成 HOME_HOVER/AUTO.LAND，最终解锁
接地。

范围仍仅限单机 Gazebo/PX4 SITL 静态障碍，不扩展动态避障、多机或真机。

### 1.1 2026-07-24 bag 证据

| 运行 | 结果 |
|---|---|
| `/tmp/astra_stage3_repro_20260724_0220.bag` | 环境重启前分析：386.829 s、535,885 条消息；低空 gate XY 切换、树旁最小净空 0.087 m、current collision 117、no feasible 79、最大连续失败 741，并复现无限 HOLD。该 `/tmp` 文件后因执行环境重启丢失，不作为交付件。 |
| `/home/yanzu/bag/astra_stage3_vertical_entry_final_2026-07-24.bag` | 263.121 s 持久化失败证据；`ENTRY_GATE_a23` 首段距实际占据 4.242 m，却被保守 OBB 2 m 膨胀边界误判 0.038 m。任务未巡检，但自动 NORMAL_RETURN→HOME_HOVER→AUTO.LAND→DONE，证明无限 HOLD 已修复。 |
| `/home/yanzu/bag/astra_stage3_vertical_entry_final_retry1_2026-07-24.bag` | 最终完整成功；同一 `a23`/同一首段记录为静态走廊软风险并由 EGO 成功规划；8 扇区加首点闭环、正常返航和落地全部通过。SHA-256 `cfc5c451749edd7a1131ee5844ec7319e7074bf2e879616a42e8230c56305dd5`。 |

详细根因、参数、状态机、复飞时间线和限制见
[`ego-stage3学习.md`](./ego-stage3学习.md) 第 0 节。

## 2. 2026-07-23 历史分级控制飞行证据

### 2.1 2 扇区

第一次飞行：

- bag：`/home/yanzu/bag/astra_stage3_two_sector_2026-07-23.bag`
- CSV：`/home/yanzu/bag/astra_stage3_two_sector_2026-07-23.csv`
- SHA-256：`872b5015de9986ce3a9e0ec5670b8c7bc90911c080737c2c128a8e1e3a32f93f`
- 结果：两个扇区已完成，EGO 返航正常；任务把返航与落地共用一个
  `goal_timeout=180 s`，导致进入正常 AUTO.LAND 约 5.4 s 后误报
  `FAILURE_LANDING`。PX4 最终仍安全接地。

修复：

- 拆分 `return_timeout=300 s` 和 `landing_timeout=90 s`；
- 用 bridge 状态进入时刻计时，不再被周期状态消息持续刷新；
- 增加独立超时判定单元测试。

成功复测：

- bag：`/home/yanzu/bag/astra_stage3_two_sector_success_2026-07-23.bag`
- CSV：`/home/yanzu/bag/astra_stage3_two_sector_success_2026-07-23.csv`
- 分析：
  `/home/yanzu/bag/astra_stage3_two_sector_success_2026-07-23.analysis.json`
- SHA-256：`3ba225d9237500ec3fa03b1d616ffc91337d486637b5c5e02e21e87a0524c56d`
- 范围：489.343 s，694,889 条消息
- 扇区：`[2,3]`，无重复
- 跟踪误差：max 0.248 m，mean 0.047 m，P95 0.076 m
- 终态：`armed=false`、`ON_GROUND`、bridge/task `DONE`

### 2.2 4 扇区

4 扇区不是第一次即通过。以下失败 bag 均保留，不能写成成功：

| 轮次 | bag / SHA-256 | 直接结果与根因 |
|---|---|---|
| attempt 1 | `astra_stage3_four_sector_2026-07-23.bag` / `d69d8f2557a64a68bac00bcc215be32c1cb691a185ab94300da8a94f8795077f` | 扇区 `[2,3,4,5]` 唯一，但 sector 5 到 home 的直线返航弦穿越塔 keep-out；出现 1,213 次 current-position collision、461 次 emergency，最后在塔旁落地。任务还错误地仅凭 bridge `DONE` 判定成功。 |
| attempt 2 | `astra_stage3_four_sector_success_2026-07-23.bag` / `0aefe7ba431502cfbd36eab6ae22f29aab6e0210e1ee281829b4f2e8f119a63e` | 文件名保留历史命名，但飞行失败。瞬时 FCU stale 后 R1/R2/re-entry 成功；38 m 返航时 planner/MAVROS 对齐误差 0.287–0.313 m 超过旧 0.25 m 门限，安全在塔外落地。 |
| attempt 3 | `astra_stage3_four_sector_attempt3_2026-07-23.bag` / `663b4f50a9cbdeef0ec8997ee73ed784656463d77a77f19a9e5c5ec72b8c5e05` | RETURN_EGRESS 已到径向子目标，但先处理 `TRAJECTORY_EXPIRED`，重建后重复发布近乎相同目标；EGO 正确返回 `Close to goal`，重试耗尽后塔外落地。 |
| attempt 4 | `astra_stage3_four_sector_attempt4_2026-07-23.bag` / `36aff2f4100c8b7ccbd7ccaea8d4c88bf2ce52386652b747e60c66c3d2d99e37` | 新返航状态转换已生效，多段圆弧成功；后续单次位置对齐 0.401 m 略超 0.40 m，重试耗尽后塔外落地。 |

对应修复：

1. 新增 `RETURN_EGRESS`，先径向外移到 24 m、升至 38 m，再选择避开塔和吊机
   的顺/逆时针圆弧，最后到 home 上空；之后才由 bridge/EGO 沿 home 垂直下降。
2. bridge `DONE` 只有在落地点距捕获 home 不超过 1.5 m 时才允许任务 `DONE`。
3. RETURN_EGRESS 每段先判断位置到达，再处理轨迹过期；重建时跳过小于 0.5 m
   的重复径向目标。
4. 对被吊机 OBB 覆盖的 sector 1 增加 `+8 m` 高度候选，并把扇区高度半宽扩为
   8 m；保留原扇区 ID。
5. Stage 3 专用 planner/MAVROS 位置对齐门限设为 0.75 m；通用 bridge 仍为
   0.25 m。该值低于独立的 1.0 m/1.0 s 持续跟踪保护和任务 2.0 m 端点净空。
6. RETURN_EGRESS 最大重试次数增至 4。

最终成功：

- bag：`/home/yanzu/bag/astra_stage3_four_sector_attempt5_2026-07-23.bag`
- CSV：`/home/yanzu/bag/astra_stage3_four_sector_attempt5_2026-07-23.csv`
- 分析：
  `/home/yanzu/bag/astra_stage3_four_sector_attempt5_2026-07-23.analysis.json`
- SHA-256：`8c21304b5e7ec162d480efe2456e974a2a0cebe087dbff4133edd551d6c75fac`
- 范围：`10614.805–11538.345`，923.540 s，1,319,838 条消息
- 扇区：`[1,2,3,4]`，无重复
- 状态：`RETURN_EGRESS 10972.169`、`RETURN_HOME 11352.022`、
  bridge `DONE 11520.934`、task `DONE 11520.969`
- PlannerStatus：无 collision/emergency，最大连续失败 0
- 跟踪误差：max 1.086 m，P95 0.441 m；超过 1.0 m 的最长连续时间
  0.779 s，小于 1.0 s 保护门限，没有触发 HOLD
- 终点：约 `(0.144,0.163,-0.019)`，`armed=false`、`ON_GROUND`

该轮停止时旧脚本在 recorder 写索引前强杀 tmux，留下 `.bag.active`。数据通过
`rosbag reindex` 完整恢复；恢复后的 bag 含 4,885 个 LZ4 chunk。未索引的原始
备份仍保留为同目录下 `.bag.orig.active`，未删除。

### 2.3 最终 8 扇区

- bag：`/home/yanzu/bag/astra_stage3_eight_sector_2026-07-23.bag`
- CSV：`/home/yanzu/bag/astra_stage3_eight_sector_2026-07-23.csv`
- 分析：
  `/home/yanzu/bag/astra_stage3_eight_sector_2026-07-23.analysis.json`
- SHA-256：`08f973610f9f0071bd0c228c401df06641ba0dc589556da3f3bb5437790e80d6`
- 范围：`10614.800–11539.550`，924.750 s，1,320,782 条消息，
  4,873 个 LZ4 chunk

完整飞行时间线：

| 仿真时间 | 事件 |
|---:|---|
| 10618.486–10633.041 | `WAIT_INPUTS → PRESTREAM → ARM_OFFBOARD → TAKEOFF → HOVER_READY` |
| 10633.070 | 选择 `ENTRY_GATE_a23`，进入三维分段 `APPROACH` |
| 10752.680–10790.969 | `s1_c47`，sector 1 在 38 m 完成 |
| 10791.020–10854.569 | `s2_c39`，sector 2 完成 |
| 10854.620–10918.969 | `s3_c0`，sector 3 完成 |
| 10919.025–10973.770 | `s4_c0`，sector 4 完成 |
| 10973.822–11019.769 | `s5_c0`，sector 5 完成 |
| 11019.822–11075.170 | `s6_c0`，sector 6 完成 |
| 11075.221–11121.320 | `s7_c0`，sector 7 完成 |
| 11121.370–11174.969 | `s0_c39`，sector 0 完成；8 个扇区无重复 |
| 11174.969–11350.022 | `RETURN_EGRESS`：38 m 塔外侧分段返航到 home 上空 |
| 11350.022–11497.641 | `RETURN_HOME`：bridge 经 EGO 沿 home 垂直走廊下降 |
| 11497.662 | 到达 home hover，`TRACK_EGO → LANDING`，PX4 接受 `AUTO.LAND` |
| 11516.943–11516.969 | PX4 报告解锁且接地；bridge/task 依次进入 `DONE` |

最终 bag 审计：

| 项目 | 结果 |
|---|---|
| 必需 topic | 全部存在 |
| 扇区 ID | `[1,2,3,4,5,6,7,0]`，无重复 |
| PlannerStatus | 18,403 条全部 `failure_reason=NONE` |
| collision / emergency | 全程 false |
| 最大连续规划失败 | 0 |
| `/planning/cancel` | 0 |
| raw `type_mask` | 44,900 条全部为 0 |
| MAVROS 控制出口 | 唯一 `/ego_mavros_bridge` |
| 跟踪误差 | max 0.597 m，mean 0.073 m，P95 0.195 m；无超 1.0 m 样本 |
| MAVROS 速度 | max 0.774 m/s，P95 0.325 m/s |
| 膨胀占据采样净空 | 最小 3.247 m（每 10 帧采样的体素中心距离） |
| frame | odom/cloud/occupancy/goal/pos_cmd 均为 `camera_init` |
| 最终位置 | `(0.045,-0.039,-0.008)` |
| 最终飞控/任务 | `armed=false`、`landed_state=ON_GROUND`、bridge/task `DONE` |

## 3. 代码修改

### 3.0 2026-07-24 收尾

- 每扇区唯一 30 m 名义点；硬约束后严格按小角度、小半径、29～31 m、小幅
  水平调整、38 m 保底的字典序选点，数值评分不能跨优先级补偿；
- 分离 `target_invalid`、`straight_corridor_blocked`、
  `planner_unreachable`，直线阻挡不再提前否定安全终点；
- 删除低空多水平等待点搜索；home hover 后锁定 home-local XY，以 3 m 步长
  上升到 30 m，主通道失败时仅允许 5 s 最小保持、最多 2 次附近 3 m 通道切换；
- 到达安全高度并驻留 4 s 后才生成、评估和一次性锁定 ENTRY_GATE，再分段水平
  转场；未知、地图过期、实际占据、静态 OBB 风险和 EGO 不可达分别记账；
- 最终 gate/巡检端点继续静态硬过滤；滚动中间点只把实际地图占据作为硬拒绝，
  保守稀疏网格 OBB 交叉记录为软风险并交由 EGO；
- 增加 `inspection_laps` 和显式 `1→…→8→1` 闭环；
- 增加 NORMAL_RETURN，反向复用实际成功进场目标；RETURN_EGRESS 只作备用；
- bridge 增加 HOME_HOVER，land 服务要求 home 误差、输入、解锁和 OFFBOARD
  门禁；task DONE 同时要求 home、接地、解除武装和 bridge DONE；
- mission-supervised HOLD 可在严格输入/姿态/控制权门禁下请求紧急 AUTO.LAND，
  terminal failure 不再留下永久 HOLD；
- 修复已到达目标被旧 `TRAJECTORY_EXPIRED` 覆盖的问题，并按 EGO target/
  trajectory generation 隔离上一目标规划结果；
- bag 分析器同步检查闭环、NORMAL_RETURN 优先和 HOME_HOVER 先于 LANDING。

### 3.1 2026-07-23 历史任务与返航状态机

- `return_timeout` 与 `landing_timeout` 分离；
- bridge 状态转换使用独立进入时间戳；
- 新增 `RETURN_EGRESS`、安全圆弧生成、子目标重试和近目标跳过；
- 返航前按塔/吊机保守几何静态验证各任务段；
- bridge `DONE` 增加 home XY 1.5 m 终态约束；
- 扇区 1 增加 38 m 同扇区安全候选；
- 返航与落地阶段继续消费 planner/HOLD 失败，失败时有界重试后安全落地。

### 3.2 bridge 配置

- 仅 Stage 3 的 `alignment_position_tolerance` 调整为 0.75 m；
- 通用 bridge 和 Stage 2 的 0.25 m 配置不变；
- 独立跟踪保护仍为误差 1.0 m 持续 1.0 s。

### 3.3 录包与分析

- `stage3_ego.sh --stop` 先单独给 recorder 发送 SIGINT；
- 最长等待 120 s 让压缩 chunk 和索引落盘；未完成时拒绝强杀并保留会话；
- recorder 完成后才停止 PX4/Gazebo 等窗口；
- 新增 `scripts/tool/analyze_stage3_bag.py`，统一检查 topic、状态时间线、
  扇区唯一性、PlannerStatus、跟踪误差持续时间、控制发布者、type mask、
  frame、终态和采样占据净空；
- 最终 8 扇区停止流程已验证直接生成正式 `.bag`，没有 `.active`。

没有修改 EGO B-spline 优化数学、外部 PX4、FAST-LIO 核心或锁定版本。

## 4. 构建与测试

2026-07-24 最后一次回归：

```text
相关包构建：通过
astra_tower_mission gtest：41/41
ego_gazebo_bridge gtest：26/26
stage3 无控制 rostest：1/1
catkin_test_results：160 tests，0 errors，0 failures，0 skipped
stage3_ego.sh / stage3_record_bag.sh bash -n：通过
analyze_stage3_bag.py py_compile：通过
持久化故障复飞：识别 ENTRY_GATE 滚动首段 OBB/实际占据判定不一致，
并自动 NORMAL_RETURN→HOME_HOVER→AUTO.LAND
最终 8 扇区控制复飞：passed=true
```

新增覆盖包括独立返航/落地超时、直线返航穿塔判定、塔外侧 egress 生成、
home 落地点约束、重建跳过近径向目标、supervised HOLD 紧急落地门禁，以及
ENTRY_GATE HOLD、同目标恢复和两次 planner failure 后最小备用点的集成注入。

## 5. 仍存在的风险

1. 当前 MID-360/FAST-LIO 注册点云没有正式 free/unknown 体素；上升 unknown
   只作诊断，已知占据和 EGO 为硬门禁。这是仿真适配结论，不能当作真机自由
   空间证明。
2. `map -> camera_init` 单位变换只在当前仿真成立，不是真机标定结果。
3. 吊机 OBB 是保守整块包围盒，会包含稀疏网格空隙；最终端点仍硬过滤，中间
   路径依赖 FAST-LIO/EGO 实际占据。最新逐扇区结果为 s3～s7 使用 30 m
   名义点、s2 使用 31 m 最小备用点、s0/s1 使用 38 m 备用点。
4. 最新 2.172 m 是采样膨胀体素中心距离，不等价于连续时间、连续
   空间的精确机体表面净空。
5. Stage 3 对齐门限 0.75 m 仍需在更长时间和扰动下观察；最新成功 bag 的最大
   跟踪误差 0.643 m，无持续超限。
6. FAST-LIO 仿真 `/Odometry.twist` 仍为零，任务速度到达条件可能低估真实速度；
   bag 分析已用 MAVROS odom 独立统计速度。
7. 正常落地统一为 EGO 到 HOME_HOVER 后由任务请求 PX4 AUTO.LAND；严重
   控制权故障的 bridge failsafe 是独立安全分支。
8. 无控制集成已注入 HOLD 和 planner failure，最终控制复飞也实际触发一次
   no-progress HOLD 并恢复；全部服务同时失败、地图长期过期和持续控制断流仍
   未穷举。
9. 动态障碍预测、多机、相机检测、Cloud/QGIS、真机和干净 clone 交付仍未验证。
10. 本轮 FAST-LIO 仿真更新了受保护运行日志
    `AstraDrone_ros1_ws/src/SLAM/FAST_LIO/Log/mat_pre.txt`；未编辑、未清理、
    未还原。
11. 当前全部修改仍未提交，需项目负责人审阅；Codex 未 commit、未 push。

## 6. 2026-07-23 历史成功 bag 复核命令

```bash
source /opt/ros/noetic/setup.bash
rosbag info \
  /home/yanzu/bag/astra_stage3_eight_sector_2026-07-23.bag
sha256sum \
  /home/yanzu/bag/astra_stage3_eight_sector_2026-07-23.bag

cd /home/yanzu/AstraDroneOpen
python3 scripts/tool/analyze_stage3_bag.py \
  /home/yanzu/bag/astra_stage3_eight_sector_2026-07-23.bag \
  --occupancy-stride 10 \
  --output \
  /home/yanzu/bag/astra_stage3_eight_sector_2026-07-23.analysis.json
```

任何后续实际控制复测仍会解锁、起飞并进入 OFFBOARD，必须重新确认控制权、
preflight 和明确批准；本文记录已完成命令，但不把批准自动延伸到下一轮。

## 7. 2026-07-24 yaw/候选高度补充闭环

- bag 根因：纯垂直段水平前进方向未定义，旧 bridge 保留了 traj_server 的
  跳变 yaw；首个 38 m 备用点又使后续 30 m 名义点落到当前向下视场之外，
  瞬时视场 unknown 被误作硬拒绝，形成高度级联。
- 最终方案：垂直/近静止段保持实测有效 yaw；仅巡检 `SECTOR` 朝塔，其余进场、
  恢复和返航朝运动方向；巡检终点 unknown 只作诊断，地图过期、真实占据、
  静态净空和 EGO 不可达仍分别作硬约束；每扇区独立执行名义点绝对优先。
- 构建与测试：相关包构建通过，160/160 测试通过，无控制集成通过。
- 完整控制复飞：
  `/home/yanzu/bag/astra_stage3_yaw_sectorfix_2026-07-24.bag`，
  分析结果 `passed=true`。上升 yaw 最大偏差 0.035 deg、最大 XY 漂移
  0.071 m；扇区目标为 `s1_c3(38)→s2_c50(31)→s3_c0…s7_c0(30)→
  s0_c3(38)→s1_c3(38)`；HOLD=0、规划连续失败=0、最大跟踪误差
  0.643 m，最终正常返航、HOME_HOVER、AUTO.LAND、解除武装、ON_GROUND、
  bridge/task DONE。
- 绕塔画面晃动的 bag 结论：tower-facing yaw 本身稳定；上一异常轮主要是
  EGO 横向加减速造成 roll/pitch 增大，而固定相机把机体姿态直接带入画面。
  最新复飞 roll/pitch p95 为 1.074/0.821 deg，已恢复到旧稳定水平。仍无云台，
  更复杂障碍和更高速度下的画面稳定性是非阻断风险。
