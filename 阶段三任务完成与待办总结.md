# 阶段三任务完成与待办总结

更新时间：2026-07-23 17:15（Asia/Shanghai）  
当前分支：`ego-project`  
当前 HEAD：`48e35c28297317ace562c95b925180c853e23f43`  
提交状态：未 commit、未 push

本文是阶段三当前状态的简明总表。启动、参数、完整历史 bag 时间线和排障细节见
[`ego-stage3学习说明文档.md`](./ego-stage3学习说明文档.md)。

## 1. 最终结论

用户批准的唯一带控制范围已经正常跑通：

```text
起飞
→ 27 个候选中选择安全 ENTRY_GATE_a23
→ 5 段三维滚动进场
→ 跳过被吊机 OBB 完全覆盖的最近几何扇区
→ 到达最近安全扇区 2 / s2_c36
→ 完成单扇区
→ EGO 返航
→ 正常 AUTO.LAND
→ armed=false + ON_GROUND
→ bridge/task DONE
```

本轮没有执行两扇区、四扇区或八扇区。最终成功流程没有进入 HOLD、恢复或异常
降落。终端最大跟踪误差为 0.307 m。

不能据此宣称阶段三全部验收。实际 38 m R1/R2 恢复、新逻辑下 ENTRY_GATE
换点、两/四/八扇区、更多故障注入、长时稳定性和真机仍未完成。

状态含义：

- **已控制验证**：有 Gazebo/PX4 SITL 控制飞行和完整 bag；
- **已自动化验证**：单元/无控制集成/dry-run 已通过，未单独飞行覆盖；
- **未飞行验证**：代码或参数已存在，但本次成功飞行未经过该分支；
- **仅设计预留**：不属于阶段三当前交付。

## 2. 本轮 bag、时间线与根因

### 2.1 修复前失败证据

- bag：
  `/home/yanzu/bag/astra_stage3_single_sector_hold_bug_2026-07-23.bag`
- CSV：
  `/home/yanzu/bag/astra_stage3_single_sector_hold_bug_2026-07-23.csv`
- SHA-256：
  `17998baac1922d463b2413af1a61df9c1bf0392df0ef16ac8394e05641982cd9`
- 范围：`10614.802–10989.049`，374.247 s，528,579 条消息

关键证据：

| 仿真时间 | 事件 |
|---:|---|
| 10635.105–10757.254 | `ENTRY_GATE_a23` 的 5 个三维子目标全部成功 |
| 10757.304 | 最近几何 `sector_id=1` 的 45/45 候选均被吊机 OBB 拒绝；`sector_id=2` 有 3 个安全候选 |
| 10757.306 | 旧任务逻辑未选择下一安全扇区，进入 HOLD/cancel |
| 10757.298→10757.318 | raw setpoint 从 `(5.694,7.381,29.473)` 跳到陈旧起飞点 `(0.024,-0.004,3.997)` |
| 10757.598–10759.462 | MAVROS 实测速度最高约 5 m/s，飞机快速回撤/下降 |
| 10759.354 | re-entry 错误变成 `recovery_reentry_ENTRY_GATE_a23` |
| 10759.739 | 高速运动造成位置对齐误差 0.290 m，再次 HOLD |
| 10823.955 | 恢复超时，任务安全请求返航；监督 HOLD 未自动降落 |
| 10967.953 | 正常返航、AUTO.LAND、解锁接地、双方 DONE |

确定根因有三项：

1. gate 后只选择几何最近扇区，没有在所有扇区中选择最近安全可用扇区；
2. bridge 的 raw EGO 输出绕过位置 setpoint 缓存，进入 HOLD 时又从陈旧的 4 m
   起飞缓存开始发布，造成危险 setpoint 跳变；
3. 没有已锁定扇区目标时，re-entry 错误继承 ENTRY_GATE 或上一恢复目标。

快速回撤/下降不是 EGO 规划回 home，也不是 PX4 自主 failsafe。直接触发源是
bridge HOLD 输出缓存错误。最后降落仍是任务返航到 home 后的正常
`AUTO.LAND`。

### 2.2 修复后 dry-run

- bag：
  `/home/yanzu/bag/astra_stage3_dryrun_safe_sector_fix_2026-07-23.bag`
- CSV：
  `/home/yanzu/bag/astra_stage3_dryrun_safe_sector_fix_2026-07-23.csv`
- SHA-256：
  `be6a69e34d9580940fadfca06d36b77054407f4842b43ab1e16694223c3442d9`

结果：

```text
ENTRY_GATE 候选：9 个角度 × 3 个半径 = 27
ENTRY_GATE_a8：KNOWN_OBSTACLE_CLEARANCE，clearance=-0.033 m
ENTRY_GATE_a23：accepted，clearance=2.388 m
最终 gate：ENTRY_GATE_a23=(5.812,7.535,30)
恢复参数：38 m
raw-local/position setpoint publishers：None
PX4：armed=false，ON_GROUND
```

### 2.3 最终成功控制证据

- bag：
  `/home/yanzu/bag/astra_stage3_single_sector_success_2026-07-23.bag`
- CSV：
  `/home/yanzu/bag/astra_stage3_single_sector_success_2026-07-23.csv`
- SHA-256：
  `9b5ed50ccfa09b7c599e683c3f737dac7a77cfab0b2e6914ace59cd35c001408`
- 范围：`10614.802–11049.347`，434.545 s，约 1.2 GB，616,715 条消息

| 仿真时间 | 事件 |
|---:|---|
| 10620.922–10635.138 | bridge 依次进入 WAIT_INPUTS、PRESTREAM、ARM_OFFBOARD、TAKEOFF、HOVER_READY |
| 10635.157 | 选择 `ENTRY_GATE_a23`，生成 5 个三维滚动目标 |
| 10635.207–10734.206 | 目标依次为 `(1.169,1.463,9.209)`、`(2.330,2.981,14.406)`、`(3.490,4.499,19.604)`、`(4.651,6.017,24.802)`、`(5.812,7.535,30)` |
| 10759.667 | gate 到达；扇区 1 的 45/45 候选被 OBB 拒绝，选择最近安全扇区 2 |
| 10759.707 | 发布 `s2_c36=(1.752,27.233,30)` |
| 10856.757 | 单扇区完成，正常进入 RETURN_HOME |
| 11013.438–11013.459 | 到达 home hover，bridge 请求 AUTO.LAND |
| 11030.334–11032.956 | ON_GROUND、armed=false、bridge/task DONE |

最终一致性审计：

| 项目 | 结果 |
|---|---|
| mission/bridge HOLD | 0 次 |
| `/planning/cancel` | 0 条 |
| PlannerStatus 最大连续失败 | 0 |
| goal/current collision | 全程 false |
| emergency stop | 全程 false |
| raw `type_mask` | 全部为 0 |
| MAVROS 控制出口 | 唯一连接 `/ego_mavros_bridge` |
| 跟踪误差 | max 0.307 m，mean 0.048 m，P95 0.081 m |
| 扇区完成位置误差 | 约 0.334 m |
| 扇区完成 MAVROS 速度 | 0.157 m/s |
| 扇区完成 yaw 误差 | 约 0.018 rad |
| planning frame | odom/cloud/occupancy/goal/pos_cmd 均为 `camera_init` |
| MAVROS frame | local odom/raw setpoint 均为 `map` |
| TF | `map -> camera_init` 为仿真单位变换 |

EGO 控制台曾在吊机附近出现单次内部优化重试，但 `/planner/status` 没有形成一次
外层规划调用失败，连续失败计数始终为 0；下一次外层规划均成功，所以任务未
进入 HOLD。这符合“真实外层 FSM 失败计数”契约。

## 3. 已完成的代码修改

### 3.1 ENTRY_GATE、扇区和恢复

- 取消低空接近点后固定 XY 垂直爬升；
- `ENTRY_GATE` 为巡检高度的特殊 `sector_id=-1` 航点，不占 8 个扇区；
- 生成 27 个角度/半径候选，并检查地图新鲜度、占据、塔/吊机净空和任务边界；
- 使用 worksite `<state>` 位姿和 DAE 米制尺寸建立吊机 yaw 定向三维 OBB：
  center `(2.7535,14.7908)`、half extent `(3.2732,19.5424)`、
  yaw `-0.479608`、z `0–35.0283`；
- 起飞 hover 到 gate 使用最大 6 m 的三维滚动子目标；
- gate 到达后选择最近安全扇区，而非只选择几何最近扇区；
- 旋转扇区顺序但不删除扇区，未来 8 扇区唯一性仍保留；
- recovery re-entry 只引用当前扇区已锁定目标或同扇区活动巡检目标；
- 当前恢复高度为 38 m，上限 40 m。

### 3.2 bridge、PlannerStatus 和旧轨迹隔离

- bridge 继续是唯一 MAVROS setpoint 出口；
- HOLD/LANDING 同时从 MAVROS 实测位姿锁存 `hold_pose_` 和
  `output_setpoint_`，修复陈旧起飞 setpoint 回跳；
- 取消后旧 trajectory generation 不得重新获得控制；
- `/planning/cancel` 同时通知 EGO FSM 和 traj_server；
- 恢复型监督 HOLD 不因有限 loss timeout 自动降落；
- yaw 首帧从 MAVROS 实测值初始化，按最短角限速；
- yaw-only 对齐超限使用持续窗口，位置/TF 超限仍立即 HOLD；
- `/planner/status` 由 EGO 外层 FSM 发布真实状态、trajectory id、规划调用失败
  计数、goal/current occupancy 和 emergency-stop 状态。

没有修改 EGO B-spline 优化数学、PX4、FAST-LIO 核心或版本。

### 3.3 脚本与录包

- `stage3_ego.sh` 新增 `--bag FILE`；
- recorder 在 bridge/mission 之前启动，集成流程等待
  `/stage3_evidence_recorder`；
- bag/`.active` 已存在时拒绝覆盖；
- 录制 `/clock`、五类 MAVROS 控制出口、planning、PlannerStatus、任务状态、
  点云、占据图、TF 和 `/rosout`；
- `--stop` 在接地后安全停止所有窗口并完整关闭 bag。

## 4. 修改文件清单

主要任务层：

- `AstraDrone_ros1_ws/src/MissionControl/astra_tower_mission/include/astra_tower_mission/stage3_planner.h`
- `AstraDrone_ros1_ws/src/MissionControl/astra_tower_mission/src/stage3_planner.cpp`
- `AstraDrone_ros1_ws/src/MissionControl/astra_tower_mission/src/stage3_ego_mission_node.cpp`
- `AstraDrone_ros1_ws/src/MissionControl/astra_tower_mission/src/occupancy_stamp_adapter_node.cpp`
- `AstraDrone_ros1_ws/src/MissionControl/astra_tower_mission/config/stage3_ego.yaml`
- `AstraDrone_ros1_ws/src/MissionControl/astra_tower_mission/launch/stage3_ego.launch`
- `AstraDrone_ros1_ws/src/MissionControl/astra_tower_mission/test/stage3_planner_test.cpp`
- `AstraDrone_ros1_ws/src/MissionControl/astra_tower_mission/test/stage3_no_control_integration.{test,py}`

bridge：

- `AstraDrone_ros1_ws/src/MissionControl/ego_gazebo_bridge/include/ego_gazebo_bridge/{ego_mavros_bridge.h,command_utils.h,recovery_control.h}`
- `AstraDrone_ros1_ws/src/MissionControl/ego_gazebo_bridge/src/{ego_mavros_bridge.cpp,command_utils.cpp}`
- `AstraDrone_ros1_ws/src/MissionControl/ego_gazebo_bridge/config/{ego_gazebo_bridge.yaml,stage3_ego.yaml}`
- `AstraDrone_ros1_ws/src/MissionControl/ego_gazebo_bridge/test/{command_utils_test.cpp,recovery_control_test.cpp}`

EGO 外层状态与取消隔离：

- `AstraDrone_ros1_ws/src/Planner/ego-planner/planner/plan_manage/include/plan_manage/{ego_replan_fsm.h,planning_status_tracker.h}`
- `AstraDrone_ros1_ws/src/Planner/ego-planner/planner/plan_manage/src/{ego_replan_fsm.cpp,traj_server.cpp}`
- `AstraDrone_ros1_ws/src/Planner/ego-planner/planner/plan_manage/test/planning_status_tracker_test.cpp`
- `AstraDrone_ros1_ws/src/Utils/astra_custom_msgs/msg/PlannerStatus.msg`

脚本和文档：

- `scripts/run_sh/stage3_ego.sh`
- `scripts/run_sh/stage3_record_bag.sh`
- `ego-stage3学习说明文档.md`
- `阶段三任务完成与待办总结.md`

相应 `CMakeLists.txt` 和 `package.xml` 也已同步。没有恢复、清理或手工编辑
用户已有的 `AstraDrone_ros1_ws/src/SLAM/FAST_LIO/Log/mat_pre.txt`；它仍保持
dirty，仿真运行时 FAST-LIO 可能继续写入该日志。

## 5. 测试结果

| 测试 | 结果 |
|---|---:|
| `stage3_planner_test` | 15/15 |
| tower route / ego task utils | 16/16 |
| `ego_gazebo_bridge` | 24/24 |
| `stage3_no_control_integration` | 1/1 |
| EGO goal height / PlannerStatus tracker | 5/5 |
| offboard trajectory / landing profile | 7/7 |
| 本轮相关独立用例合计 | 68/68 |
| 任务层与 bridge 构建 | 通过 |
| EGO 外层 FSM/traj_server 构建 | 通过 |
| 修复后实际栈 dry-run | 通过 |
| 单 ENTRY_GATE/单扇区控制飞行 | 通过 |

无控制集成测试覆盖了 gate HOLD 换点、cancel、真实 PlannerStatus 失败、
R1/R2/re-entry 和返航。它不创建 MAVROS setpoint publisher，不能冒充控制飞行
证据。

## 6. 尚未解决的风险

1. **38 m 恢复尚未飞行**：参数、边界和单测已通过；最终成功路径未触发恢复，
   没有实际飞到 38 m。
2. **HOLD/re-entry 修复尚未受控故障注入**：故障 bag 已定位根因，单元和
   无控制集成已覆盖；最终成功飞行全程无 HOLD/RECOVERING。
3. **新 gate 换点尚未控制验证**：无控制集成已覆盖，真实控制飞行未故意占据
   `ENTRY_GATE_a23`。
4. **OBB 是保守目标预过滤**：它包含网格空隙；目标
   `s2_c36` 的直线走廊被标记 `STRAIGHT_CORRIDOR_BLOCKED`，最终由 EGO 根据
   实际点云/占据图成功重规划。仍需自动统计真实最小净空。
5. **速度源契约**：当前 FAST-LIO `/Odometry.twist` 为零，任务到达速度条件
   可能低估真实速度。本次独立 MAVROS 速度为 0.157 m/s，仍满足 0.2 m/s 门限。
6. **TF 仍是仿真假设**：最终 bag 证明 frame/时间戳一致，不代表真机标定。
7. **正常降落架构未统一**：本次仍由 bridge 在 home 请求 `AUTO.LAND`；需决定
   是否改为正常 OFFBOARD 受控下降，仅保留 AUTO.LAND 作为失效保护。
8. **覆盖范围有限**：两/四/八扇区、长时稳定、全部故障注入、动态障碍、真机
   均未验证。
9. **工作区未提交**：需要负责人审阅 diff；Codex 不会 commit 或 push。

## 7. 下一步

按照当前证据，下一步优先级是：

```text
无控制 ENTRY_GATE 占据/地图过期注入
→ 无控制 command/odom/map 断流与恢复耗尽
→ 审阅最终成功 bag 和故障注入结果
→ 另行批准后做单扇区 38 m 恢复场景
→ 另行批准两扇区
→ 四扇区
→ 最后重新审阅是否批准八扇区
```

不要直接执行两/四/八扇区。任何新的 `--control`、ARM、起飞或 OFFBOARD 都
需要新的明确批准。

## 8. 可复制验证命令

### 8.1 构建

```bash
source /opt/ros/noetic/setup.bash
cd /home/yanzu/AstraDroneOpen/AstraDrone_ros1_ws
catkin_make \
  -DCATKIN_WHITELIST_PACKAGES='astra_tower_mission;ego_gazebo_bridge' -j2
```

### 8.2 单元与无控制集成测试

```bash
source /opt/ros/noetic/setup.bash
export ROS_HOME=/tmp/astra_stage3_tests
export ROS_LOG_DIR=/tmp/astra_stage3_tests/log
mkdir -p "$ROS_LOG_DIR"
cd /home/yanzu/AstraDroneOpen/AstraDrone_ros1_ws
catkin_make \
  -DCATKIN_WHITELIST_PACKAGES='astra_tower_mission;ego_gazebo_bridge' \
  run_tests_astra_tower_mission run_tests_ego_gazebo_bridge -j2
```

### 8.3 无控制实际栈 dry-run

```bash
cd /home/yanzu/AstraDroneOpen
scripts/run_sh/stage3_ego.sh --sector-limit 1 \
  --bag /tmp/stage3_dryrun_repeat.bag --gui --rviz --attach
```

停止：

```bash
cd /home/yanzu/AstraDroneOpen
scripts/run_sh/stage3_ego.sh --stop
```

### 8.4 最终 bag 检查

```bash
source /opt/ros/noetic/setup.bash
rosbag info \
  /home/yanzu/bag/astra_stage3_single_sector_success_2026-07-23.bag
sha256sum \
  /home/yanzu/bag/astra_stage3_single_sector_success_2026-07-23.bag
```

已经完成的控制命令如下，仅用于复现记录。后续实际执行前必须再次获得明确批准：

```bash
cd /home/yanzu/AstraDroneOpen
scripts/run_sh/stage3_ego.sh --control --sector-limit 1 \
  --bag /tmp/stage3_single_sector_repeat.bag --gui --rviz --attach
```

本文不提供两/四/八扇区的直接控制启动命令。
