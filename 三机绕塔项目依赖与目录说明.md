# 三机绕塔项目依赖与目录说明

本文只说明当前仓库中三架无人机绕塔仿真所需的环境、外部组件和主要目录。事实依据是当前的 `package.xml`、`CMakeLists.txt`、launch、脚本和配置；通用教程中超出当前 ROS 1 仿真主线的内容不作为本任务依赖。

## 一、项目依赖

### 1. 基础环境

当前项目已经实际使用或明确锁定的开发环境如下：

| 项目 | 当前基线 | 说明 |
|---|---|---|
| 操作系统 | Ubuntu 20.04.6 LTS | 项目 PC 初始化脚本只接受 Ubuntu 20.04（Focal）；旧教程提到的其他版本没有三机任务验证证据。 |
| ROS | ROS 1 Noetic | 当前没有可用于本任务的 ROS 2 工作空间。 |
| 仿真器 | Gazebo Classic 11.15.1 | launch 使用 `gazebo_ros/empty_world.launch`，不是新版 Gazebo。 |
| Python | Python 3.8.10 | 多机管理、安全、坐标适配和证据记录节点均使用 Python 3。 |
| 编译工具 | CMake 3.16.3、GCC 9.4、GNU Make | C++ 包使用 C++11、C++14 或 C++17；以上是已验证机器上的版本，不代表每个依赖都单独锁定了补丁版本。 |
| 工作空间 | ROS 1 catkin | 下层为 `simulation/sim_workspace/`，上层为 `AstraDrone_ros1_ws/`；仓库的可靠构建方式是 `catkin_make`。 |

三机仿真负载较高。CPU、内存和显卡的最低规格没有在仓库中形成可验证要求，需要根据本机环境确认。

### 2. 系统和 ROS 依赖

下面的精简安装集合来自三机相关包的 manifest、CMake 和启动脚本。ROS Noetic 软件源应先按 ROS 官方方式配置好。

```bash
sudo apt update
sudo apt install -y \
  build-essential cmake git pkg-config \
  python3-dev python3-pip python3-rosdep \
  python3-matplotlib python3-numpy \
  libeigen3-dev libpcl-dev libopencv-dev libboost-all-dev \
  libprotobuf-dev protobuf-compiler libapr1-dev \
  ros-noetic-desktop-full \
  ros-noetic-mavros ros-noetic-mavros-extras \
  ros-noetic-gazebo-ros-pkgs ros-noetic-gazebo-plugins \
  ros-noetic-pcl-ros ros-noetic-cv-bridge \
  ros-noetic-image-transport ros-noetic-camera-info-manager \
  ros-noetic-tf2-geometry-msgs ros-noetic-xacro
```

这些软件分别覆盖 catkin/编译器、Eigen/PCL/OpenCV/Boost、Mid360 Gazebo 插件所需的 Protobuf、Livox ROS 驱动所需的 APR，以及 MAVROS、Gazebo ROS 插件、TF、图像传输、Xacro 和 RViz。`python3-matplotlib` 用于运行结束后的轨迹图；`python3-numpy` 主要用于离线检查工具。

首次使用 `rosdep` 时执行：

```bash
sudo rosdep init
rosdep update
```

随后可针对本任务目录补齐 manifest 中可解析的依赖：

```bash
cd /home/yanzu/AstraDroneOpen
rosdep install --from-paths \
  simulation/sim_workspace/src/env_map \
  simulation/sim_workspace/src/sensors/Mid360_simulation_plugin/livox_laser_simulation \
  simulation/sim_workspace/src/sensors/realsense_ros_gazebo \
  AstraDrone_ros1_ws/src/Utils/astra_custom_msgs \
  AstraDrone_ros1_ws/src/Utils/livox_ros_driver \
  AstraDrone_ros1_ws/src/Utils/livox_ros_driver2 \
  AstraDrone_ros1_ws/src/SLAM/FAST_LIO \
  AstraDrone_ros1_ws/src/Planner/ego-planner/planner \
  AstraDrone_ros1_ws/src/Planner/ego-planner/uav_simulator/Utils/quadrotor_msgs \
  AstraDrone_ros1_ws/src/Planner/ego-planner/uav_simulator/Utils/waypoint_generator \
  AstraDrone_ros1_ws/src/MissionControl/astra_uavoffbard_frame/offboard \
  AstraDrone_ros1_ws/src/MissionControl/astra_tower_mission \
  AstraDrone_ros1_ws/src/MissionControl/ego_gazebo_bridge \
  AstraDrone_ros1_ws/src/Swarm \
  --ignore-src --rosdistro noetic -r -y --skip-keys=px4
```

`px4` 是仓库外源码工程，不是本工作空间内可由 `rosdep` 安装的普通包。不同机器的 rosdep 数据库状态也可能不同；若命令报告未知 key，应先核对对应的 `package.xml`，不要随意安装同名非 ROS 软件。

MAVROS 还需要 GeographicLib 数据。仓库包含 `third_party/GeographicLib/` 数据副本，系统安装的 MAVROS 也提供：

```bash
sudo /opt/ros/noetic/lib/mavros/install_geographiclib_datasets.sh
```

二者选择哪一种作为机器安装来源，需要按团队现有部署方式确认，避免覆盖已经锁定的数据。

### 3. 第三方工程

| 组件 | 作用 | 仓库是否包含 | 是否另行下载/安装 | 版本或来源状态 |
|---|---|---|---|---|
| PX4 SITL | 三个飞控实例、OFFBOARD 控制和 Gazebo 动力学接口 | 仓库只包含定制 airframe、SDF 和 launch 资产，不包含完整 PX4 源码 | **需要**单独准备并编译 | 当前验证树为 `/home/yanzu/PX4-Autopilot`，PX4 v1.15.4，提交 `99c40407ffd7ac184e2d7b4b293f36f10fe561ef`，工作树为 dirty/detached；禁止自行升级。 |
| MAVROS | 在 ROS 与三套 PX4/MAVLink 实例之间转换状态、服务和控制指令 | 不包含源码 | 通过 apt 安装 | 已验证为 MAVROS 1.20.1；其他补丁版本需要确认。 |
| FAST-LIO | 将每架机 Mid360 点云和 IMU 转为里程计、注册点云与 TF | **已包含**于主工作空间 | 不需另行下载，但需编译其系统依赖 | 仓库未给出独立、可重放的上游提交标识。 |
| 当前多机规划器 | EGO 的地图、搜索、B 样条优化、轨迹服务和多机轨迹消息 | **已包含**于 `AstraDrone_ros1_ws/src/Planner/ego-planner/` | 不需另行下载 | 当前代码包含项目适配和多机轨迹能力；仓库没有可可靠确认的精确上游提交，不能用任意新版覆盖。 |
| Livox ROS 驱动 | 提供 FAST-LIO 编译所需的 `livox_ros_driver/CustomMsg` 等接口 | **已包含** `livox_ros_driver` 和 `livox_ros_driver2` 源码 | `livox_ros_driver` 构建还需要 Livox-SDK 库 | manifest 显示 `livox_ros_driver` 2.0.0、`livox_ros_driver2` 1.2.6；SDK 来源元数据见下方注意事项。 |
| Mid360 Gazebo 插件 | 在 Gazebo 中为三架机发布独立雷达数据 | **已包含**于下层仿真工作空间 | 不需另行下载，但必须先编译下层工作空间 | 插件名为 `liblivox_laser_simulation.so`，依赖 Gazebo、PCL 和 Protobuf。 |
| D435 Gazebo 插件 | 为三架机生成各自命名空间下的彩色、红外、深度和点云话题 | **已包含**于下层仿真工作空间 | 不需为本仿真另装实体相机驱动 | 使用本仓库的 `realsense_ros_gazebo` 和 `librealsense_gazebo_plugin.so`；三机规划主链仍使用 Mid360/FAST-LIO，不以 D435 深度图作为规划输入。 |
| Gazebo 世界与模型 | 提供 `worksite.world`、铁塔、环境模型及三机传感器外观 | **已包含** | 不需另行下载 | 模型搜索路径由三机运行脚本设置。 |
| `third_party/` 源码 | 保存 Livox SDK、GeographicLib、NLopt、librealsense 等第三方快照 | **已包含大部分源码** | 只安装当前构建实际需要的组件 | NLopt、实体 RealSense SDK 等目录存在，不等于三机 Gazebo 任务必须安装。多数快照没有单独可核验的上游提交信息。 |

克隆和 PX4 需要特别注意：

- 根目录当前没有 `.gitmodules`，因此不要把 `git clone --recurse-submodules` 写成可靠安装步骤。
- Git 索引中仍有 `AstraDrone_ros1_ws/src/Utils/livox_ros_driver/livox_ros_driver/Livox-SDK` 的 gitlink，但缺少对应 `.gitmodules` 映射。干净克隆后该目录可能为空，且 `git submodule update` 会失败。仓库另有 `third_party/Livox-SDK/` 源码副本，但二者没有自动关联；应由项目负责人确认 SDK 的安装来源和版本。
- `scripts/run_sh/three_uav_inspection.sh` 当前把 PX4 路径固定为 `/home/yanzu/PX4-Autopilot`，而安装脚本使用 `$HOME/PX4-Autopilot`。这只在用户名为 `yanzu` 时自然一致。
- 三机 launch 需要 PX4 已构建目录中的 `1048/1049/1050_gazebo-classic_iris_mid360_*` airframe。仓库保存了相应资产，但没有找到将它们可靠部署到外部 PX4 源码并重建的自动化说明；新机器应向项目负责人取得锁定 PX4 树或明确部署步骤，不要把仓库文件直接覆盖到任意 PX4 版本。

### 4. 克隆后的安装与编译顺序

以下流程只准备环境和构建，不会启动或解锁无人机。

1. 普通克隆仓库，不附加 `--recurse-submodules`：

   ```bash
   git clone <AstraDroneOpen仓库地址> /home/yanzu/AstraDroneOpen
   cd /home/yanzu/AstraDroneOpen
   ```

2. 安装上一节列出的 apt/ROS 依赖，初始化 `rosdep`，并处理 Livox-SDK。不要直接运行不透明的 `.bin` 安装器来替代依赖核对；这些安装器可能修改系统、环境变量或构建目录。

3. 在 `/home/yanzu/PX4-Autopilot` 准备锁定的 PX4 v1.15.4 源码和 `px4_sitl_default` 构建，并确认三机 airframe 已进入该构建。PX4 的完整部署步骤目前没有在仓库中闭合，必须先向项目负责人确认。

4. 先编译下层仿真工作空间：

   ```bash
   source /opt/ros/noetic/setup.bash
   cd /home/yanzu/AstraDroneOpen/simulation/sim_workspace
   catkin_make \
     -DCATKIN_WHITELIST_PACKAGES='env_map;livox_laser_simulation;realsense_ros_gazebo' \
     -j2
   source devel/setup.bash
   ```

5. 保持下层工作空间已 source，再编译三机任务需要的主工作空间包：

   ```bash
   cd /home/yanzu/AstraDroneOpen/AstraDrone_ros1_ws
   catkin_make \
     -DCATKIN_WHITELIST_PACKAGES='astra_custom_msgs;quadrotor_msgs;livox_ros_driver;fast_lio;traj_utils;plan_env;path_searching;bspline_opt;ego_planner;waypoint_generator;offboard;astra_tower_mission;ego_gazebo_bridge;astra_swarm_msgs;astra_swarm_manager;astra_swarm_safety;astra_swarm_tf;astra_swarm_perception;astra_swarm_bringup' \
     -j2
   source devel/setup.bash
   ```

6. 每个新终端按同一顺序加载环境：

   ```bash
   source /opt/ros/noetic/setup.bash
   source /home/yanzu/AstraDroneOpen/simulation/sim_workspace/devel/setup.bash
   source /home/yanzu/AstraDroneOpen/AstraDrone_ros1_ws/devel/setup.bash
   ```

`build/` 和 `devel/` 都是生成物，不应手工编辑。以上白名单来自当前三机启动链；如果后续启用与任务无关的检测、探索或真机功能，需要另行核对依赖。

## 二、三架无人机绕塔任务涉及的文件夹

- `AstraDrone_ros1_ws/src/Swarm/astra_swarm_manager/`
  - 主要作用：多机协调策略、角色顺序、联合入口选择和许可管理。
  - 三机流程职责：汇总三机状态，协调 UAV3、UAV2、UAV1 的进场、绕塔放行、退出和落地许可；三机编队参数在 `config/three_uav_inspection_formation.yaml`。

- `AstraDrone_ros1_ws/src/Swarm/astra_swarm_safety/`
  - 主要作用：心跳、定位、状态时效和机间距离安全检查。
  - 三机流程职责：生成全局安全许可或抑制状态，使协调层和任务层在输入异常时保持安全闭锁。

- `AstraDrone_ros1_ws/src/Swarm/astra_swarm_msgs/`
  - 主要作用：定义多机状态、预测轨迹、协调状态和安全事件消息。
  - 三机流程职责：为三架机和全局协调节点提供统一通信格式。

- `AstraDrone_ros1_ws/src/Swarm/astra_swarm_tf/`
  - 主要作用：适配 FAST-LIO、PX4/MAVROS 和多机前缀坐标系。
  - 三机流程职责：把每架机的里程计、点云、TF 和 MAVROS 位姿隔离到 `/uav1`、`/uav2`、`/uav3` 命名空间，并提供 PX4 外部里程计。

- `AstraDrone_ros1_ws/src/Swarm/astra_swarm_perception/`
  - 主要作用：队友点云过滤、自机点云过滤和可选的世界点云融合。
  - 三机流程职责：避免把自身或队友机体误写成静态障碍，并向每套 EGO 地图提供过滤后的本机点云。

- `AstraDrone_ros1_ws/src/Swarm/astra_swarm_bringup/`
  - 主要作用：三机仿真的统一启动、模型生成、参数组织、诊断和运行记录。
  - 三机流程职责：`launch/triple_tower_inspection.launch` 是三机总 launch；`launch/triple_px4_mavros.launch` 创建三套 PX4/MAVROS 命名空间；`launch/uav_tower_stack.launch` 为每架机连接 FAST-LIO、EGO、bridge 和任务节点。
  - 其中 `config/` 保存三机 RViz、三份 bridge 参数、低空 EGO 参数和 PX4 参数守卫输入；`config/triple_tower.rviz` 是三机可视化配置。`models/`、`urdf/` 保存带 Mid360 与 D435 的三机模型和显示坐标；`scripts/` 保存模型生成验证、PX4 参数检查、诊断和证据记录节点。

- `AstraDrone_ros1_ws/src/MissionControl/astra_tower_mission/`
  - 主要作用：单机巡塔任务状态机、候选点与路线生成、任务参数和状态输出。
  - 三机流程职责：每架机各运行一份任务节点；`config/low_altitude_inspection.yaml` 提供三机共同使用的低空任务参数，协调许可由多机管理器输入。

- `AstraDrone_ros1_ws/src/MissionControl/ego_gazebo_bridge/`
  - 主要作用：连接 EGO 轨迹与 MAVROS/PX4，并执行输入健康、控制权、起飞、跟踪、返航和降落门禁。
  - 三机流程职责：每架机拥有独立 bridge 和独立 MAVROS 控制出口；三份覆盖参数位于 `astra_swarm_bringup/config/uav1_bridge.yaml`、`uav2_bridge.yaml`、`uav3_bridge.yaml`。

- `AstraDrone_ros1_ws/src/Planner/ego-planner/planner/`
  - 主要作用：包含 `plan_env`、`path_searching`、`bspline_opt`、`plan_manage` 和 `traj_utils`。
  - 三机流程职责：每架机建立局部占据地图、搜索并优化轨迹，发布轨迹命令，同时通过项目适配后的多机轨迹消息参与协同避碰。

- `AstraDrone_ros1_ws/src/Planner/ego-planner/uav_simulator/Utils/`
  - 主要作用：保存 `quadrotor_msgs` 和 `waypoint_generator` 等 EGO 支撑包。
  - 三机流程职责：提供规划轨迹消息和目标转接节点；不是三机动力学仿真的主入口。

- `AstraDrone_ros1_ws/src/SLAM/FAST_LIO/`
  - 主要作用：Mid360 激光雷达—惯性里程计与注册点云。
  - 三机流程职责：三份 `mapping_mid360.launch` 分别处理各机雷达和 IMU，为 EGO、任务节点和 PX4 外部视觉里程计提供定位输入。

- `AstraDrone_ros1_ws/src/Utils/livox_ros_driver/`
  - 主要作用：Livox ROS 消息和驱动源码。
  - 三机流程职责：当前 FAST-LIO 源码直接依赖其 `CustomMsg` 类型；仿真雷达插件源码位于下层工作空间。

- `simulation/sim_workspace/src/sensors/Mid360_simulation_plugin/`
  - 主要作用：Mid360 Gazebo 模型、扫描模式、消息和 `liblivox_laser_simulation.so` 插件源码。
  - 三机流程职责：为三架模型分别发布命名空间隔离的雷达点云；其动态库必须能通过 `GAZEBO_PLUGIN_PATH` 找到。

- `simulation/sim_workspace/src/sensors/realsense_ros_gazebo/`
  - 主要作用：D435 Gazebo 模型、网格和 `librealsense_gazebo_plugin.so` 插件源码。
  - 三机流程职责：生成 `/uav1/d435`、`/uav2/d435`、`/uav3/d435` 下的仿真相机数据；当前绕塔规划不直接消费这些深度图。

- `simulation/sim_workspace/src/env_map/`
  - 主要作用：让 ROS package 系统定位仓库中的 Gazebo world 资源。
  - 三机流程职责：三机 PX4/MAVROS launch 通过它定位 `worksite.world`。

- `simulation/px4_sim_files/`
  - 主要作用：保存项目定制的 PX4 SITL airframe、基础机体 SDF 和示例 launch 资产。
  - 三机流程职责：提供三套无 GPS Mid360 机型的模型基础和 airframe 参考；它不是完整 PX4 仓库，也不自动完成外部 PX4 部署。

- `simulation/astra_gazebo_worlds/` 与 `simulation/astra_gazebo_models/`
  - 主要作用：保存 Gazebo 世界和环境模型。
  - 三机流程职责：三机任务使用 `worksite.world`，并从模型目录加载铁塔、D435、Mid360 和其他场景资源。

- `scripts/run_sh/`
  - 主要作用：保存项目级启动脚本。
  - 三机流程职责：`three_uav_inspection.sh` 负责加载两个工作空间、设置 PX4/Gazebo 搜索路径、调用三机总 launch，并把 bag、CSV、日志和图片集中写入运行产物目录。脚本默认不授权飞行控制。

- `scripts/tool/`
  - 主要作用：保存 bag、占据图和轨迹的离线检查及绘图工具。
  - 三机流程职责：用于分析三机位置、绕塔覆盖、点云净空和轨迹图，不参与实时飞行控制。

- `docs/`、`rule.md`、`ego_swarm_three_uav_integration_report.md`
  - 主要作用：`docs/` 保存正式通用教程；`rule.md` 是当前扇区、候选、净空和三机角色规则；集成报告保存三机任务的当前验收事实。
  - 三机流程职责：帮助协作者理解约束和已验证范围。运行参数仍以 YAML、launch 和参数服务器为准，程序不会读取 Markdown 作为配置。

- `runtime_artifacts/`
  - 主要作用：只保存飞行试验产生的临时数据、ROS 日志、bag、CSV、JSON、图片、视频和调试输出。
  - 三机流程职责：每次三机运行使用独立时间戳目录；根 `.gitignore` 已忽略该目录。
  - 禁止将核心源代码、正式 launch、YAML 或模型配置放入其中，也不得将其内容加入 Git。不得重新创建已经删除的 `test_evidence/` 和 `trc_picture/`。
