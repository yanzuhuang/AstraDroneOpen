# Stage 5 UAV2 绕塔轨迹根因专项只读审计

日期：2026-08-03

分支：`ego-swarm`

审计 HEAD：`f68f07137db57698c15f39d093f812ff9851da15`

失败样本：`test_evidence/stage5_octagon_optimization_validation_20260803_024800/`

成功样本：`test_evidence/stage5_octagon_optimization_validation_20260803_040000/`

## 1. 结论摘要

UAV2 的“明显内切”“HOLD/小回头”“失败样本第 8 扇区占据报错”不是一个单一故障，而是三个可分离因素：

1. **明显内切的已证明直接原因是 EGO 实际生成的局部 B-spline 本身已经内切，UAV2 只是以约 5 cm 量级平均误差跟随它。** 成功样本 S6、S8、闭圈 S1 的 speed scale 全程均为 1.0，但实际最小塔心半径分别为 7.971、9.059、9.710 m；对应 B-spline 的“每条轨迹最小半径之均值”分别只有 8.879、9.621、10.183 m，实际平均跟踪误差分别为 0.058、0.057、0.047 m。因而深度内切不是 PX4 跟踪偏差，也不是 speed scale 直接把飞机拉向内侧。
2. **trajectory 高频更新是 EGO 当前配置下三机共同存在的约 1 Hz 周期重规划，不是 UAV2 独有异常。** 成功样本 UAV1/UAV2/UAV3 分别为 0.870/0.872/0.916 Hz；UAV2 的 669 条 B-spline 有 669 个不同几何哈希，排除了“仅更新时间戳/重复发布”。但更新频率与半径偏差的相关系数仅 -0.146，UAV2 最深的 S6 反而只有 1 次大形变，因此不能把“ID 多”当作内切根因。
3. **speed scale 不会触发 EGO 重新规划。** `/uav2/swarm/orbit_speed_scale` 只在 bridge 的 `PositionCommand` 回调后缩放 v/a/yaw_rate，不反馈到 EGO 的 goal、map 或 FSM。成功样本 scale 与半径偏差相关系数为 -0.0001。最深内切全部出现在 scale=1.0。部分调速段的平均跟踪误差从全速 0.052 m 增至 0.090 m，说明现有“位置不变、只缩放导数”的接口会放大局部跟踪瞬态，可能贡献 HOLD 附近的小回头，但不解释深度内切。
4. **HOLD cancel/resume 没有留下旧 B-spline。** 成功样本两次 cancel 后，traj_server 分别清空 ID 296、382 并停止命令；fresh goal 到达后才接受 ID 297、383。EGO goal generation、任务 candidate ID 与 bridge/traj_server 的 post-cancel generation 一致。恢复轨迹几何会从当前位置和新地图重新生成，但未发现旧新轨迹并行发布、错误续接或绕塔方向翻转。
5. **失败样本第 8 扇区的直接终止原因是项目 `planner_status_adapter` 在 UAV2 当前位置 0.55 m 内看到了膨胀占据点。** 13008.745 报错时，最近原始 FAST-LIO 点距机体 0.726 m，最近膨胀体仅 0.173 m；0.55 m 内有 17 个膨胀点。原始点云中最近的一簇点持续位于机体相对坐标约 `(-0.43~-0.63, ±0.11, +0.36~+0.56) m`，随飞机一起移动，且 peer filter 不过滤自身、ground filter 只过滤低于 0.2 m 的点。结合 0.25 m 地图分辨率和 0.40 m 膨胀，**高概率是机体自身回波未经自体滤除后被膨胀进安全判定半径**，不是实际撞入固定障碍。
6. 地图和定位时间在首次第 8 扇区报错处是新鲜且数值一致的：odom、原始 odom、原始/适配点云同 stamp，适配前后 odom 数值差 0、点云数据哈希相同；点云距事件 12 ms、膨胀地图距事件 33 ms。现有 bag 不含 `/tf`、最终 filtered cloud、EGO 原始 occupancy，因此不能做到数学意义上的完全排除，但证据明显反对 TF 数值变换错误、地图长延迟和旧地图残留。

责任域判定：**多个因素共同造成**。明显内切主要属于“任务到局部规划器的轨迹形状/走廊契约 + EGO 局部规划几何”；HOLD 附近瞬态属于“相位协同接口”；失败占据属于“地图感知预处理 + 状态适配器”；现有证据不支持把 traj_server 状态残留列为根因。

## 2. 审计范围、数据和方法

所有时间均为 bag 中 `/clock` 仿真时间。分析统一使用下列数据：

- 任务：`/uavN/tower_mission/state`、`current_target`、`current_sector`、`candidate_targets`；
- 实际运动：`/uavN/Odometry`、`/uavN/fast_lio/Odometry_raw` 和 10 Hz `uavN.csv`；
- 规划：`/uavN/planning/goal`、`planning/bspline`、`planning/pos_cmd`、`planner/status`；
- 相位：`/uavN/swarm/orbit_speed_scale`；
- 地图：`/uav2/cloud_registered`、`fast_lio/cloud_registered_raw`、`stage3/occupancy_inflate`；
- 状态转换和错误原因：对应运行的 `rosout.log`、`candidates.jsonl`。

正式窗口定义为各机首次 `ORBIT_RELEASE` 到首次离开正式绕塔状态。坐标统一到 world ENU：UAV2 本地 `camera_init` 的 x 加 home offset 4 m，塔心为 `(-10.0551, 19.7104)`。轨迹几何通过三阶 B-spline 控制点和 knot 向量重建。相邻新旧轨迹在共同有效时间上采样，RMS 大于 0.5 m 记为“大形变”。几何哈希只覆盖控制点、knot、阶数，不含 header stamp 和 trajectory ID。

实际局部曲率使用 7 点位置平滑和两侧约 0.5 s 三点外接圆估计；两侧位移低于 0.08 m 时视为 HOLD/近静止噪声并剔除。该曲率适合比较同一数据处理下的突变强弱，不当作机体动力学极限。

新增只读诊断产物位于：

- `test_evidence/stage5_uav2_trajectory_root_cause_audit_20260803/analyze_audit.py`
- `test_evidence/stage5_uav2_trajectory_root_cause_audit_20260803/audit_metrics.json`
- `test_evidence/stage5_uav2_trajectory_root_cause_audit_20260803/waypoint_statistics.csv`
- `test_evidence/stage5_uav2_trajectory_root_cause_audit_20260803/uav2_unified_timeline.png`

原始 bag、日志、CSV、JSON、PNG 均未覆盖或删除。

### 2.1 数据边界

两份 bag 没有录制 `/tf`、`/uav2/stage3/cloud_registered_filtered`、`cloud_registered_peer_filtered` 和 EGO 原始 `/grid_map/occupancy`。`stage3/occupancy_inflate` 是直接可见的最终膨胀地图。因此：

- 可以证明 frame adapter 前后数值未变，但不能逐条回放完整 TF 树；
- 可以证明原始点云存在机体邻近簇、最终膨胀图占据当前位置，但不能从 bag 直接查看该簇通过 peer/ground filter 后的逐点集合；
- 可以否定长时延和简单旧图残留，不能完全排除 EGO 内部未录制状态的瞬时异常。

## 3. 成功样本 UAV2 完整时间线

首次 `ORBIT_RELEASE` 为 12243.911；正式窗口开始时 UAV2 已从首点释放，依次执行 S2～S8，再回到 S1 完成闭圈，于 13011.100 进入 `WAIT_EXIT_PERMISSION`。以下 `r(min/mean)` 均为实际塔心半径；`κ95` 为实际绝对局部曲率的 95 分位。

| 时间 / s | 主点 / candidate | `/planning/bspline` ID | 新 goal 数 | scale min/mean/max | r min/mean / m | 半径偏差 mean / m | κ95 / 1/m | 规划轨迹最小半径均值 / m | 证据解释 |
|---|---|---:|---:|---:|---:|---:|---:|---:|---|
| 12244.0～12324.4 | S2 `l0_s1_c3` | 105～174（70，0.871 Hz） | 1 | 1/1/1 | 11.751/12.093 | -0.407 | 2.213 | 11.841 | 周期重规划，几何稳定 |
| 12324.6～12415.4 | S3 `l0_s2_c0` | 176～255（80，0.881 Hz） | 1 | 1/1/1 | 11.177/11.730 | -0.770 | 2.699 | 11.382 | B-spline 已轻度内切 |
| 12415.5～12504.0 | S4 `l0_s3_c0` | 256～331（76，0.859 Hz） | 2 | 0/0.907/1 | 11.322/11.825 | -0.675 | 2.368 | 11.574 | 12456.421 HOLD；同 candidate fresh goal 恢复 |
| 12504.2～12612.9 | S5 `l0_s4_c0` | 332～407（76，0.699 Hz） | 2 | 0/0.642/1 | 11.393/11.763 | -0.737 | 1.921 | 11.570 | 12555.722～12578.622 长 HOLD |
| 12613.0～12719.0 | S6 `l0_s5_c3` | 408～503（96，0.906 Hz） | 1 | 1/1/1 | **7.971/10.373** | **-2.127** | 3.179 | **8.879** | 最深内切；只有 1 次大形变，之后同拓扑稳定更新 |
| 12719.1～12825.0 | S7 `l0_s6_c0` | 504～599（96，0.907 Hz） | 1 | 1/1/1 | 11.107/12.201 | -0.299 | 5.189 | 11.327 | 半径恢复；曲率事件不对应 scale 变化 |
| 12825.2～12924.4 | S8 `l0_s7_c0` | 601～695（95，0.958 Hz） | 1 | 1/1/1 | **9.059/10.680** | **-1.820** | 4.788 | **9.621** | ID 610 出现一次极端几何跳变，随后纠正并稳定内切 |
| 12924.6～13011.0 | S1 `l0_s0_c3` | 696～772（77，0.891 Hz） | 1 | 1/1/1 | 9.710/11.324 | -1.176 | 2.317 | 10.183 | 闭圈段持续内切；无 speed/HOLD 触发 |

任务边界处会有一个 0.1 s 样本仍带前一 candidate/新 sector，故表中跳过持续时间为 0 的边界行；它不表示额外目标或倒序。

关键 B-spline 证据：

- S6 goal 于 12613.071 发布。ID 408 于 12613.090 生成，计划半径 min/mean=7.835/10.029 m，绕塔净角 +36.72°；ID 410 于 12615.123 与上一条共同有效段 RMS 仅 0.075 m。也就是说，EGO 一开始就选择了明显内侧路径，并在后续周期重规划中保持，而不是数十次更新逐渐“抖”进内侧。
- S8 ID 610 于 12830.580 短暂生成 min=2.281 m 的异常局部几何，与上一条 RMS=3.244 m、最大差=7.582 m；ID 611/612 随即修正，ID 614 于 12832.887 已回到 min=8.990 m，随后共同有效段 RMS=0.057 m。实际飞机没有到达 2.281 m，但这证明地图/局部优化可在同一目标下造成一次显著形变。
- 闭圈 S1 ID 696 于 12924.629 的 min=9.472 m；ID 697 于 12925.648 相对前一条 RMS=2.254 m，ID 698 后又降到 0.081 m，并稳定沿内侧拓扑执行。
- 以上轨迹的绕塔净角均为正；成功窗口没有一次 winding sign flip。内切不等于反向拓扑。

## 4. 失败样本 UAV2 完整时间线

失败窗口为 12238.210～13052.300，退出为 `FAILURE_LANDING`。S2/S3 与成功样本近似；差异从 S4 的多次相位 HOLD 开始，在 S6 进入无解/候选切换，最后在 S8 因当前位置占据形成持续失败。

### 4.1 主目标、候选和 trajectory ID

| 时间 / s | 主点 / candidate | trajectory ID / 更新 | scale mean | r min/mean / m | goal generations | 状态/错误 |
|---|---|---:|---:|---:|---:|---|
| 12238.3～12318.2 | S2 `l0_s1_c3` | 104～173 / 70 | 1.000 | 11.728/12.096 | 1 | 正常 |
| 12318.4～12409.2 | S3 `l0_s2_c0` | 174～255 / 82 | 1.000 | 11.156/11.722 | 1 | 正常 |
| 12409.4～12528.3 | S4 `l0_s3_c0` | 256～337 / 82 | 0.661 | 11.303/11.729 | 6 | 5 次 phase HOLD/fresh goal 重建 |
| 12528.4～12645.1 | S5 `l0_s4_c0` | 338～414 / 77 | 0.619 | 11.435/11.773 | 2 | 12571.745～12601.846 phase HOLD |
| 12645.2～12649.6 | S6 `l0_s5_c3` | 415～416 / 2 | 1.000 | 12.249/12.278 | 3 | 12645.345、12647.595 `NO_FEASIBLE_TRAJECTORY` |
| 12649.7～12668.5 | S6 `l0_s5_c9` | 417～432 / 16 | 1.000 | 12.235/13.397 | 2 | cancel/bounded retry，12666.545 无解 |
| 12668.6～12673.9 | S6 `l0_s5_c12` | 433～434 / 2 | 1.000 | 14.009/14.063 | 2 | 12668.746、12671.945 无解 |
| 12674.0～12687.4 | S6 `l0_s5_c18` | 435～444 / 10 | 1.000 | 13.237/13.687 | 2 | 12674.245、12685.445 无解 |
| 12687.5～12703.8 | S6 `l0_s5_c1` | 445～457 / 13 | 1.000 | 12.372/12.918 | 3 | 12687.645 无解；12701.795 HOLD |
| 12703.9～12740.1 | S6 `l0_s5_c4` | 458～490 / 33 | 0.858 | 12.332/13.390 | 2 | 12731.545 phase HOLD；12738.145 首次当前位置占据 |
| 12740.2～12831.2 | S6 `l0_s5_c7` | 491～571 / 81 | 0.999 | 13.722/14.377 | 2 | 12809.145 phase HOLD；随后完成 S6 |
| 12831.3～12915.0 | S7 `l0_s6_c0` | 572～644 / 73 | 1.000 | 12.249/12.932 | 2 | 继续完成 |
| 12915.1～12929.8 | S8 `l0_s7_c0` | 645～659 / 15 | 1.000 | 12.008/12.315 | 3 | 12919.845、12927.795 无解；bounded retry |
| 12929.9～13010.7 | S8 `l0_s7_c6` | 659～743 / 85 | 1.000 | **8.885/10.596** | 2 | 12932.096 retry 后规划稳定内切；13008.745 当前位置占据 |

13008.745 之后任务前向穷尽候选的完整序列如下：

| candidate 生效/报错时间 | 新 B-spline | 结果 |
|---|---:|---|
| 13010.945 `l0_s7_c12` | 0 | 当前位置占据；13012.995 bounded retry，13013.095 仍占据 |
| 13015.445 `l0_s7_c18` | ID 744（13017.552） | fresh goal 后生成一条，但状态适配器仍报当前位置占据 |
| 13019.795 `l0_s7_c1` | 0 | 13021.845 retry 后仍占据 |
| 13024.245 `l0_s7_c7` | ID 745～746 | 短暂生成两条，13026.395 再次占据 |
| 13028.596 `l0_s7_c13` | 0 | 持续占据 |
| 13032.945 `l0_s7_c19` | 0 | 持续占据 |
| 13037.295 `l0_s7_c2` | 0 | 持续占据 |
| 13041.645 `l0_s7_c8` | 0 | 持续占据 |
| 13045.995 `l0_s7_c14` | 0 | 13048.145 仍占据；13050.251 无安全候选 |
| 13052.296 | home goal | — | 返航也报当前位置占据，随后失败降落 |

这段不是旧 trajectory ID 反复“卡住”：任务持续发 fresh goal/bounded retry，但 `current_position_in_collision` 在目标变化之外保持为真，使新候选多数无法产生 B-spline。

### 4.2 S4/S5 HOLD 重建链

失败样本同一 S4 candidate `l0_s3_c0` 的 cancel→fresh goal 序列为：

- 12450.645 HOLD，12452.746 恢复：ID 296 → fresh ID 297；
- 12456.545 HOLD，12458.646 恢复：ID 300 → fresh ID 301；
- 12458.745 HOLD，12460.847 恢复：ID 301 → fresh ID 302；
- 12461.245 HOLD，12463.347 恢复：ID 302 → fresh ID 303；
- 12472.145 HOLD，12493.846 恢复：ID 311 → fresh ID 312。

S5 为 12571.745 HOLD、12601.846 恢复：ID 380 → fresh ID 381。每次任务 candidate 不变，goal header sequence 单调增加，恢复后的首轨迹均被分类为 `fresh_goal_or_state_rebuild`。

## 5. trajectory_id 更新分类

### 5.1 分类结果

| 样本/UAV | 更新数 | 频率 / Hz | 平均间隔 / s | 95% 间隔 / s | 周期/碰撞重规划 | fresh goal/状态重建 | 几何唯一数 | 大形变数 | winding 翻转 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 成功 U1 | 673 | 0.870 | 1.139 | 1.021 | 658 | 14 | 673 | 26 | 0 |
| 成功 U2 | 669 | 0.872 | 1.135 | 1.021 | 659 | 9 | 669 | 18 | 0 |
| 成功 U3 | 664 | 0.916 | 1.079 | 1.021 | 652 | 11 | 664 | 26 | 0 |
| 失败 U2 | 644 | 0.791 | 1.222 | 1.022 | 609 | 34 | 644 | 54 | 0 |

注：首条轨迹单独计 1 条，故分类相加还需加首条。

逐类判断：

1. **真正的新几何：是。** 所有 UAV2 B-spline 的控制点/knot 几何哈希都唯一。没有两个 ID 只是相同几何换时间戳。
2. **仅重新发布或更新时间戳：否。** 成功/失败样本精确几何重复数均为 0。
3. **speed scale 触发重规划：否。** speed scale 不进入 EGO；UAV2 S6/S8/S1 在 scale 恒为 1 时仍约 0.9～0.96 Hz 更新。
4. **地图更新触发：部分可能，但 bag 不能为每条轨迹唯一归因。** EGO FSM 的 `thresh_replan_time=1.0 s` 本来就会周期重规划，安全检查也可在碰撞预测时提前重规划。bag 没有记录“本次 replan 触发枚举”，所以 659 条只能合并标为周期/碰撞重规划，不能逐条武断归为地图变化。
5. **HOLD/cancel/resume 状态重建：可以识别。** 新 goal 后紧随的首条轨迹共成功样本 9 条、失败样本 34 条；它们与普通约 1 Hz 轨迹可按 goal receipt time 分开。

源代码契约与运行频率吻合：`advanced_param.xml` 将重规划时间阈值设为 1.0 s；`ego_replan_fsm.cpp` 每次 `reboundReplan` 成功都会构造新 `traj_id/start_time/control points/knots` 并发布 `/planning/bspline`，同时刷新 swarm trajectory chain。这解释了三机共同的约 1 Hz，而不是 UAV2 专有故障。

## 6. speed scale、轨迹更新、半径和小回头的因果检验

### 6.1 分组与相关性

成功 UAV2：

| scale 分组 | 时间占比 | 半径偏差 mean/min / m | 曲率绝对值 mean / 1/m | 平均跟踪误差 / m | trajectory 更新率均值 / Hz |
|---|---:|---:|---:|---:|---:|
| full (=1) | 86.30% | -1.040 / **-4.529** | 0.137 | 0.052 | 0.891 |
| reduced (0～1) | 10.24% | -0.846 / -1.128 | 0.106 | **0.090** | 0.960 |
| zero/HOLD | 3.46% | -1.040 / -1.178 | 0（静止样本剔除） | 0.122 | 0.100 |

Pearson 相关系数：

- scale vs 半径偏差：-0.0001；
- scale vs |局部曲率|：0.042；
- 5 s trajectory 更新率 vs 半径偏差：-0.146；
- 5 s trajectory 更新率 vs |局部曲率|：0.095。

这些数值不存在可复现的强线性关系。更关键的反事实是，S6/S8/S1 的 scale 均恒为 1，仍产生全部三段深内切；S6 更新率普通且只有一次大形变，却是最深内切。故“speed scale 改变 → 高频 ID → 深内切”这一因果链被现有数据否定。

### 6.2 小回头事件

成功 UAV2 共 13 个小回头，最大回撤 0.731°、最大反向路程 0.900 m。与 HOLD 明确重合的两段是：

- 12457.8～12463.8，回撤 0.416°、0.309 m，对齐 12456.421 HOLD 和 12460.723 fresh resume；
- 12556.9～12581.6，回撤 0.731°、0.900 m，对齐 12555.722 HOLD 和 12578.622 fresh resume。

其余事件在 12244.0～12246.0、12606.7～12609.1、12630.0～12633.1、12636.0～12638.3、12682.5～12688.7、12689.7～12691.7、12694.3～12697.2、12758.5～12760.0、12774.1～12775.5、12909.5～12921.5、12923.7～12927.9。它们多数发生在 scale=1 的普通周期重规划阶段，而非 scale 边沿。

因此只有 HOLD 附近的两次较大小回头与 cancel/resume 有明确时间关联；其余局部曲率/小回头没有稳定跟随 scale 变化。恢复后重建轨迹会产生一次几何跳变，这是合理的二级诱因，但不构成持续深内切的统一原因。

### 6.3 相位接口的代码级不一致

`ego_mavros_bridge.cpp:691-699` 保留 traj_server 当前时间对应的位置，只把 velocity、acceleration、yaw_dot 乘 scale；scale=0 时 `publishTrajectorySetpoint()` 才在当前位置锁存 HOLD。部分 scale 下，位置仍按原 B-spline 时间向前而导数被降低，p/v/a 不再是同一个时间参数化轨迹。这与 reduced 段跟踪误差升高相符，可解释局部跟踪瞬态；但因为 position 仍来自已内切 B-spline，也不会把规划几何改成新的内侧拓扑。

## 7. UAV1/UAV2/UAV3 对照

### 7.1 正式窗口整体对照（成功样本）

| UAV | 更新数/Hz | scale mean | 目标距离 mean / m | κ95 / 1/m | 半径 min/mean/max / m | 平均跟踪误差 / m | goal generations | 大形变 | HOLD 行为 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---|
| UAV1 | 673 / 0.870 | 0.926 | 9.079 | 2.499 | 9.297/12.203/16.176 | 0.056 | 15 | 26 | 7 段；含 phase 和规划失败恢复 |
| **UAV2** | **669 / 0.872** | **0.939** | **9.211** | **4.029** | **7.971/11.480/13.835** | **0.059** | **10** | **18** | 2 段 phase HOLD，第二段 22.7 s |
| UAV3 | 664 / 0.916 | 1.000 | 9.914 | 2.852 | 8.981/12.029/15.280 | 0.052 | 10 | 26 | 2 段短规划失败恢复 |

UAV2 并没有更高的总体 trajectory 更新频率或更多大形变；它的区别是曲率 95 分位最高、平均半径最低，并且同一条低半径规划拓扑在多个扇区被稳定执行。

### 7.2 相同目标阶段的空间对照

| 阶段 | UAV/candidate | 更新 Hz | scale | 目标距离 / m | r min/mean / m | κ95 | goals/大形变 |
|---|---|---:|---:|---:|---:|---:|---:|
| S6 | UAV1 `l0_s5_c13` | 0.895 | 0.930 | 8.047 | 12.293/14.626 | 2.956 | 2/6 |
| S6 | **UAV2 `l0_s5_c3`** | **0.906** | **1.000** | 8.877 | **7.971/10.373** | 3.179 | **1/1** |
| S6 | UAV3 `l0_s5_c0` | 0.986 | 1.000 | 9.855 | 12.300/13.293 | 2.121 | 2/3 |
| S8 | UAV1 `l0_s7_c9` | 0.881 | 1.000 | 7.942 | 11.928/12.167 | 2.142 | 2/1 |
| S8 | **UAV2 `l0_s7_c0`** | **0.958** | **1.000** | 9.917 | **9.059/10.680** | 4.788 | 1/7 |
| S8 | UAV3 `l0_s7_c9` | 0.915 | 1.000 | 9.957 | 8.981/10.648 | 4.423 | 2/4 |
| 闭圈 S1 | UAV1 `l0_s0_c15` | 0.908 | 1.000 | 9.530 | 9.297/10.864 | 1.913 | 1/3 |
| 闭圈 S1 | **UAV2 `l0_s0_c3`** | **0.891** | **1.000** | 8.340 | 9.710/11.324 | 2.317 | 1/1 |
| 闭圈 S1 | UAV3 `l0_s0_c0` | 0.907 | 1.000 | 11.876 | 10.089/11.253 | 2.013 | 1/1 |

对照得到三点：

1. 三机 S8 都能出现局部内切，UAV3 S8 甚至与 UAV2 接近，说明这是 EGO 局部轨迹形状的系统性能力边界，不是 UAV2 唯一的软件分支。
2. UAV2 的总体差异主要由 **S6 独有的 7.971 m 深内切**、S8 内切和闭圈内切连续叠加；UAV1/UAV3 在 S6 走了外侧/较外 B-spline。UAV2 S6 目标位于西侧，邻近已建模 telephone pole 区域，EGO 生成的内侧局部走廊明显比两机对应段更短；但 bag 未录制原始局部 occupancy，不能把具体障碍同伦类别完全证明。
3. UAV1/UAV3 各有一个安全原因明确的 14.5 m 主点，而 UAV2 八点均为 12.5 m；这会抬高两机整体半径，但不能单独解释 UAV2 低至 7.971 m，因为 UAV2 S6 的规划 B-spline 本身已远低于目标半径。

HOLD 对照同样不支持“UAV2 因恢复次数最多所以更差”：成功样本 UAV1 有 7 段 HOLD、26 次大形变，均多于 UAV2；UAV3 最大单次回头也大于 UAV2。UAV2 的长 phase HOLD 确实对应一次 0.900 m 小回头，但深内切发生在 HOLD 之后的全速新目标段。

## 8. 第 8 扇区 `CURRENT_POSITION_IN_OCCUPANCY` 证据链

### 8.1 首次报错的统一时间轴

任务日志首次 S8 报错为 13008.745，`planner/status` 在约 13008.712 已转为 collision。最近同步数据如下：

| 数据 | receipt / header stamp | frame | 位置/最近距离 | 与事件时差 |
|---|---|---|---|---:|
| `/uav2/Odometry` | 13008.741 / 13008.732990 | `uav2/camera_init` | local `(-14.9419,8.2646,3.0106)`；world `(-10.9419,8.2646,3.0106)` | header 12 ms |
| `/uav2/fast_lio/Odometry_raw` | 13008.740 / 同上 | `camera_init` | 数值与适配 odom 完全相同，delta=0 | 12 ms |
| `/uav2/cloud_registered` | 13008.742 / 同上 | `uav2/camera_init` | 3269 点；最近原始点 0.725537 m | 12 ms |
| `/uav2/fast_lio/cloud_registered_raw` | 同上 | `camera_init` | 3269 点，数据 SHA-256 与适配云相同 | 12 ms |
| `/uav2/stage3/occupancy_inflate` | 13008.712 / 13008.712 | `uav2/camera_init` | 21247 点；最近膨胀体 0.172506 m | 33 ms |

当时 UAV2 world 塔心半径约 11.48 m，并不在塔的实体包络内。最近原始点为 `(-15.5106,8.2195,3.4589)`，相对机体 `(-0.5687,-0.0451,+0.4483)` m。原始云在 0.55 m 内为 0 点、1.2 m 内为 353 点；膨胀图在 0.40/0.55/1.20 m 内分别为 7/17/166 点。`planner_status_adapter` 使用严格 `<0.55 m` 检查最终膨胀点，因此该报错有直接数据依据。

13010.945 下一 candidate 报错时仍相同：原始最近点 0.719631 m、膨胀体最近 0.135870 m，膨胀体在 0.55 m 内 18 点。不是单帧离群。

### 8.2 自身点云过滤

13007.283～13011.912 的原始最近点距机体始终约 0.687～0.754 m；其相对位置保持在机体附近，主要落在 x=-0.43～-0.63、y 约 ±0.11、z=+0.36～+0.56 m。一个固定 world 障碍不会在飞机移动时长期保持这种机体相对簇，因此它高度符合机架/桨/传感器安装结构的自身回波。

现有感知链代码也支持该判断：

- `teammate_cloud_filter_node.cpp` 仅以 1.2 m 包络删除 **peer** 点，并加入静态圆柱；没有 self pose/self exclusion volume；
- `ground_cloud_filter_node.cpp` 只删除 `z < 0.20 m`，这簇点 z≈3.46 m，必然不会被地面滤波删掉；
- `grid_map.cpp` 每帧先重置当前 local update buffer，再按 `ceil(0.40/0.25)=2` 个 XY cell、1 个 Z cell 膨胀。一个 0.726 m 原始点经过盒状膨胀和体素量化后落到离机体 0.173 m 是几何上可解释的；
- `planner_status_adapter_node.cpp` 优先使用 `occupancy_inflate`，在 0.55 m 内任一点即置 `CURRENT_POSITION_IN_OCCUPANCY`。

因此“自身点云过滤正常”这一假设不成立：管线没有专门的自身滤波，失败时也确实存在随机体运动的邻近点簇。

### 8.3 TF、地图延迟和旧地图残留

- **坐标数值转换错误：证据反对。** `frame_adapter_node.py` 对 odom/cloud 只 deep-copy 并重写 frame 名；bag 中适配前后 odom delta=0，点云数据哈希相同。若是 4 m home offset 被误加进本地 EGO 云，二者数值不会完全相同。
- **地图延迟：证据反对。** 报错帧 odom/cloud header 同为 13008.732990，最终膨胀图只早 21 ms、距任务报错 33 ms，远小于常见 map timeout；状态也没有报 `MAP_STALE`。
- **旧地图残留：低概率。** vendor cloud callback 每一新云都先 `resetBuffer(camera_pos ± local_update_range)`，而原始邻近簇每约 0.12 s 重新出现，膨胀占据也持续刷新。即使清掉旧图，下一帧仍会由自身回波重新产生占据。
- **真实进入障碍：低概率。** 当时塔心半径 11.48 m，world 位置不在已知塔/静态圆柱实体内；成功样本在相同后半扇区的对照时刻 12850 和 12900，原始最近点分别为 1.832、1.336 m，膨胀体最近 1.024、0.631 m，`planner/status` 未报 collision。这更像失败运行中特定视角/姿态产生自体回波，而非固定 world 障碍。

结论：第 8 扇区错误的**直接机制已证明**为“最终膨胀图占据了当前位置安全球”；其**高概率物理来源**为“未滤除的自体点云经 0.40 m 膨胀进入 0.55 m 判定半径”。由于 final filtered cloud 和 `/tf` 未入 bag，来源可信度评为高而非绝对。

## 9. HOLD/cancel/resume 状态一致性

成功样本两次完整链如下：

| HOLD | cancel 前最后轨迹 | fresh goal | post-cancel 首轨迹 | traj_server/bridge 证据 | 几何变化 |
|---|---|---|---|---|---|
| 12456.421，S4 `l0_s3_c0` | ID 296 @12456.182 | seq 6 @12460.771 | ID 297 @12460.781 | 12456.422 traj_server clear/stop；12460.787 bridge accepted 297 | 与旧轨迹共同有效采样 RMS 0.878 m；绕塔方向不变 |
| 12555.722，S5 `l0_s4_c0` | ID 382 @12555.063 | seq 8 @12578.671 | ID 383 @12578.681 | 12555.723 clear/stop；12578.687 accepted 383 | 长 HOLD 后剩余共同时间仅 0.382 s，形式 RMS 3.270 m；实际不存在并行执行 |

状态契约检查：

- `traj_server.cpp:31-38` 在 cancel 时设置 `receive_traj_=false` 并 `traj_.clear()`，定时发布立即停止；
- fresh goal stamp 必须新于 cancel；B-spline 的 start time/ID 必须通过 cancellation gate，否则拒绝；
- 新 B-spline 到达时 `traj_.clear()` 后用新位置轨迹及其一、二阶导数整体替换；
- bridge 同样在 gate active 时拒绝 old/cancelled ID，只在 fresh goal 后打印 `New post-cancel trajectory ... accepted`；
- 任务 candidate 在两次恢复中分别始终为 `l0_s3_c0`、`l0_s4_c0`，goal sequence 增加，EGO 新 traj ID 增加。

所以：

- traj_server 保留旧 B-spline：**否，日志和源码均否定**；
- EGO trajectory、goal generation、任务 goal ID 不一致：**未发现**；
- 新旧轨迹短时重叠或错误续接：**未发现实际命令重叠**；离线几何重叠指标只用于比较，不表示旧轨迹当时仍发布；
- 恢复后不同绕障拓扑：首条重建轨迹可明显不同，但所有成功/失败 UAV2 轨迹净绕塔方向均未翻转。bag 没有拓扑类标签和原始占据图，不能证明换了障碍同伦类。

HOLD 重建是可见的局部跳变来源，但现有 traj_server/EGO cancel gate 按设计工作，不是深内切或 occupancy storm 的直接根因。

## 10. 根因排序与可信度

### 10.1 已证明的直接原因

1. **EGO 下发的局部 B-spline 已经内切（置信度：很高，0.98）。** S6/S8/S1 的计划半径与实际半径一致，实际平均跟踪误差仅 0.047～0.058 m，scale=1。问题归属“任务到 EGO 的形状/走廊契约 + EGO 局部轨迹几何”，不是任务候选半径和 PX4 跟踪。
2. **高频 ID 是新几何的周期重规划，不是重复发布（0.99）。** 三机约 1 Hz，UAV2 每个几何哈希唯一；由 EGO 1.0 s FSM 阈值和成功即发布代码解释。
3. **speed scale 不触发 trajectory ID（0.99）。** 它只在 bridge 消费 `PositionCommand` 后缩放导数；深内切段全部 scale=1。
4. **失败报错由膨胀地图进入 0.55 m 当前位姿判定球直接触发（0.99）。** 13008.745 最近膨胀点 0.173 m、球内 17 点。
5. **cancel 后旧 traj 被清理（0.99）。** 源码、traj_server 日志、bridge post-cancel acceptance 和 ID 顺序四重一致。

### 10.2 高概率原因

1. **仅用八个稀疏主目标且没有连续径向走廊约束，EGO 在避障/长度优化中可合法选择内侧局部走廊（0.90）。** 这不是要求增加圆弧点；问题是 planner 接收到的是端点目标而非“端点 + 整段允许走廊/最小径向契约”。S6 从首条 ID 408 就稳定内切是最强证据。
2. **自体点云未过滤，经 EGO 膨胀后污染当前位置（0.90）。** 机体相对点簇、缺失 self filter、膨胀几何和同步时序共同支持。
3. **部分 speed scale 的 p/v/a 时间参数不一致放大 HOLD 邻近跟踪瞬态（0.80）。** reduced 段跟踪误差明显上升，两次大回头与 HOLD 恢复对齐；但它不造成 S6/S8 深内切。
4. **HOLD 后从新当前位置/新地图重建使首条轨迹发生跳变（0.80）。** 失败 S4 多次重建的 overlap RMS 约 0.54～0.98 m，长 HOLD 后更大；这是局部曲率/小回头的二级因素。

### 10.3 尚不能证明或证据反对

1. **每一次普通 trajectory ID 都由地图更新触发：不能证明。** 记录缺少 replan trigger enum；1 Hz 时间阈值本身足够解释大多数更新。
2. **HOLD 恢复选择不同障碍同伦拓扑：不能证明。** 无 topology label/raw occupancy，且 winding sign 无翻转。
3. **TF 错位/4 m home offset 重复应用：证据反对。** 适配前后数值和点云 hash 一致。
4. **长地图延迟或旧地图未清：证据反对。** 首错处时差仅 12～33 ms，local buffer 每云重置；更像每帧重新注入自体点。
5. **真实进入固定占据物：低概率。** world 位置、塔心半径、成功对照和机体相对点簇均不支持，但缺少完整最终 filtered cloud，不能绝对排除未建模细障碍。
6. **traj_server 旧轨迹并行执行：已否定。** cancel clear/stop 和 post-cancel gate 均有直接日志。

## 11. 下一轮最小修改位置和建议（本轮未实施）

按风险和证据强度排序：

1. **先修感知入口的 self exclusion，不动安全阈值。** 最小位置是 `teammate_cloud_filter_node.cpp` 或其后、ground filter 之前的独立预处理节点。使用时间同步 odom/传感器外参定义机体自遮罩体积，并发布“输入/自体删除/peer 删除/输出点数”和最近自体点诊断。不要修改 0.55 m、0.40 m、机间阈值或 world。
2. **给 EGO 外部接口增加连续径向走廊/轨迹验收契约，不增加主航点。** 在任务到 EGO 的适配层提供基于现有八边形边段的允许走廊，或在 B-spline 进入 traj_server/bridge 前检查整条计划轨迹是否违反既有塔净空/任务半径契约；拒绝时走现有 fail-closed/retry。不要增加中间圆弧点、改八扇区或修改 vendor 核心，除非外部方案证明不足后另行批准 vendor patch。
3. **把 phase scale 改为一致的时间重参数化。** 对同一个轨迹参数统一计算 p(t')、v(t')、a(t') 和 yaw rate，或将非零 scale 明确移到轨迹时间推进层；避免当前位置继续按原时间前进、导数却缩小。保持 scale=0 的当前位姿 HOLD 安全语义。
4. **保持 EGO 1 Hz 重规划参数不变，先补触发原因记录。** 目前更新率与三机一致，直接降频没有根因证据。应给每个 trajectory 记录 `trigger={time_threshold, collision, fresh_goal, retry}`、goal generation、map stamp/hash、控制点 hash和 overlap 指标。
5. **下一次短时复核必须录制关键缺口，而非直接重跑完整三机。** 先在无控制/单机或受控短窗录制 final filtered cloud、peer-filtered cloud、raw/inflated occupancy、`/tf`、planning cancel、goal generation、pos_cmd 和 MAVROS raw setpoint，验证 self exclusion 后 0.55 m 内膨胀点归零，再决定是否做完整验收。

建议不触碰：主航点数、12.5/14.5/16.5 Tier、八扇区、ENTRY_GATE、角色顺序、机间阈值、安全净空、world、飞行方向和 EGO 重规划周期。尤其不应以“降低地图膨胀/碰撞半径”掩盖自身点云污染。

## 12. 最终归属判断

| 现象 | 主要责任域 | 次要责任域 | 排除项 |
|---|---|---|---|
| UAV2 明显内切 | 任务→EGO 的连续走廊契约、EGO 局部 B-spline 几何 | 局部地图/障碍布局 | hard Tier、扇区顺序、PX4 跟踪、speed scale |
| 局部绕行/曲率突变 | EGO 普通/碰撞重规划及个别大形变 | HOLD 后状态重建、相位接口 | trajectory 重复时间戳 |
| 小回头 | phase HOLD/cancel/resume 瞬态（部分事件） | 普通局部重规划、导数缩放不一致 | 任务目标倒序 |
| 第 8 扇区持续占据 | 感知预处理缺少 self filter、膨胀图状态适配 | 特定视角/姿态的自体回波 | 长地图延迟、旧 traj、明显 TF 数值偏移 |
| 高频 trajectory ID | EGO 设计内约 1 Hz 周期重规划 | fresh goal/碰撞提前重规划 | UAV2 专有异常、仅重复发布 |

本轮为只读专项审计：未修改任务层、EGO/EGO-Swarm、traj_server、FAST-LIO、PX4 源码或任何参数/安全阈值，未执行控制飞行，未清理任何原始证据。
