# AstraDroneOpen 项目启动入口收尾审计报告

> 审计日期：2026-09-08  
> 审计分支：`scene01-3uav-circuit-mission`  
> 审计 HEAD：`43dd4bf0e4a565ae8dc0b6cedcf673d835b83ba8`  
> 最高优先级：不破坏任何已经跑通的成果。  
> 本报告整理前一轮只读审计结论；用户随后授权将结论保存为 Markdown。本次仅新增本文档。

## 审计结论与边界

**可以统一用户级启动方式，但当前没有可直接删除的启动文件。** 最稳妥的路线是在 `scripts/run_sh/` 统一参数和操作习惯，继续复用现有 launch、节点及任务配置，逐项证明替代关系后再考虑归档。

只读审计过程中没有修改、移动或删除文件，没有启动 ROS、Gazebo、训练或测试。审计开始和结束时，Git 均未报告工作区修改。本文档保存前复核的 HEAD 与审计基线一致。

盘点覆盖 78 个 `.sh`、123 个 `.launch`、2 个 launch XML，以及 294 个 Python 文件；其中包含依赖、测试和普通工具，并非全部属于用户级启动入口。扫描同时覆盖 README、AGENTS、studynote、教程及专题文档中的启动命令。

调用关系来自当前源码及本机可读的相关外部 launch。条件启动分支、外部依赖及二进制入口单独标记。**静态审计不等于启动或飞行验证；未检出文本引用不等于没有依赖。**

调用图仍有以下边界：

- 四个 `.bin` 为 stripped ELF，内部调用关系尚未证明。
- 用户其他终端中的环境变量、外部自定义 `controller_definition`、任意外部 world/plugin 不具备已证明的完整依赖闭包。
- Python 普通模块、测试与依赖包附带示例不全部列为日常操作者入口；它们不因此获得删除许可。
- 本次没有重新验收任何历史飞行、qualification、training 或 evaluation 成果。

## 1. 当前用户级启动入口及用途

### 1.1 `scripts/run_sh/` 内的 27 个脚本

“无控制”仍可能启动仿真、节点并写入工件，不表示只读。

| 入口 | 用途及当前行为 | 建议定位 |
|---|---|---|
| `three_uav_inspection.sh` | 三机巡塔公共启动器；默认无控制，支持 GUI、RViz、场景及录制参数 | 保留，当前核心入口 |
| `three_uav_multi_height_inspection.sh` | 三机多高度，选择单层或多层；禁止 Learning Speed | 保留，推荐主入口之一 |
| `three_uav_outdoor_village.sh` | 三机 outdoor 初始化；明确拒绝 `--control` | 保留独立初始化入口 |
| `sector_inspection.sh` | 单机八扇区任务；支持高度、层偏移、录制、停止 | 保留 |
| `ego_waypoint_inspection.sh` | 单机 EGO waypoint 任务 | 保留独立任务模式 |
| `fixed_orbit_inspection.sh` | 固定圆周任务；默认只做路线预览 | 保留预览及历史任务能力 |
| `ego_planner_stack.sh` | PX4、FAST-LIO、EGO、bridge 集成演示；默认无控制 | 保留开发/规划入口 |
| `pc_example.sh` | 旧 tmux 演示；默认启动旧自动控制，`--base-only` 才关闭该部分 | 优先归档候选 |
| `fixed_speed_baseline.sh` | `0.08/0.12/0.16/0.20` 固定速度基线及证据采集 | 历史实验入口 |
| `fixed_speed_baseline_batch.sh` | 固定 ID 的 12 次旧基线编排；内部传入 `--control` | 优先归档候选 |
| `high_speed_exploration.sh` | 固定速度与固定 run-ID 配对的旧高速实验 | 历史实验入口 |
| `high_speed_exploration_batch.sh` | 旧高速实验顺序编排 | 优先归档候选 |
| `high_speed_boundary_refinement.sh` | 旧高速边界补充实验 | 优先归档候选 |
| `learning_speed_manual_run.sh` | A/B 单次标定、代际参数及高速 qualification | 保留，共享实验执行器 |
| `learning_speed_manual_batch.sh` | A/B 六档历史标定矩阵；含基础设施重试逻辑 | 历史实验入口 |
| `learning_speed_postfix_batch.sh` | post-fix A/B 七档矩阵，固定历史目录与命名 | 优先归档候选 |
| `learning_speed_progressive_qualification.sh` | Environment B 逐档高速资格验证，失败即停 | 保留资格验证协议，停止作为普通演示入口 |
| `learning_speed_calibration.sh` | 对已运行系统启动 Observation C／标定数据采集 | 保留附加采集入口 |
| `learning_speed_sac_training.sh` | Worksite 正式 SAC training 编排 | 保留受限研究入口 |
| `learning_speed_forest_sac.sh` | Forest `preflight/training/smoke/evaluation` | 保留受限研究入口 |
| `prepare_hector_training_overlay.sh` | 检查或准备 Hector overlay；可能复制、移动和构建 | 保留环境准备工具 |
| `sector_inspection_record_bag.sh` | 单机扇区任务专项 bag | 保留，被主脚本调用 |
| `three_uav_self_filter_record_bag.sh` | 三机点云过滤专项证据 | 保留诊断录制配置 |
| `uav2_occupancy_record_bag.sh` | UAV2 占据图、定位、模型状态专项证据 | 保留诊断录制配置 |
| `sector_inspection_failure_injection.sh` | 向 `/planner/status` 发布故障状态 | 保留测试工具，不能作为只读诊断 |
| `echo.sh` | 旧单机 MAVROS topic 监视 | 归档候选 |
| `record.sh` | 旧 topic 集合录制，输出到 `~/bag` | 优先归档候选 |

### 1.2 `scripts/run_sh/` 外的入口

| 路径／入口 | 用途 | 建议定位 |
|---|---|---|
| `AstraDrone_ros1_ws/src/Swarm/scripts/run_dual_tower.sh` | 双机 `high/low` 配置、控制、融合、RViz、停止 | 将用户入口接入 `run_sh`，原文件暂保留 |
| `simulation/forest/launch_three_zone_forest.sh` | 构建 Forest 生成器并启动地图可视化 | 接入场景工具入口 |
| `simulation/forest/generate_forest_randomization_v1.sh` | 批量生成、保存并验证 Forest world | 资产生成工具，不能并入普通启动的隐式步骤 |
| `simulation/sim_workspace/src/craic_sim/scripts/prepare_craic_2026_runtime_world.sh` | 生成 CRAIC runtime world | 底层依赖，必须保留 |
| `scripts/tool/*.py`、`cpu_or_gpu_stress_test.sh`、`get_temp.sh` | 录制、分析、绘图、RC override、资源/通信诊断 | 工具入口，保留实现位置 |
| `scripts/env_sh/*.sh` | 系统、PX4、第三方环境安装与配置 | 安装维护入口，独立管理 |
| `scripts/pc_installer_x86.bin` | PC 安装器 | 内部调用 UNKNOWN，禁止据此清理 |
| `scripts/onboard_installer_x86.bin` | onboard 安装器 | 内部调用 UNKNOWN，禁止据此清理 |
| `scripts/build_AstraDrone_ros1_x86.bin` | 主工作空间构建器 | 内部调用 UNKNOWN，禁止据此清理 |
| `scripts/build_sim_workspace_x86.bin` | 仿真工作空间构建器 | 内部调用 UNKNOWN，禁止据此清理 |
| 依赖目录内构建、驱动及 Docker 脚本 | 依赖维护 | 不纳入项目启动脚本删减范围 |

## 2. 启动调用关系

本节使用 `包名/文件名` 表示 ROS 包中的 launch 或节点。条件节点只在相应参数启用时运行，不能将图中的所有分支理解为每次同时启动。

### 2.1 三机和双机主链

```text
three_uav_multi_height_inspection.sh
  └─ three_uav_inspection.sh
       └─ triple_tower_multi_height_inspection.launch
            └─ triple_tower_inspection.launch

three_uav_inspection.sh
  └─ triple_tower_inspection.launch

three_uav_outdoor_village.sh
  └─ triple_outdoor_village_initialization.launch
       └─ triple_tower_inspection.launch
            （显式关闭各机控制及任务相关能力）

triple_tower_inspection.launch
  ├─ triple_px4_mavros.launch
  │    ├─ gazebo_ros/empty_world.launch → gzserver、可选 gzclient
  │    ├─ single_vehicle_spawn_verified.launch × 已启用无人机
  │    │    └─ px4/px4、spawn_model_verified.py
  │    └─ mavros/px4.launch → mavros/node.launch → mavros_node
  ├─ dual_tower_inspection.launch，传入 start_sim=false
  │    └─ UAV1/UAV2 栈及公共协调、安全组件
  ├─ UAV3 对应传感器、定位、过滤及任务栈
  ├─ swarm_evidence_recorder.py
  ├─ swarm_rviz_diagnostics.py
  └─ 可选 RViz

run_dual_tower.sh
  └─ dual_tower_inspection.launch
       ├─ dual_px4_mavros.launch
       │    ├─ gazebo_ros/empty_world.launch
       │    ├─ px4/single_vehicle_spawn.launch
       │    └─ mavros/px4.launch
       └─ 双机定位、任务、协调及安全组件
```

各机及公共组件继续展开为：

| 组件 | launch／节点 |
|---|---|
| 定位 | `fast_lio/mapping_mid360.launch` → `fastlio_mapping` |
| 传感器 TF | `uav_sensor_frames.launch` → Xacro 参数生成、静态 TF |
| 公共坐标适配 | `frame_adapter_node.py` |
| 点云过滤 | `teammate_cloud_filter_node`、`self_cloud_filter_node` |
| 状态共享 | `swarm_state_publisher.py` |
| 任务栈 | `uav_tower_stack.launch` → `occupancy_stamp_adapter_node`、`sector_inspection_mission_node`、可选 `speed_adapter_node.py` |
| 规划执行 | `ego_gazebo_bridge.launch` → `advanced_param.xml` → `ego_planner_node`，以及 `traj_server`、`ego_mavros_bridge`、条件启用的过滤/waypoint/RViz 节点 |
| 公共安全 | `swarm_safety_node.py`、`px4_param_guard.py` |
| 公共协调 | `swarm_manager_node.py` |
| 可选融合 | `world_cloud_fusion_node` |
| 脚本收尾 | `rosbag record`、`plot_three_uav_trajectory.py` |

两条不能误判为重复的直接源码依赖：

- [多高度脚本](scripts/run_sh/three_uav_multi_height_inspection.sh)第 26 行执行 `three_uav_inspection.sh`。
- [三机总 launch](AstraDrone_ros1_ws/src/Swarm/astra_swarm_bringup/launch/triple_tower_inspection.launch)第 184 行包含 `dual_tower_inspection.launch`，并在第 199 行关闭其仿真启动，复用内部组件。

### 2.2 单机任务链

公共仿真底座：

```text
simulation/px4_sim_files/px4_launch/astra_launch/astra_example.launch
  ├─ px4/posix_sitl.launch
  │    ├─ px4/px4
  │    ├─ gazebo_ros/empty_world.launch
  │    └─ gazebo_ros/spawn_model
  └─ mavros/px4.launch → mavros_node
```

| 脚本 | 调用关系 |
|---|---|
| `ego_planner_stack.sh` | 公共底座 + `mapping_mid360.launch` + `ego_gazebo_bridge.launch` |
| `ego_waypoint_inspection.sh` | 公共底座 + FAST-LIO + `ego_waypoint_inspection.launch` → bridge/EGO + `ego_waypoint_mission_node` |
| `sector_inspection.sh` | 公共底座 + FAST-LIO + `sector_inspection.launch` → bridge/EGO + occupancy adapter + `sector_inspection_mission_node`；可调用专项录包和 `plot_inspection_trajectory.py` |
| `fixed_orbit_inspection.sh` | 预览只调用 `fixed_orbit_inspection.launch` → `tower_mission_node`、RViz；控制模式增加公共底座和 FAST-LIO |
| `pc_example.sh` | tmux 中启动 roscore、公共底座、FAST-LIO、默认 `offboard/autoarming_control.launch`，以及 `qgc` |

`fixed_orbit_inspection.launch` 的固定任务链不等同于 EGO 扇区任务链，不能只因两者都绕塔就互相替代。

### 2.3 固定速度、标定及高速实验链

```text
fixed_speed_baseline_batch.sh
  └─ fixed_speed_baseline.sh
       └─ fixed_speed_baseline_uav1.launch

high_speed_exploration_batch.sh
high_speed_boundary_refinement.sh
  └─ high_speed_exploration.sh
       └─ high_speed_exploration_uav1.launch

learning_speed_manual_batch.sh
learning_speed_postfix_batch.sh
learning_speed_progressive_qualification.sh
  └─ learning_speed_manual_run.sh
       ├─ high_speed_parameter_preflight.py（相应高速配置）
       └─ manual_fixed_speed_calibration_uav1.launch
```

这三个 UAV1 launch 均复用 `triple_tower_inspection.launch`，通过参数选择 UAV1 栈和控制权，并关闭 UAV2/UAV3 对应任务栈。

| 附加链 | 最终节点／脚本 |
|---|---|
| 固定基线、高速探索 | `observation_c_trajectory_fusion.launch` + `fixed_speed_baseline_recorder.py` + rosbag |
| 单次人工标定 | `calibration_data_collection.launch` + rosbag |
| `learning_speed_calibration.sh` | `calibration_data_collection.launch` → `calibration_data_recorder.py`，按参数启动 Observation C |
| Observation C | `observation_c_node.py` + 可选 `observation_v2_lidar_surrogate.launch` → `observation_v2_node.py` |
| 实验后处理 | `enrich_fixed_speed_baseline.py`、`summarize_fixed_speed_baseline.py`、`summarize_high_speed_exploration.py`、`summarize_manual_calibration.py` |
| post-fix／qualification 后处理 | `summarize_postfix_calibration.py`、`summarize_high_speed_progressive_qualification.py` |

### 2.4 Hector／SAC／Forest 链

```text
learning_speed_sac_training.sh
  ├─ prepare_hector_training_overlay.sh
  └─ hector_worksite_sac_training.launch
       ├─ sac_training_runner.py
       └─ hector_worksite_training_episode_reset.launch
            └─ hector_training_episode_reset.launch

learning_speed_forest_sac.sh
  ├─ prepare_hector_training_overlay.sh
  └─ hector_forest_sac_training.launch
       ├─ forest_map_manager.py
       ├─ sac_training_runner.py
       └─ hector_training_episode_reset.launch

hector_training_episode_reset.launch
  ├─ training_episode_reset_coordinator.py
  └─ hector_training_observation_c.launch
       ├─ truth_cloud_registration_adapter.py
       ├─ speed_adapter.launch → speed_adapter_node.py
       ├─ observation_c_trajectory_fusion.launch
       └─ hector_ego_training_backend.launch
            ├─ gazebo_ros/empty_world.launch
            ├─ hector_spawn_training_quadrotor.launch
            │    ├─ Xacro、spawn_model、robot_state_publisher
            │    ├─ message_to_tf、条件启用的 pose estimation
            │    └─ controller_definition
            │         → 当前 backend 指定的 Pose/Twist controller launch
            ├─ gazebo_truth_odometry_adapter.py
            ├─ advanced_param.xml → ego_planner_node
            ├─ traj_server
            ├─ position_command_to_hector.py
            └─ 条件启用的 hector_backend_qualifier.py
```

当前 [Forest shell](scripts/run_sh/learning_speed_forest_sac.sh)只接受 `preflight/training/smoke/evaluation`，没有 `demo` 模式；不能从历史记忆恢复旧参数。

基础 `hector_ego_training_backend.launch` 的默认无控制边界，不适用于所有上层入口：`hector_training_episode_reset.launch` 显式向 backend 链传入控制启用参数。不能因名称含 preflight、reset 或 observation 就推断其不启动控制。

### 2.5 场景、录制及诊断链

| 入口 | 最终调用 |
|---|---|
| `launch_three_zone_forest.sh` | CMake 构建 `create_random_forest` → Gazebo empty world → 执行生成器 |
| `generate_forest_randomization_v1.sh` | 读取 `forest_pool_v1.json` → 构建生成器 → Gazebo → 逐 seed 生成/保存 world → `verify_forest_randomization_v1.py` |
| `astra_craic_2026_runtime.launch` | `<param command>` → `prepare_craic_2026_runtime_world.sh` → `generate_craic_2026_world.py`；随后 PX4/Gazebo/MAVROS |
| 三个专项 `*_record_bag.sh` | `rosbag record`，各自独立 topic 集合 |
| `record.sh` | tmux → `rosbag record`，旧 topic 集合 |
| `echo.sh` | tmux → 四路 `rostopic echo` |
| `sector_inspection_failure_injection.sh` | `rostopic pub` → `/planner/status` |

### 2.6 直接 launch、包级示例及 Python 入口

以下入口来自实际 launch 文件及文档命令扫描，也需要在未来入口清单中得到定位。依赖包的示例不必全部升级为项目正式一键入口。

| 入口族 | 调用关系与用途 |
|---|---|
| YOLO 单机／三机 | `ppe_yolo_three_uav.launch` → 三次 `ppe_yolo_uav.launch` → `yolo_detect.py`；依赖显式 Python 环境和权重 |
| Offboard | `position_control.launch` → `position_control`；`autoarming_control/autoarming_Mult/continuous_trajectory.launch` → 相应数量的 `autoarming_control` 节点及 TF/RViz |
| EGO 自带模拟器 | `simple_run.launch` → `rviz.launch` + `swarm.launch`；`swarm/swarm_large` → 多个 `run_in_sim.launch` → `advanced_param.xml`、`simulator.xml`、`traj_server` |
| EGO 单机模拟 | `single_run_in_sim.launch` → 地图生成器、planner、traj_server、模拟感知与里程计 |
| Mock 地图 | `maze2d/maze3d/mockamap/perlin3d/post2d.launch` → `mockamap_node`，部分带 RViz |
| FAST-LIO 各传感器配置 | `mapping_*.launch` → mapping 节点及可选 RViz；`mapping_mid360_color` 还有 `freedom_node` |
| RC 避障、自动降落 | 分别启动 `rc_obstacle_avoidance_node`、`astra_static_land` |
| AprilTag／ArUco／跟踪 | 对应检测节点、TF/预测节点；`pix_tracker.launch` 还启动 `pix_tracker.py`、`target_pulisher.py` |
| Livox ROS1 驱动 | 对应 driver 节点及按配置启用的 RViz/rosbag |
| Livox ROS2 Python launch | `launch_ROS2/` 下 HAP、MID360、MID360s 和 mixed 的 Python launch；属于驱动附带能力 |
| 单传感器仿真 | Mid360 `test_pattern`、lidar `example`、RealSense `simulation*`/`hil*`/`description` → Gazebo、spawn、状态发布或 RViz |
| 其他场景 | CRAIC、dynamic avoidance → PX4/Gazebo/MAVROS；moving 模式增加 `obstacle_controller.py`；`env_map/start` → gzserver/gzclient |
| UGV | Hunter/Hunter SE 的 display、Gazebo、controller launch → 模型、控制器、状态发布、转向工具及相关 Python 节点 |
| 独立工具节点 | camera、fusion、pixel2map、pub_cam_info、rc_topic、rtk2local、topic/TF 转换等各自包内节点 |
| Learning Speed 独立接口 | `astra_drone_episode_uav1.launch` → `astra_drone_episode_runner.py`；`sac_runner_startup_contract.launch` → speed adapter + SAC runner |

Python 文件中的 ROS 节点、离线分析器和工具脚本不应全部移动到 `run_sh`。例如 `sac_training_runner.py`、reset coordinator、map manager、证据 recorder 都是 launch/CMake 安装链中的实现文件。

## 3. 功能重复与不能直接合并的差异

| 重复区域 | 判断 |
|---|---|
| ROS/PX4/Gazebo 环境加载 | 多个 shell 重复实现，适合以后提取公共函数；当前 source 顺序、`--extend` 和路径补偿存在差异，不能直接统一成任意一份 |
| 进程等待、停止、日志、录包 | 重复较多，但 tmux 与前台子进程生命周期不同，需要逐项迁移 |
| 三机普通／多高度入口 | 已经是公共执行器与参数配置入口的复用关系，不能删除公共执行器 |
| 双机／三机 launch | 存在结构复用，不是两份独立冗余实现 |
| 固定速度实验脚本 | 编排重复，但速度范围、ceiling、加速度、代际、重试和 terminal 合同不同 |
| 三个专项录包脚本 | 录制动作重复，topic 集合和诊断目的不同 |
| Worksite／Forest SAC | 共享 backend，但 Forest 多了地图池、切图及 evaluation seed 合同，不能仅替换 world 路径 |
| 固定圆周／waypoint／八扇区 | 是不同任务模式，不属于已证明可互相替代的重复 |
| 同名/近似传感器 launch | 多为设备、消息或可视化配置差异，不能按文件名清理 |

对 `.sh` 和 `.launch` 做字节内容比较后，只发现一组完全相同的 launch：

```text
AstraDrone_ros1_ws/src/Utils/livox_ros_driver/livox_ros_driver/launch/livox_lidar.launch
AstraDrone_ros1_ws/src/Utils/livox_ros_driver/livox_ros_driver/launch/livox_template.launch
```

它们仍可能承担不同的公开文件名接口，而且属于依赖包；**不建议在本次项目收尾中删除**。

## 4. 必须保留的底层 launch

以下文件及其配置、模型和节点依赖应列入保护清单：

| 保护对象 | 原因 |
|---|---|
| `triple_tower_inspection.launch` | 普通三机、多高度、outdoor 初始化及多个 UAV1 实验共同复用 |
| `dual_tower_inspection.launch` | 三机内部仍直接包含 |
| `triple_tower_multi_height_inspection.launch` | 当前多高度任务参数的明确配置入口 |
| `triple_outdoor_village_initialization.launch` | 固化只初始化、不控制的边界 |
| `triple_px4_mavros.launch`、`dual_px4_mavros.launch` | 仿真及飞控编排 |
| `single_vehicle_spawn_verified.launch` | 三机 verified spawn 流程 |
| `uav_tower_stack.launch`、`uav_sensor_frames.launch` | 各机任务、规划、传感器公共组件 |
| `ego_gazebo_bridge.launch`、`advanced_param.xml` | 当前 EGO 与执行桥主链 |
| 单机四个 inspection launch | 对应不同任务/预览能力 |
| `mapping_mid360.launch` | 当前 full-stack 定位主链 |
| Hector 全部 backend/reset/observation/controller launch | 训练执行、身份和 reset 合同 |
| Learning Speed adapter/Observation/collection/runner launch | 数据与动作链 |
| YOLO 两个 launch | 单机节点及三机复用 |
| `astra_example.launch` | 多个单机入口仍直接调用 |
| CRAIC runtime launch 及生成脚本 | 存在 `<param command>` 隐式执行关系 |
| EGO 模拟器、驱动、传感器、UGV 及测试 launch | 独立开发/依赖能力，未证明被新入口替代 |

统一用户入口不要求移动这些文件。它们继续留在 ROS 包中，维持 `$(find package)` 和安装规则。

## 5. 可归档的旧一键脚本

这里的“可归档”是候选分类，不是当前允许搬走。

### 5.1 优先候选

- `pc_example.sh`
- `echo.sh`
- `record.sh`
- `fixed_speed_baseline_batch.sh`
- `high_speed_exploration_batch.sh`
- `high_speed_boundary_refinement.sh`
- `learning_speed_postfix_batch.sh`

### 5.2 历史实验协议候选

以下适合保留为历史实验协议、退出日常入口清单：

- `learning_speed_manual_batch.sh`
- `fixed_speed_baseline.sh`
- `high_speed_exploration.sh`
- `learning_speed_progressive_qualification.sh`

### 5.3 归档限制

1. 批处理与执行器、分析器之间的调用要一起记录。
2. 许多脚本通过自身目录推导仓库根目录；直接移动会失效。
3. `run_dual_tower.sh` 暂不应归档，它仍是双机任务的用户入口。
4. `three_uav_inspection.sh`、`learning_speed_manual_run.sh`、overlay preparer 和被调用的 recorder，不属于当前可搬离的旧文件。
5. 可以先在文档中标为历史、保持原位置；真正物理归档另做迁移。

## 6. 新入口完全替代后可删除的对象

**当前确认可删除：0 个。**

未来有条件删除的对象主要是旧 shell 编排外壳：

| 文件／类别 | 删除前必须证明 |
|---|---|
| `echo.sh` | 新诊断入口覆盖原四路 topic 及 namespace 选择 |
| `record.sh` | 原录制需求有明确对应 profile，输出目录迁移已确认 |
| `pc_example.sh` | `base-only`、旧 Offboard 演示、FAST-LIO、QGC 功能已分别安置；教程及操作习惯完成迁移 |
| 固定 ID 的历史 batch 脚本 | 实验顺序、停止条件、基础设施重试、输出 schema 已保留为可追溯协议 |
| 专项 recorder shell | topic 集合、压缩、命名、拒绝覆盖等行为完全迁入新 recorder |
| 重复的公共 shell 实现 | 所有旧入口已改为调用新公共实现，路径引用、测试、安装及文档均完成迁移 |

删除判据至少包括：

- 静态依赖闭合，包括 shell、launch/XML、Python、测试、CMake 安装及文档。
- 参数与默认值等价，原控制、命名空间、坐标、任务和安全边界保持。
- 无控制验证通过。
- 原已验收场景具有迁移后的相应运行证据。
- 录制、退出、故障与结果表达行为等价。

训练入口的迁移不构成启动正式训练的授权，也不能用入口检查替代 qualification。

## 7. 推荐的最终 `scripts/run_sh/` 结构

建议保留按任务命名的入口，增加少量公共实现；不必把所有任务塞进一个巨大脚本。

以下是目标结构，标注新增的名称目前尚不存在：

```text
scripts/run_sh/
├── README.md
├── three_uav_inspection.sh
├── three_uav_multi_height_inspection.sh
├── three_uav_outdoor_village.sh
├── two_uav_inspection.sh                  # 新增双机用户入口
├── sector_inspection.sh
├── ego_waypoint_inspection.sh
├── fixed_orbit_inspection.sh
├── ego_planner_stack.sh
├── learning_speed_manual_run.sh
├── learning_speed_calibration.sh
├── learning_speed_sac_training.sh
├── learning_speed_forest_sac.sh
├── prepare_hector_training_overlay.sh
├── ppe_yolo.sh                            # 新增感知入口
├── record_evidence.sh                     # 新增录制入口
├── diagnostics.sh                        # 新增诊断入口
├── forest_scene.sh                       # 新增场景工具入口
├── lib/                                  # 新增公共实现，逐项迁移
│   ├── environment.sh
│   ├── arguments.sh
│   ├── process_lifecycle.sh
│   └── artifacts.sh
└── profiles/
    └── recording/                        # 经迁移核对的录制配置
```

任务、安全、训练参数继续由原 ROS YAML/launch 持有，避免 shell 配置再复制一份。底层 Python 实现、ROS launch、模型和插件继续留在原所属包中。

历史脚本完成对应迁移前保持原位置。安装器、构建器及依赖脚本继续与日常运行入口分开。

## 8. 统一启动脚本的参数设计方案

核心原则：统一参数含义，保留各入口原有任务边界和默认行为。以下为设计建议，不代表现有脚本已经支持全部参数。

### 8.1 公共参数

| 参数 | 推荐语义 |
|---|---|
| `--help` | 只显示帮助，不建目录、不 source 会产生副作用的流程 |
| `--print-plan` | 只显示将调用的组件、有效参数、输出位置和控制授权状态 |
| `--check-only` | 检查已有环境和文件；不构建、不启动节点、不生成 world |
| `--control` | 明确开启任务控制；初始化、录制和感知入口拒绝 |
| `--gui / --headless` | 仅选择 Gazebo GUI；与 RViz 分开 |
| `--rviz / --no-rviz` | 独立控制 RViz |
| `--single-layer / --multi-layer` | 互斥；选择已有审核配置，不通过 shell 重算任务语义 |
| `--record none\|light\|full` | 保留当前三机语义；不同任务需明确各自实际覆盖内容 |
| `--record-profile NAME` | 专项 self-filter、occupancy、control-chain 等录制配置 |
| `--run-id ID` | 安全字符校验、拒绝路径穿越及旧 ID 复用 |
| `--results-dir DIR` | 限于审核后的 `runtime_artifacts/` 范围，保留已有参数兼容 |
| `--duration SEC` | 明确为 wall time；到时停止不能记为任务成功 |
| `--world FILE` | 保留现有能力；打印实际 world，不隐式调整任务净空或路径 |
| `--attach` | 只影响终端呈现 |
| `--stop` | 只停止当前入口登记的本次运行，不按进程名称全局清理 |

`light` 不含完整点云和 Gazebo model states，不能替代占据图/模型状态专项证据；`none` 不保留任务证据，不能用于需要完整审计工件的验收。

### 8.2 专属参数及限制

- 双机：`--profile high|low`。
- 人工标定：保留 `--environment`、`--v-max`、代际、ceiling、加速度及现有 qualification 约束。
- Forest：保留当前四种 mode；明确说明其 `preflight` 是会运行仿真的流程，不能与新的只读 `--check-only` 混用。
- YOLO：`--namespace`／三机模式、`--model`、`--python`、`--device`。
- overlay：显式准备与 `--check-only` 分开。
- 多高度：保留 Learning Speed 禁用边界；不能添加通用参数绕过该限制。
- outdoor 初始化：继续拒绝 `--control`；不因统一解析器而开放飞行。
- 安全净空、机间距离、Reward、Observation C、Hector PID 等不因入口统一而重新设定。

### 8.3 已存在的目标形式

下面的形式当前已经存在；本报告仅记录，未执行：

```bash
./scripts/run_sh/three_uav_multi_height_inspection.sh \
  --multi-layer --control --gui --rviz --record light
```

当前源码中的多高度配置为 `26/20/14 m`，多层对应 `26→22 / 20→16 / 14→10 m`。后续整理应以[当前多高度 launch](AstraDrone_ros1_ws/src/Swarm/astra_swarm_bringup/launch/triple_tower_multi_height_inspection.launch)为基准，不恢复历史高度值。

### 8.4 结果与退出语义

进程退出码、任务结果和 qualification verdict 应分别记录。当前 Forest wrapper 会核对 summary，而 Worksite training wrapper 主要返回 roslaunch 状态；不能在公共函数中把退出码 0 一律解释为成功。

原失败、人工中断、超时、基础设施错误和正常任务结束必须保留各自语义，不能通过统一入口自动重试或改写为 PASS。

## 9. 删除风险、隐式依赖及文档问题

### 9.1 文件及执行风险清单

本节列出已确认的重点风险；未逐项列出的依赖文件同样没有删除许可。

| 风险对象 | 已确认的问题／依赖 |
|---|---|
| `three_uav_inspection.sh` | 多高度 shell 的直接被调用者 |
| `dual_tower_inspection.launch` | 三机 launch 的直接被包含者 |
| `sector_inspection_record_bag.sh` | 单机主脚本通过内部 component 调用 |
| `learning_speed_manual_run.sh` | 三类 batch/qualification 调用；高速 preflight 也检查其源码 |
| `prepare_hector_training_overlay.sh` | 两个 SAC shell 调用，且有测试直接引用其路径 |
| `scripts/tool/plot_three_uav_trajectory.py`、`plot_inspection_trajectory.py` | 任务停止/退出时才调用，容易被只检查启动过程遗漏 |
| 各 `summarize_*`、`enrich_*` | 收尾或批处理阶段使用，不能当作无主分析脚本删除 |
| 包内 Python 节点 | `catkin_install_python`、launch 和生成的 devel 包装器存在安装依赖 |
| `advanced_param.xml`、`simulator.xml` | 不以 `.launch` 结尾，但属于真正启动链 |
| Xacro、URDF、SDF、YAML、RViz、插件库 | 参数生成、模型加载、传感器和控制器的隐式依赖 |
| CRAIC prepare/generator | `<param command>` 会执行生成器；不加区分地展开 launch 参数也可能写文件 |
| Hector `controller_definition` | 动态选择 include 文件；基础 spawn 默认还可指向上游 controller launch |
| `runtime_artifacts/sac_python_packages/` | 被加入 `PYTHONPATH`，是运行依赖，不能当旧 run 数据删除 |
| 外部 PX4、Hector 源目录及 overlay | 仓库外依赖；入口收尾不能顺带修改或清理 |
| `.bashrc` 中 `astra`、`qgc` | 当前旧 `pc_example.sh` 依赖：前者 source 主工作空间，后者指向 QGroundControl AppImage |
| 旧 batch 脚本 | 多个没有外层 `--control` 开关，执行后内部直接传入控制参数 |
| `pc_example.sh`、`echo.sh`、`record.sh` | 会结束同名 tmux 会话；不能直接继承为统一入口默认行为 |
| `generate_forest_randomization_v1.sh` | 默认输出位于场景目录；显式 overwrite 模式会删除旧目标 world |
| `learning_speed_progressive_qualification.sh` | 后处理还指定根目录报告路径，并非所有输出都限于 run 目录 |
| 四个 `scripts/*.bin` | 内部依赖 UNKNOWN，不能靠脚本搜索结果判断可删除 |
| `AstraDrone_ros1_ws/src/Planner/ego-planner/CMakeLists.txt` | 当前为指向 `/opt/ros/kinetic/share/catkin/cmake/toplevel.cmake` 的失效符号链接；属于另一个构建审计问题，本轮保留 |
| 测试 launch、`.test`、测试代码 | 是迁移验收依赖，不能因不属于用户入口而清理 |

### 9.2 文档问题

| 文档 | 当前问题 | 后续处理方向 |
|---|---|---|
| [scripts/run_sh/README.md](scripts/run_sh/README.md) | 仍把整个目录描述成旧 tmux 脚本集合，与当前 27 个脚本明显不符 | 首先重写为当前入口索引，逐项说明控制、录制、输出和停止语义 |
| [AGENTS.md](AGENTS.md) 主要入口 | 未完整覆盖多高度、Forest、YOLO、专项录制和双机入口 | 仅在事实确认后更新入口索引，继续保留训练 NO-GO 边界 |
| [studynote.md](studynote.md) | 同时存在 shell training 入口和直接 launch 命令 | 明确操作者入口与开发调试入口，不将命令存在视为已获运行许可 |
| [三机 YOLO 文档](docs/05-三机YOLO部署与交接说明.md) | 仍要求第二终端直接 `roslaunch` | 增加 `ppe_yolo.sh` 后迁移用户命令，继续保持感知与飞行控制职责独立 |
| [安装教程](docs/01-安装脚本详解.md)、[ros.md](ros.md) | 仍提及本仓库未找到的 `demonstration.sh`、`onboard_example.sh`、`start_camera_with_retry.sh` 等旧名称 | 标明历史或外部来源，不作为当前可运行入口推荐 |
| `topic_tf_tran/readme.md` | 提到 `mjpeg_pub.py`，当前仓库未找到对应文件 | 标为待确认，不凭文档推断存在 |
| 部分上游文档 | 引用外部或未找到的 launch，如 `mavros_posix_sitl_room.launch`、`third_robot_world.launch` | 标明外部／历史／待确认，不据此删除上游能力 |

### 9.3 推荐后续工作顺序

1. 整理入口索引、兼容参数表和保护清单。
2. 只为缺失的用户入口增加薄包装，继续调用原实现。
3. 分别验证参数、默认控制状态、输出及退出行为。
4. 有必要时再逐项抽取公共 shell 实现，保留兼容路径。
5. 对应能力完成替代验证后，单独评审物理归档。
6. 完成依赖及运行验收后，再单独评审删除。

**物理归档、公共代码抽取及删除应分别验收，避免把目录收尾扩大成已经跑通主链的重构。本报告不授权清理文件、改变安全参数或启动正式训练。**
